# OS
Thumbs.db
Desktop.ini
.DS_Store

# IDE
.vscode/
.idea/

# Claude Code
.claude/

# Temp
*.tmp
~$*

# 내부 문서 (업로드 제외)
CLAUDE.md
다채움 포털 분석보고서.md
SFR_프론트엔드_반영사항_분석.docx
# 작업지시서 폴더 내용은 기본 제외 (디렉토리 자체는 추적 대상으로 유지하여 하위 화이트리스트 가능)
작업지시서/**
# 단, 일일 자동 점검에 필요한 시나리오 문서는 원격 에이전트가 받아야 하므로 추적
!작업지시서/일일점검_시나리오.md
# AIDT 연계 작업계획서 (구현 단계별 참조용)
!작업지시서/AIDT_연계_xAPI_구조_정비_계획서.md

# 의존성/빌드
node_modules/

# 환경/민감정보
.env
.env.*
cookies*.txt

# 데이터/업로드 (로컬 런타임)
data/
uploads/
server.log
*.log

# 테스트 산출물
test-results/
playwright-report/
test/e2e/.smoke-state/

# 큰 미디어/샘플 파일
*.mp4
KakaoTalk_*.jpg
testpdf.pdf
샘플-*.xlsx
선도교원 *.jpg
다차원 *.pdf

# 전체 Claude 워크스페이스는 제외 (.claude/settings.local.json, launch.json은 이미 위에서 제외)
.claude/worktrees/
.claude/agents/
.claude/tasks/
.claude/plans/

# 포함예정 폴더 (커밋 대상 아님)
포함예정/

# 별도 서브프로젝트 (자체 git 저장소)
06_채움cbt 실동작/

# 문항 생성 작업 산출물/백업 (로컬 전용, 커밋 제외)
backups/
output-chunks/
input-chunks/
input-chunks-micro/
scripts/test-data/
*.xlsx
AIDT*.pdf
tmp_*.json
gen_*.js

# DB 백업 (절대 커밋 금지)
deploy/migration-backup-*/
*.db
*.db-wal
*.db-shm
