# 다채움 포털 메인 (`/`, `public/index.html`) 전면 재설계 기획서

> 파일: `public/index.html`
> 작성일: 2026-05-06
> 작성자: UI 디자이너 서브에이전트
> 적용 범위: 포털 진입 페이지 전체 재구성. 4 역할(학생/교사/학부모/교직원) + 관리자.
> 참고 선행 기획서: `docs/spec_my_class_redesign.md`(채움클래스 통합 대시보드 — 4슬롯·오늘 할 일 패턴 차용)

---

## 0. 한눈에 보는 결정 10가지

| # | 결정 사항 | 근거 |
|---|----------|------|
| 1 | **배너 슬라이더 폐기** — 4 역할 전용 **단일 컴팩트 헤더**(140px)로 전환. 자동 슬라이드/캐러셀 제거. 학부모 슬라이드가 학생에게 노출되는 문제·시각 노이즈·로딩 비용 동시 해결 | 4 테스터 모두 "내 역할과 무관한 슬라이드 노출" 지적. h1 35.2px → 28~30px (공통 스케일 위반 해소) |
| 2 | **"오늘 할 일" 카드 신설**(히어로 섹션 대체) — 역할별 동적 문구 + 상위 3개 액션 + 우측 → 화살표. 학생 "마감 과제 2건 / 미응시 평가 1건 / 새 알림 3건", 학부모 "오늘 우리 아이"(자녀별 그룹), 교직원 "오늘 학사"(공지·결재) | 4 테스터 공통 1순위 요청. 진입 직후 "내가 할 일이 무엇인가" 답을 1초에 제시 |
| 3 | **4슬롯(개인 대시보드) 전면 재설계** — 라벨/숫자/클릭 이동을 4 역할 × 4슬롯 = 16개 모두 명시. "진행 중 과제" 같은 모호어 폐기 → "미채점 N건"·"미제출 학생 N명". 클릭 시 100% 페이지 이동(준비 중 토스트 금지) | 교사 "진행 중 과제" 모호 지적, 학생/학부모/교직원 "준비 중 토스트만 뜬다" 지적 |
| 4 | **활동 요약 섹션 신설** — 채움클래스/채움콘텐츠/스스로채움 3분할 카드. 각 영역마다 핵심 통계 1~2개 + "이동" 버튼 | 사용자 핵심 요구 6번(3대 서비스 활동 요약 + 클릭 유도) |
| 5 | **명예의 전당 미리보기 신설** — 이번 주 Top3(학습자/콘텐츠/클래스) 가로 카드 + "전체 보기" 링크. 진입점 0건 문제 해결 | 사용자 핵심 요구 5번. 학부모/교사 "자녀·우리반 학생 수상 알림 없다" 지적 |
| 6 | **인기 콘텐츠 카드 클릭 가능 + 필터링** — 본인 역할/교과/학년 기반 추천. 교사는 본인 교과·학년 우선, 학부모는 자녀 학년 우선. ❓(quiz) 썸네일 추상화 → 평이한 단색 아이콘 4종(영상/문서/이미지/링크)으로 절제 | 학생 "클릭 안 됨" + 교사 "교과 필터 없음" + 교직원 "❓ 5건 신뢰도 저하" |
| 7 | **인기 클래스 자기 제외 + 자기 클래스 별도 표시** — 본인 소유 클래스는 "내가 운영 중인 클래스 N개" 라벨로 분리. 랭킹은 외부 인기만 | 교사 "본인 클래스가 1·2위라 의미 훼손" 지적. 트렌드 발견 목적과 충돌 |
| 8 | **아이콘 절제 원칙** — 카드 헤더 1개(컬러 단색 16px) + 4슬롯 큰 이모지 폐기(컬러 dot 20px로 대체) + 모든 서비스 그리드는 컬러 라운드 박스 1개로 단순화. 명예의 전당·인기 영역은 본 영역 의미상 강조 아이콘 1개 허용 | 사용자 추가 원칙 1번. 디자이너 디테일 책임 1번(동일 기능 동일 레이아웃) |
| 9 | **공통 UI 스케일 전면 적용** — body 16→17px / h1 35.2→28~30px / card h3 18→20px / stat-label 12→15px / button label 14→16px. 폰트 사이즈 위반 0건 보장 | CLAUDE.md UI 공통 스케일. 학부모/교직원 모바일 12px 가독성 미달 지적 |
| 10 | **빈 상태 친절 카피 + CTA** — "없습니다" → "아직 가입한 클래스가 없어요. 코드로 가입하거나 만들어 볼까요?" + CTA 2개. 5원칙 (d) 적용 | 4 테스터 공통 빈 상태 무책임 메시지 지적 |

---

## 1. 목적과 배경

### 1.1 현재 문제 (4 테스터 종합)

| # | 문제 | 출처 | 우선순위 |
|---|------|------|---------|
| P1 | 슬라이드 배너가 역할 필터링 안 됨 — 학부모 슬라이드가 학생 첫 화면에 노출 | 학생 3.8/10 | 즉시 |
| P2 | 4슬롯 클릭 시 "준비 중" 토스트만 — 모든 카드가 죽은 길 | 학생·학부모 | 즉시 |
| P3 | 4슬롯 라벨 모호 ("진행 중 과제" → "미채점 N건"으로 변경 필요) | 교사 5.0/10 | 즉시 |
| P4 | 학부모 4슬롯("자녀 클래스/이번 주 학습/읽은 책/체인지") 모두 무의미 — 자녀 그룹핑·출결·미제출이 0 | 학부모 3.0/10 | 즉시 |
| P5 | 교직원 4슬롯("운영 클래스/문서 작성/이번 주 활동") — 학사 운영과 무관, 교사 로직 재활용 | 교직원 3.5/10 | 즉시 |
| P6 | 인기 콘텐츠 카드 클릭 링크 없음(no-link) | 학생 | 즉시 |
| P7 | 인기 콘텐츠가 교사 교과·학년 필터 없어 무관한 콘텐츠 노출 | 교사 | 단기 |
| P8 | 명예의 전당 진입점 0건 — 학부모/교사 자녀·우리반 학생 수상 시 알림 없음 | 4 역할 모두 | 즉시 |
| P9 | 모바일 "저장한 콘텐츠" 라벨 줄바꿈 + stat-label 12px 가독성 미달 | 학생·학부모 | 즉시 |
| P10 | 인기 콘텐츠 ❓(quiz) 썸네일 5건 신뢰도 저하 | 교직원 | 즉시 |
| P11 | 본인 소유 클래스가 "이달의 인기 클래스" 1·2위에 노출 → 자기 추천 의미 훼손 | 교사 | 단기 |
| P12 | 자녀별 그룹핑 UI 없음(다자녀 학부모 시나리오) | 학부모 | 단기 |
| P13 | 학사 일정·공지·결재 모두 빈 상태 — 교직원 진입 가치 0 | 교직원 | 즉시 |
| P14 | 폰트 사이즈 5종 공통 스케일 위반 (body 16/h1 35/h3 18/stat-label 12/quick-label 13.6/all-service 14.4) | CLAUDE.md 위반 | 즉시 |

### 1.2 재설계 6가지 목표 (사용자 핵심 요구)

| 목표 | 설명 | 본 기획에서의 답 |
|------|------|------------------|
| ① 개별 맞춤형 | 4 역할 × 본인 데이터 | 컴팩트 헤더·4슬롯·오늘 할 일 카드 모두 역할별 분기 |
| ② 새 콘텐츠/클래스/서비스 유입 | 신규 사용자 발견 유도 | 명예의 전당 미리보기·인기 콘텐츠/클래스(자기 제외)·추천 서비스 |
| ③ 나의 중요 정보 즉각 확인 | 오늘 할 일·마감·새 알림 | 1순위 섹션 "오늘 할 일" — 역할별 동적 카드, 상위 3개 액션, 화살표 즉시 이동 |
| ④ 사용자 트렌드 | 인기 콘텐츠/클래스 | 인기 콘텐츠(역할 필터·교과·학년) + 인기 클래스(자기 제외) |
| ⑤ 명예의 전당 진입 | 바로 진입 또는 요약 | 가로 3 카드 미리보기 + "전체 보기" + 자녀/우리반 학생 수상 시 알림 |
| ⑥ 채움클래스/콘텐츠/스스로채움 활동 요약 + 클릭 유도 | 3대 서비스 통합 요약 | "활동 요약" 신규 섹션 — 3분할 가로 카드, 각 영역 통계 1~2개 + "이동" 버튼 |

### 1.3 사용자 5원칙 적용 포인트

| 원칙 | 본 화면 적용 |
|------|--------------|
| (a) 진입 경로 | "오늘 할 일" 카드를 최상단(컴팩트 헤더 바로 아래)에 배치. 우측 → 화살표가 즉시 액션으로 이동 |
| (b) 현재 위치 피드백 | 컴팩트 헤더 우측에 역할 배지(이미 존재). 4슬롯 hover 시 좌측 라인 색 변경. 카드 클릭 시 즉시 페이지 전환 |
| (c) 복귀 동선 | 모달 우상단 × 버튼 + Esc 키. 명예의 전당 미리보기 카드는 별도 모달 대신 페이지 이동(`/class/hall-of-fame.html`) |
| (d) 빈 상태·오류 상태 | "없습니다" 한 줄 → 친절 카피 + CTA 2개. 예: "아직 가입한 클래스가 없어요. [코드로 가입] [클래스 만들기]" |
| (e) 평이한 한국어 | "진행 중 과제" → "미채점 과제 N건" / "운영 클래스" → "내가 만든 클래스 N개" / "이번 주 활동" → "이번 주 학사 일정 N건" |

### 1.4 추가 디자인 원칙 (사용자 명시)

- **아이콘 절제**: 카드 헤더에 1개(단색 16px), 4슬롯의 큰 이모지 폐기(컬러 dot 20px로 대체), 인기 콘텐츠 ❓ 썸네일 폐기(영상/문서/이미지/링크 4종 단색 아이콘으로 단순화), 모든 서비스 그리드는 컬러 라운드 박스 1개만. **카드당 아이콘 ≤ 2개**
- **공통 UI 스케일 엄수**: body 17 / h1 28~30 / card h3 19~20 / 본문 16 / 라벨 14~15 / 메타 13 / button 16 / badge 13. **금지선: body 12px 이하, button 14px 이하**

---

## 2. 사용자 페르소나 매트릭스 (4 역할 × 우선 정보 5)

### 2.1 페르소나별 첫 화면 정보 우선순위

| 역할 | 1순위 | 2순위 | 3순위 | 4순위 | 5순위 |
|------|-------|-------|-------|-------|-------|
| **학생** (student1) | 오늘 마감 과제·미응시 평가 | 새 알림(교사 피드백·답글) | 이번 주 활동 요약(스스로채움 진도·클래스 새글) | 명예의 전당 자기/우리반 등재 여부 | 인기 콘텐츠(학년 필터) |
| **교사** (teacher1) | 오늘 할 일(미채점·승인대기·마감 임박) | 미제출 학생 명단(반별) | 이번 주 활동 요약(우리반 출석·과제·평가 제출률) | 명예의 전당 우리반 학생/콘텐츠 등재 | 인기 콘텐츠(본인 교과·학년) |
| **학부모** (parent1) | 오늘 우리 아이(출결·미제출·교사 알림·감정) | 자녀별 활동 요약(다자녀 그룹) | 새 알림(교사 메시지·자녀 수상) | 명예의 전당 자녀 등재 알림 | 인기 콘텐츠(자녀 학년) |
| **교직원** (staff1) | 오늘 학사(공지·결재·일정) | 학사 일정 캘린더 미리보기(이번 주) | 활동 요약(공지 열람률·설문 응답률) | 명예의 전당 학교 홍보용 미리보기 | 추천 행정 도구(채움모니터·캔버스·한글웹에디터) |
| **관리자** (admin) | 오늘 운영(미승인 가입·시스템 알림) | 사용자/클래스 통계 대시보드 | 활동 요약(LRS 핵심 지표) | 명예의 전당(자기 학교 노출 여부 조회) | 인기 콘텐츠(전체) |

### 2.2 4슬롯 라벨/숫자/클릭 이동 (16개 + 5 = 25개 모두 명시)

| 역할 | 슬롯 1 | 슬롯 2 | 슬롯 3 | 슬롯 4 |
|------|--------|--------|--------|--------|
| **학생** | 참여 클래스 N개 → `/class/index.html` | 이번 주 학습 N건 → `/self-learn/index.html` | **새 알림 N건** → `/notifications.html`(또는 클래스 알림장) | 모은 포인트 N → `/growth/index.html` |
| **교사** | 내가 만든 클래스 N개 → `/class/index.html?filter=owned` | **미채점 과제 N건** → `/class/index.html?filter=ungraded` | **승인 대기 N건**(가입·갤러리 통합) → `/class/index.html?filter=pending` | 마감 임박 평가 N건 → `/cbt/index.html?filter=upcoming` |
| **학부모** | 자녀 N명 → `/growth/index.html?role=parent` | **미제출 과제 N건**(자녀 통합) → `/class/index.html?role=parent&filter=overdue` | **새 알림 N건**(교사 메시지·수상) → `/notifications.html` | 자녀 출결 양호도(이번 주 %) → `/growth/index.html?tab=attendance` |
| **교직원** | **미확인 공지 N건** → `/notice` | **결재 대기 N건** → `/admin/approval`(준비 시 안내) | 이번 주 학사 일정 N건 → `/calendar?role=staff` | 운영 클래스 N개(있을 때만) → `/class/index.html` |
| **관리자** | 전체 사용자 N → `/admin/users` | 전체 클래스 N → `/admin/classes` | 오늘 접속자 N → `/admin/stats` | **시스템 알림 N건** → `/admin/notifications` |

> ✱ **굵은 항목**은 즉시 행동 가능한(actionable) 슬롯. 0건일 때는 "오늘은 처리할 게 없어요 👍" 같은 칭찬 카피로 빈 상태 처리.

### 2.3 명예의 전당 표출 방식

| 역할 | 표출 방식 | 알림 트리거 |
|------|-----------|------------|
| 학생 | 이번 주 Top3 카드 + "내가 등재됐어요!" 강조 배지(있을 때) | 본인 등재 시 새 알림 1건 자동 발송 |
| 교사 | 이번 주 Top3 카드 + "우리반 학생 N명 등재" 요약 | 우리반 학생 등재 시 교사에게 알림(현재 누락 → API 추가 권고) |
| 학부모 | 이번 주 Top3 카드 + "자녀 등재" 강조 배지(있을 때) | 자녀 등재 시 학부모에게 알림(현재 누락 → API 추가 권고) |
| 교직원 | 이번 주 Top3 카드 + "우리 학교 등재 N명" 요약(학교 홍보용) | 학교 단위 등재 시 알림 |
| 관리자 | 이번 주 Top3 카드 + 전체 학교 비교 미니 차트(옵션) | 시스템 통계용 |

---

## 3. 새 화면 와이어프레임

### 3.1 데스크톱 (≥1200px) — 메인 레이아웃

```
┌─────────────────────────────────────────────────────────────────────────────┐
│ [공통 GNB ─ 다채움 / 채움클래스 / 채움콘텐츠 / 스스로채움 / ...  김선생  로그아웃] │
├─────────────────────────────────────────────────────────────────────────────┤
│ ┌─ COMPACT HERO (140px, 역할별 색상) ──────────────────────────────────┐    │
│ │ ┃●  안녕하세요, 이학생님 👋                  [학생 🎓]                │    │  ← h1 28~30px
│ │ ┃   오늘도 다채로운 하루 되세요                                       │    │  body 16
│ │ └────────────────────────────────────────────────────────────────────┘    │
├─────────────────────────────────────────────────────────────────────────────┤
│ ┌─ ⭐ 오늘 할 일 (히어로 카드, 역할별) ─────────────────────────────┐        │  ← 1순위
│ │  ① 마감 과제 2건  · 수학 단원평가(D-1) 외 1건           [→ 보기]  │        │  card h3 20
│ │  ② 미응시 평가 1건 · 영어 듣기평가                       [→ 응시]  │        │  body 17
│ │  ③ 새 알림 3건   · 김선생님 피드백 · 클래스 공지 외 1건  [→ 알림]  │        │
│ └──────────────────────────────────────────────────────────────────────┘    │
├─────────────────────────────────────────────────────────────────────────────┤
│ ┌─ 🔖 바로가기 (편집)──────┐ ┌─ 📊 나의 4슬롯 (역할 분기)────────────┐         │  ← 2순위
│ │ [채움  ][채움  ][채움  ] │ │ ●  4         ●  2        ●  3   ●  352│         │
│ │ [클래스][콘텐츠][CBT  ] │ │   참여 클래스   이번 주 학습  새 알림  포인트│         │
│ │ [성장  ][LRS   ][더보기]│ │   → 클래스      → 스스로채움  → 알림  → 성장│         │
│ └──────────────────────────┘ └────────────────────────────────────────┘    │
├─────────────────────────────────────────────────────────────────────────────┤
│ ┌─ 📈 활동 요약 (3분할, 가로 균등) ───────────────────────────────────┐       │  ← 3순위 (신규)
│ │ ┌─ 채움클래스 ──┐ ┌─ 채움콘텐츠 ──┐ ┌─ 스스로채움 ──┐                   │
│ │ │ 새글 12 / 신규│ │ 담은 자료 14건│ │ 오늘의 학습 8/10│                   │
│ │ │  공지 2     │ │ 추천 시청 3건 │ │ 오답노트 미해결 4│                   │
│ │ │     [→ 이동] │ │     [→ 이동] │ │      [→ 이동]   │                   │
│ │ └────────────┘ └────────────┘ └─────────────────┘                   │
│ └─────────────────────────────────────────────────────────────────────┘    │
├─────────────────────────────────────────────────────────────────────────────┤
│ ┌─ 🏆 명예의 전당 미리보기 (이번 주 Top3, 가로 3카드)  [전체 보기 →] ──┐       │  ← 4순위 (신규)
│ │ [👑 1위 김창의 │ [🥈 2위 이성장 │ [🥉 3위 박학습                        │
│ │   학습량 24차시│   콘텐츠 8개   │   리뷰 12건                           │
│ │   금성초 4학년 │   금성초 5학년 │   금성초 6학년                        │
│ │   📌 우리반 ★] │                │  📌 자녀]                              │  ← 본인 관련 강조 배지
│ └─────────────────────────────────────────────────────────────────────┘    │
├─────────────────────────────────────────────────────────────────────────────┤
│ ┌─ 🔥 인기 콘텐츠 (2/3) ───────────┐ ┌─ 🏅 인기 클래스 (1/3) ─────────┐     │  ← 5순위
│ │ [영상] 분수의 덧셈   👁 1,234 │ │ 1. 4학년 1반 김선생  · 32명     │     │
│ │ [문서] 한자성어 모음 👁 872   │ │ 2. 컴공 동아리       · 18명     │     │
│ │ [영상] 영문법 기초   👁 651   │ │ 3. 영어 회화반       · 15명     │     │
│ │ [이미지] 우주의 신비 👁 512   │ │ ※ 내가 운영하는 클래스 2개 별도 │     │
│ │ ※ 내 학년·교과 우선   [더보기]│ │                          [더보기]│     │
│ └────────────────────────────┘ └────────────────────────────┘           │
├─────────────────────────────────────────────────────────────────────────────┤
│ ┌─ 📢 공지사항 + 일정 (1/2) ────┐ ┌─ ⭐ 추천 서비스 (1/2, 역할별) ──┐         │  ← 6순위
│ │ [중요] 다채움 v2.5 업데이트   │ │ [LRS분석]    추천                │         │
│ │ [공지] 4월 평가 일정 안내      │ │ [채움CBT]    추천                │         │
│ │ ── 이번 주 일정 (3건) ──      │ │ [성장기록]   추천                │         │
│ │ 04/22 📚 수업 1교시           │ │                                  │         │
│ │ 04/23 📝 과제 마감             │ │                                  │         │
│ └────────────────────────────┘ └────────────────────────────────┘         │
├─────────────────────────────────────────────────────────────────────────────┤
│ ┌─ 🎯 모든 서비스 (역할별 정렬, 절제된 아이콘) ────────────[접기]──────┐      │  ← 7순위
│ │ [채움클래스] [채움콘텐츠] [스스로채움] [채움CBT] [성장기록] [LRS분석]│      │
│ │ [채움모니터] [채움캔버스] [한글웹] [채움코딩] [채움타자] [채움수학] │      │
│ │ [채움영어 ] [진로진학  ] [나우늘봄]                                │      │
│ └─────────────────────────────────────────────────────────────────────┘    │
└─────────────────────────────────────────────────────────────────────────────┘
```

### 3.2 태블릿 (768~1199px)

- 컴팩트 헤더 동일
- 오늘 할 일 카드 동일 (가로 풀)
- **바로가기 + 4슬롯**: 세로 2행 (각각 가로 풀)
- **활동 요약**: 3분할 유지(가로) — 카드 padding 축소
- **명예의 전당**: 가로 3카드 유지
- **인기 콘텐츠 + 클래스**: 1컬럼 세로 적층(콘텐츠 먼저, 클래스 다음)
- **공지+일정 + 추천 서비스**: 1컬럼 세로 적층
- **모든 서비스**: `auto-fill, minmax(160px, 1fr)`로 자동 정렬

### 3.3 모바일 (<768px)

```
┌─────────────────────────────┐
│ [공통 GNB ─ 햄버거 메뉴]    │
├─────────────────────────────┤
│ ┌─ COMPACT HERO 100px ───┐  │  ← h1 24px (모바일 축소)
│ │ ●  안녕하세요, 이학생님│  │
│ │     [학생 🎓]          │  │
│ └────────────────────────┘  │
├─────────────────────────────┤
│ ┌─ 오늘 할 일 (3건 적층)─┐  │
│ │ ① 마감 과제 2건  →     │  │
│ │ ② 미응시 평가 1건 →    │  │
│ │ ③ 새 알림 3건    →     │  │
│ └────────────────────────┘  │
├─────────────────────────────┤
│ ┌─ 4슬롯 (2x2 그리드)───┐  │
│ │ ┌──────┐ ┌──────┐     │  │  ← 라벨 14px 이상 (12px 금지)
│ │ │ 4    │ │ 2    │     │  │  ← 숫자 28px bold
│ │ │참여  │ │이번주│     │  │
│ │ │클래스│ │학습  │     │  │
│ │ └──────┘ └──────┘     │  │
│ │ ┌──────┐ ┌──────┐     │  │
│ │ │ 3    │ │ 352  │     │  │
│ │ │새알림│ │포인트│     │  │
│ │ └──────┘ └──────┘     │  │
│ └────────────────────────┘  │
├─────────────────────────────┤
│ ┌─ 바로가기 (가로 스크롤)┐  │
│ │ [○클래스][○콘텐츠]→   │  │
│ └────────────────────────┘  │
├─────────────────────────────┤
│ ┌─ 활동 요약 (세로 적층) ┐  │
│ │ 채움클래스 ────────→   │  │
│ │ 채움콘텐츠 ────────→   │  │
│ │ 스스로채움 ────────→   │  │
│ └────────────────────────┘  │
├─────────────────────────────┤
│ ┌─ 명예의 전당 (가로 스와이프) │
│ │ [Top1][Top2][Top3] ⇆   │  │
│ └────────────────────────┘  │
├─────────────────────────────┤
│ ┌─ 인기 콘텐츠 (1열)    ┐  │
│ │ ●분수의 덧셈 1,234👁  │  │
│ │ ●한자성어 모음 872👁  │  │
│ └────────────────────────┘  │
├─────────────────────────────┤
│ ┌─ 인기 클래스 ────────┐  │
│ └────────────────────────┘  │
│ ┌─ 공지사항+일정 ──────┐  │
│ └────────────────────────┘  │
│ ┌─ 추천 서비스 ────────┐  │
│ └────────────────────────┘  │
│ ┌─ 모든 서비스(2열 그리드)│  │
│ └────────────────────────┘  │
└─────────────────────────────┘
```

> 모바일 분기점: <768px. 본문 16px, **라벨/메타 14px(절대 12px 금지)**, 버튼 16px.

---

## 4. 컴포넌트 명세 (정확 픽셀)

### 4.1 컴팩트 헤더 (배너 슬라이더 폐기)

```
높이: 데스크톱 140px / 태블릿 120px / 모바일 100px
배경: 역할별 그라디언트(기존 색상 유지)
  - student: linear-gradient(135deg, #10B981, #06B6D4)
  - teacher: linear-gradient(135deg, #4A7CFF, #7C5CFC)
  - parent:  linear-gradient(135deg, #F59E0B, #EC4899)
  - staff:   linear-gradient(135deg, #6366F1, #8B5CF6)  ← 신규
  - admin:   linear-gradient(135deg, #1E293B, #475569)  ← 신규
padding: 24px 32px
border-radius: 16px
margin-bottom: 24px
좌측 콘텐츠:
  h1 (인사말): font-size 28~30px(데/태) / 24px(모), font-weight 700, color white, line-height 1.3
  desc: font-size 16px, color rgba(255,255,255,0.85)
우측: 역할 배지(이미 존재 .user-type-badge)
배경 모션: 기존 shape 3개 유지(미세 float). 단 `🌱` 등 큰 이모지 일러스트는 제거(아이콘 절제 원칙)
배너 도트 / 자동 슬라이드 인터벌 / 다른 역할 슬라이드: 모두 제거
```

### 4.2 "오늘 할 일" 카드 (히어로, 신규)

```
배경: white
border-left: 4px solid var(--primary)  ← 역할별 primary 변경
border-radius: 16px
padding: 20px 24px
box-shadow: var(--shadow)
margin-bottom: 24px

헤더:
  h3 ("⭐ 오늘 할 일") font-size 20px, font-weight 700, color var(--gray-900)
  아이콘: ⭐ 1개만(컬러 #F59E0B, 20px)
  헤더 우측: 새로고침 버튼(↻ 16px, gray-500)

본문 — 액션 아이템 1~3개(역할별):
  height: 64~80px(아이템당)
  padding: 14px 16px
  background: var(--gray-50)
  border-radius: 12px
  margin-bottom: 8px (마지막 0)
  display: flex; align-items: center; gap: 14px
  
  좌측 인덱스: ①②③ (font-size 18px, color var(--gray-400), font-weight 600)
  본문: 
    제목 font-size 17px, font-weight 600, color var(--gray-800)
    부제(메타) font-size 14px, color var(--gray-500), margin-top 2px
  우측 → 화살표: 28x28 원형, background var(--primary)/15, color var(--primary)
  
  hover: background white, border 1px solid var(--primary)/30, 화살표 → background primary/color white
  click: location.href = 해당 페이지 URL (역할별)

빈 상태: "🎉 오늘은 처리할 게 없어요!" + "여유 있게 활동을 즐겨보세요"  → 추천 콘텐츠 1건 표시
```

#### 4.2.1 역할별 액션 아이템 (예시)

| 역할 | 아이템 1 | 아이템 2 | 아이템 3 |
|------|---------|---------|---------|
| 학생 | 마감 과제 N건 → `/class/...` | 미응시 평가 N건 → `/cbt/...` | 새 알림 N건 → 알림장 |
| 교사 | 미채점 과제 N건 | 승인 대기 N건(가입·갤러리 통합) | 마감 임박 평가 N건 |
| 학부모 | 자녀 미제출 과제 N건 | 자녀 결석 알림(있을 때) | 새 교사 메시지 N건 |
| 교직원 | 미확인 공지 N건 | 이번 주 학사 일정 N건 | 결재 대기 N건 |
| 관리자 | 미승인 사용자 N명 | 시스템 알림 N건 | 오늘 가입 N건 |

### 4.3 4슬롯 카드 (개인 대시보드, 재설계)

```
.stats-grid: grid-template-columns: repeat(4, 1fr) ← 데스크톱 (현재 2x2 → 4x1)
            repeat(2, 1fr) ← 태블릿/모바일 (2x2)
gap: 12px

.stat-card:
  padding: 20px 16px (현재 14px → 확대)
  border-radius: 12px
  display: flex; flex-direction: column; gap: 4px (현재 가로 → 세로 적층)
  cursor: pointer
  transition: all 0.2s
  
  좌상단 dot: width 20px, height 20px, border-radius: 50%, background 역할별 색상
    (현재 큰 이모지 1.3rem → 단색 dot 20px로 절제)
  숫자 (.stat-value): font-size 32px, font-weight 700, color var(--gray-900)
    (현재 17.6px → 32px로 강조)
  라벨 (.stat-label): font-size 15px, color var(--gray-600)
    (현재 12px → 15px, 모바일 14px로 확대 — 공통 스케일 준수)
  보조 메타 (있을 시): font-size 13px, color var(--gray-500), margin-top 2px
    예: "지난 주 대비 +2건"
  
  hover: transform translateY(-2px), box-shadow var(--shadow), border-color var(--primary)
  active: transform translateY(0)
  click: location.href = 해당 페이지 URL (절대 토스트 금지)
  
  background: 역할별·슬롯별(기존 4가지 그라디언트 유지)
```

### 4.4 활동 요약 카드 (신규, 3분할)

```
컨테이너:
  display: grid; grid-template-columns: repeat(3, 1fr); gap: 20px
  태블릿 동일 / 모바일 1열 적층
  margin-bottom: 24px

각 카드:
  background: white
  border-radius: 16px
  padding: 24px
  box-shadow: var(--shadow)
  border-top: 4px solid (영역별 색상)
    채움클래스 #4F46E5 / 채움콘텐츠 #059669 / 스스로채움 #EC4899
  
  헤더:
    아이콘 1개(영역 색상 단색, 24px) + 영역명 h3 20px, font-weight 700
    예: "📚 채움클래스" / "📝 채움콘텐츠" / "🎯 스스로채움"
  
  본문 — 통계 1~2개:
    통계 라인: font-size 16px, color var(--gray-700)
      숫자 강조: font-size 20px, font-weight 700, color 영역색
      예: "새 글 12건 / 신규 공지 2건"
    margin-top: 12px / line-height 1.7
  
  하단 CTA:
    "이동 →" 버튼: width 100%, padding 12px, font-size 16px, font-weight 600
    background 영역색/10, color 영역색, border-radius 10px
    hover: background 영역색, color white
```

#### 4.4.1 영역별 통계 항목 (역할별)

| 영역 | 학생 | 교사 | 학부모 | 교직원 |
|------|------|------|--------|--------|
| 채움클래스 | 새 글 N / 새 공지 N | 미채점 N / 미제출 학생 N | 자녀 새 글 N / 새 알림 N | 미확인 공지 N / 새 게시글 N |
| 채움콘텐츠 | 담은 자료 N / 추천 시청 N | 내 콘텐츠 조회 N / 신규 추천 N | 자녀 시청 N / 추천 N | 행정 자료 N / 신규 등록 N |
| 스스로채움 | 오늘 학습 N/M / 오답 N | 우리반 진도율 N% | 자녀 진도율 N% | (해당 없음 → 추천 도구 영역으로 대체) |

### 4.5 명예의 전당 미리보기 (신규)

```
컨테이너:
  background: white
  border-radius: 16px
  padding: 24px
  box-shadow: var(--shadow)
  margin-bottom: 24px

헤더:
  h3 "🏆 이번 주 명예의 전당" font-size 20px, font-weight 700
  우측: "전체 보기 →" 링크 (font-size 15px, color var(--primary))

본문 — 가로 3카드:
  display: grid; grid-template-columns: repeat(3, 1fr); gap: 16px
  태블릿 3카드 유지 / 모바일 가로 스와이프(scroll-snap-x)

각 카드:
  background: var(--gray-50)
  border-radius: 12px
  padding: 16px
  display: flex; flex-direction: column; align-items: center; gap: 8px
  cursor: pointer (→ /class/hall-of-fame.html 또는 해당 사용자 프로필)
  
  순위 뱃지: 32x32 원형
    1위 #FCD34D→#F59E0B, 2위 #D1D5DB→#9CA3AF, 3위 #FBBF24→#D97706
    font-size 16px, font-weight 700, color white
  썸네일: 80x80 (학생 아바타 또는 콘텐츠 썸네일)
    border-radius: 12px
  이름/제목: font-size 16px, font-weight 600, color var(--gray-800)
    text-align: center
  메타: font-size 14px, color var(--gray-500)
    예: "학습량 24차시" / "금성초 4학년"
  
  본인 관련 강조 배지(상단 우측 절대 좌표):
    "📌 우리반" / "📌 자녀" / "📌 나"
    background var(--primary), color white, padding 3px 10px, border-radius 12px
    font-size 13px, font-weight 600
  
  hover: transform translateY(-3px), box-shadow var(--shadow-md)

빈 상태: "이번 주 등재된 사용자가 아직 없어요. 활동을 시작해 보세요!" + [활동 시작] CTA
```

### 4.6 인기 콘텐츠 카드 재설계 (썸네일 절제)

```
.content-item:
  cursor: pointer ← 필수 (현재 클릭 안 됨 문제 해결)
  click: window.location.href = `/content/detail.html?id=${id}` (또는 모달)
  
.content-thumb (썸네일):
  width 56x56, border-radius 14px
  font-size 24px(아이콘만), color white
  
  type별 단일 컬러 + 단일 아이콘 (❓ 폐기):
    video    🎬 background #EF4444
    document 📄 background #3B82F6
    image    🖼️ background #10B981
    link     🔗 background #6366F1
    quiz     ✏️ background #F59E0B  ← ❓ 대신 ✏️ (학습 의미 명확화)

.content-name: font-size 16px (현재 14.4px → 16px), font-weight 600, line-height 1.5
  -webkit-line-clamp: 2

.content-meta: font-size 14px, color var(--gray-500), gap 8px
  type 라벨 + 조회수

서버 측 추천 로직 강화:
  GET /api/contents/recommendations?role=student&grade=4&subject=math&limit=6
  - 역할 + 학년/교과 필터 적용 (현재 미구현)
  - 본인 학교/지역 우선
```

### 4.7 인기 클래스 자기 제외

```
서버 측: GET /api/class/popular?excludeOwnerId=<currentUserId>&limit=5
  - 본인 소유 클래스 자동 필터링 (현재 미구현)
  
프론트 표시:
  랭킹 1~5위 클래스 카드 + 본인 운영 클래스 별도 줄:
    "※ 내가 운영하는 클래스 N개 → /class/index.html?filter=owned"
    font-size 14px, color var(--gray-500), margin-top 12px, border-top 1px dashed
```

### 4.8 추천 서비스 (역할별 분기)

```
.recommend-content:
  display: flex; flex-direction: column; gap: 12px
  
각 추천 서비스 카드:
  padding: 14px 16px (현재 12px → 확대)
  background: linear-gradient(135deg, #FFFBEB, #FEF3C7) ← 노란 톤 유지
  border: 1px solid #FDE68A
  border-radius: 12px
  display: flex; align-items: center; gap: 14px
  cursor: pointer
  
  아이콘: 44x44 라운드 박스, 컬러 배경, 화이트 아이콘
  본문: 서비스명 16px / 설명 13px gray-600
  우측: "추천" 배지(13px, color #D97706) ← ⭐ 아이콘은 절제 위해 제거 또는 1개만
```

#### 4.8.1 역할별 추천 서비스

| 역할 | 추천 1 | 추천 2 | 추천 3 |
|------|-------|-------|-------|
| 학생 | 스스로채움 | 채움코딩 | 채움수학 |
| 교사 | LRS 분석 | 채움CBT | 성장기록 |
| 학부모 | 성장기록 | 진로진학 | 나우늘봄 |
| 교직원 | 채움모니터 | 채움캔버스 | 한글웹에디터 |
| 관리자 | LRS 분석 | 관리자 | 채움클래스 |

### 4.9 바로가기 + 4슬롯 (상단 섹션 재배치)

```
.top-section:
  현재: grid 1fr 1fr (가로 동등)
  변경: grid 1fr 2fr (바로가기 1, 4슬롯 2 → 4슬롯 강조)
  태블릿/모바일: 세로 적층(바로가기 → 4슬롯)
  
.quick-menu-card:
  padding: 20px (현재 16px → 확대)
  
.quick-menu-icon:
  width: 48x48 (현재 52x52 → 약간 축소, 라벨이 잘 보이도록)
  font-size: 1.4rem
  
.quick-menu-label: font-size 15px (현재 13.6px → 15px), font-weight 600
```

### 4.10 모든 서비스 그리드 (절제)

```
.all-services-grid:
  grid-template-columns: repeat(auto-fill, minmax(180px, 1fr))
  gap: 14px
  
.all-service-item:
  padding: 16px (현재 14px)
  
.all-service-icon:
  width: 44x44 (현재 42x42)
  font-size: 1.2rem
  border-radius: 12px (현재 12px 유지)
  
.all-service-name: font-size 16px (현재 14.4px → 16px), font-weight 600

추천 서비스 강조: ⭐ 아이콘 1개만, 우측 텍스트 ★ 제거
```

---

## 5. 인터랙션 상태

### 5.1 hover / focus / active / disabled

| 컴포넌트 | hover | focus(키보드) | active(클릭 중) | disabled |
|----------|-------|--------------|----------------|---------|
| stat-card | translateY(-2px) + shadow + border primary | outline 2px solid var(--primary) offset 2px | translateY(0) | opacity 0.5 cursor not-allowed |
| 오늘할일 아이템 | bg white + border primary/30 + 화살표 색 반전 | outline 동일 | bg primary/5 | (해당 없음, 빈 상태로 처리) |
| 활동요약 카드 CTA | bg 영역색 100% / color white | outline 동일 | bg 영역색 진하게 | - |
| 명예의 전당 카드 | translateY(-3px) + shadow-md | outline 동일 | - | - |
| content-item | bg white + border gray-300 + translateY(-2px) | outline 동일 | - | - |
| class-item | bg white + border gray-300 + translateX(4px) | outline 동일 | - | - |
| all-service-item | bg white + border gray-300 + translateY(-2px) | outline 동일 | - | - |

### 5.2 클릭 동작 (모두 즉시 페이지 이동, "준비 중 토스트" 모두 제거)

- 4슬롯: 8명세에 정의된 URL로 이동
- 인기 콘텐츠: `/content/index.html?id=<id>` 또는 모달 즉시 표시
- 인기 클래스: `/class/class-home.html?classId=<id>`
- 명예의 전당 카드: `/class/hall-of-fame.html?tab=<learners|creators|classes>`
- 활동 요약 CTA: `/class/index.html`, `/content/index.html`, `/self-learn/index.html`
- 외부 서비스 링크 없음(url:null) — 토스트 대신 **카드 자체 비활성**(opacity 0.5 + "외부 서비스" 배지)

---

## 6. 빈 상태 / 에러 상태

### 6.1 빈 상태 (5원칙 (d) 적용)

| 영역 | 데이터 0건 시 카피 | CTA |
|------|-------------------|-----|
| 오늘 할 일 | "🎉 오늘은 처리할 게 없어요! 여유 있게 활동을 즐겨보세요" | [추천 콘텐츠 보기] |
| 4슬롯(특정) | 슬롯별 친절 카피. 예: 미채점 0건 → "👍 모든 과제를 채점했어요!" | (생략) |
| 활동 요약 | "아직 시작한 활동이 없어요" | [채움클래스 가입하기] / [콘텐츠 둘러보기] |
| 명예의 전당 | "이번 주 등재된 사용자가 아직 없어요. 활동을 시작해 보세요!" | [활동 시작] |
| 인기 콘텐츠 | "추천 콘텐츠를 준비 중이에요. 잠시만 기다려 주세요" | [공개 콘텐츠 둘러보기] |
| 인기 클래스 | "등록된 클래스가 없어요" | [클래스 만들기] / [코드로 가입] |
| 공지사항 | "새로운 공지가 없어요. 모두 확인하셨네요!" | (생략) |
| 일정 | "이번 주에 예정된 일정이 없어요" | [일정 추가] |
| 자녀(학부모) | "자녀를 등록하면 자녀의 학습 현황을 확인할 수 있어요" | [자녀 등록] |

### 6.2 에러 상태 (5원칙 (d) 적용)

```
공통 에러 카드:
  배경: linear-gradient(135deg, #FEE2E2, #FECACA)
  아이콘: ⚠️ (24px)
  제목: "잠시 후 다시 시도해 주세요"
  설명: "데이터를 불러오는 중 문제가 발생했어요"
  CTA: [다시 시도] 버튼(클릭 시 fetch 재호출)
```

---

## 7. 반응형 분기

| 분기점 | 레이아웃 |
|--------|---------|
| ≥1200px | 메인 1컬럼(상단~하단 적층). top-section 1fr 2fr / content-grid 2fr 1fr / two-column-grid 1fr 1fr / 활동 요약 3분할 / 명예의 전당 3분할 |
| 768~1199px | top-section 세로 적층 / content-grid 1fr / two-column-grid 1fr / 활동 요약 3분할 유지 / 명예의 전당 3분할 유지 / 모든 서비스 auto-fill 160px |
| <768px (모바일) | 컴팩트 헤더 100px + h1 24px / 4슬롯 2x2 / **stat-label 14px 이상**(절대 12px 금지) / 바로가기 가로 스크롤 / 활동 요약 세로 적층 / 명예의 전당 가로 스와이프 / 모든 서비스 2열 그리드 |

---

## 8. 데이터 흐름 + API 매핑

### 8.1 기존 API 활용 (확장 권고)

| API | 현재 상태 | 권고 변경 |
|-----|-----------|----------|
| `GET /api/portal/my-summary` | ✓ 구현됨 (classCount/pendingHomework/...) | **staff/admin 분기 추가**: pendingApprovals(가입 승인+갤러리 승인 통합), staffNoticeUnread, todayStaffEvents, parentChildrenCount, parentOverdueCount, parentNewMessageCount, parentChildAttendanceRate |
| `GET /api/portal/hall-of-fame?period=weekly` | ✓ 구현됨 | **myInvolvement 필드 추가**: 본인/자녀/우리반 관련 등재 여부 표시 → 강조 배지 |
| `GET /api/portal/calendar?startDate=&endDate=` | ✓ 구현됨 | (변경 없음) |
| `GET /api/portal/trending?limit=5` | ✓ 구현됨 | (변경 없음) |
| `GET /api/contents/recommendations?limit=6` | ✓ 구현됨(역할 필터 없음) | **role/grade/subject 파라미터 추가**: 학생·자녀 학년 우선, 교사 본인 교과·학년 우선 |
| `GET /api/class/` | ✓ 구현됨 | **excludeOwnerId 파라미터 추가**: 본인 소유 클래스 자동 제외 |
| `GET /api/class/my` | ✓ 구현됨 | (변경 없음) |
| `GET /api/notice/<classId>` | ✓ 구현됨 | (변경 없음) |
| `GET /api/growth/reading` | ✓ 구현됨 | (변경 없음) |

### 8.2 신규 API 권고 (선택, 클라 측 집계도 가능)

| 신규 API | 용도 | 클라 측 대안 |
|---------|------|-------------|
| `GET /api/portal/today-action?role=` | "오늘 할 일" 카드 통합 데이터 (역할별 상위 3개 액션) | 기존 `/my-summary` + `/calendar` + `/notice/...` 조합으로 클라 측 가공 가능 (단, 호출 4건 필요) |
| `GET /api/portal/highlights` | 명예의 전당 + 자녀/우리반/본인 등재 정보 통합 | 기존 `/hall-of-fame` + `currentUser.role`로 클라 가공 가능 |
| `GET /api/admin/notifications` | 관리자 시스템 알림 | 기존 `/admin/...` 라우트 확장 |
| `GET /api/notifications` | 통합 알림 (학부모 자녀 수상·교사 우리반 수상 알림) | **추가 권고** — 현재 학부모/교사가 자녀·우리반 학생 수상을 알 길이 없음(P8 해결) |

### 8.3 클라 측 데이터 흐름

```
1. dacheim:user-loaded 이벤트 수신
   ↓
2. 병렬 fetch (Promise.all):
   - GET /api/portal/my-summary
   - GET /api/portal/calendar?startDate=today&endDate=+7d
   - GET /api/portal/hall-of-fame?period=weekly
   - GET /api/contents/recommendations?role=&grade=&limit=6
   - GET /api/class/?excludeOwnerId=<currentUserId>&limit=5
   - GET /api/class/my (활동 요약·인기 클래스 자기 분리용)
   - GET /api/portal/trending?limit=5
   ↓
3. 렌더 순서 (FCP 우선):
   - 컴팩트 헤더 (즉시, 사용자 정보만)
   - 오늘 할 일 (my-summary + calendar 결합)
   - 4슬롯 (my-summary)
   - 활동 요약 (my-summary + recommendations + reading)
   - 명예의 전당 미리보기 (hall-of-fame)
   - 인기 콘텐츠 / 인기 클래스
   - 공지사항 / 일정
   - 추천 서비스 / 모든 서비스
   ↓
4. 에러 발생 시 해당 영역만 에러 카드 표시 (다른 영역 영향 없음)
```

---

## 9. 체크리스트 (구현 후 더블체크용)

### 9.1 사용자 5원칙

- [ ] (a) 진입 경로: 첫 화면 1초 내 "오늘 할 일" 시인 가능
- [ ] (b) 현재 위치 피드백: 역할 배지·hover·active 상태 명확
- [ ] (c) 복귀 동선: 모든 모달 × 버튼 + Esc 키 닫기
- [ ] (d) 빈 상태/에러 상태: 9.6의 카피·CTA 모두 적용
- [ ] (e) 평이한 한국어: "진행 중 과제" → "미채점", "운영 클래스" → "내가 만든 클래스" 적용

### 9.2 디자이너 디테일 8책임

- [ ] 1. 동일 기능 동일 레이아웃: 4슬롯 카드 컴포넌트 1개로 모든 역할 사용
- [ ] 2. 요소 크기 비례: 4슬롯 숫자 32px / 라벨 15px / dot 20px 균형
- [ ] 3. 색상 코딩 일관성: 역할 색상 컴팩트 헤더·dot에 통일 적용
- [ ] 4. 공통 스케일 수치 명시: 본 문서 4장에 모든 픽셀 명시 완료
- [ ] 5. 반응형 분기점: 7장에 ≥1200 / 768~1199 / <768 모두 명시 완료
- [ ] 6. 인터랙션 상태: 5장에 hover/focus/active/disabled 모두 명시 완료
- [ ] 7. 역할 분기 UI 일관성: 4슬롯·헤더 컴포넌트 1개 + 데이터 슬롯만 역할별 분기
- [ ] 8. 기존 유사 기능 재활용: 채움클래스 통합 대시보드(`spec_my_class_redesign.md`)의 "오늘 할 일" 패턴·4슬롯 패턴 차용

### 9.3 글자 크기 위반 0건 (모든 항목 ≥ 14px)

- [ ] body 17px (현재 16px → 변경)
- [ ] h1 28~30px (현재 35.2px → 축소)
- [ ] h3 19~20px (현재 18px → 확대)
- [ ] stat-label 15px (현재 12px → 확대)
- [ ] stat-value 32px (현재 17.6px → 강조)
- [ ] notice-text 15px (현재 14.08px → 확대)
- [ ] all-service-name 16px (현재 14.4px → 확대)
- [ ] quick-menu-label 15px (현재 13.6px → 확대)
- [ ] 모바일 stat-label 14px 이상 (절대 12px 금지)
- [ ] button label 16px (현재 일부 14px → 확대)

### 9.4 아이콘 절제 (카드당 ≤ 2개)

- [ ] 컴팩트 헤더: 큰 일러스트 이모지(🌱📚🎯) 제거. 역할 배지 1개만
- [ ] 4슬롯 카드: 큰 이모지 제거 → 컬러 dot 20px로 대체
- [ ] 인기 콘텐츠 ❓ 썸네일 폐기 → ✏️ 또는 단순 4종(영상/문서/이미지/링크)
- [ ] 추천 서비스 ⭐ 다발(3개) → 1개만(헤더에)
- [ ] 모든 서비스 별표(★) 텍스트 제거(아이콘 1개로 충분)
- [ ] 카드 헤더 아이콘은 1개(컬러 단색 16px)만

### 9.5 4슬롯 클릭 이동 100% (5 역할 × 4슬롯 = 20개 모두 검증)

- [ ] 학생 슬롯 1~4: 클릭 시 페이지 이동(준비 중 토스트 0건)
- [ ] 교사 슬롯 1~4: 동일
- [ ] 학부모 슬롯 1~4: 동일
- [ ] 교직원 슬롯 1~4: 동일
- [ ] 관리자 슬롯 1~4: 동일

### 9.6 명예의 전당 진입점

- [ ] 미리보기 가로 3카드 표시
- [ ] "전체 보기 →" 링크 우상단 표시
- [ ] 본인/자녀/우리반 등재 시 강조 배지 표시
- [ ] 자녀·우리반 학생 등재 시 알림 발송 (학부모/교사) — API 신규 권고

### 9.7 모바일 검증

- [ ] stat-label 14px 이상
- [ ] 4슬롯 2x2 그리드 (가로 스크롤 없음)
- [ ] 라벨 줄바꿈 0건 ("저장한 콘텐츠" 같은 라벨 한 줄에 들어가도록)
- [ ] 컴팩트 헤더 100px + h1 24px
- [ ] 명예의 전당 가로 스와이프 동작

### 9.8 역할별 분기 검증

- [ ] 학생 로그인: 학생 슬라이드 + 학생 4슬롯 + 학생 추천
- [ ] 교사 로그인: 교사 슬라이드 + 교사 4슬롯 + 교사 추천 + 본인 클래스 인기랭킹 제외
- [ ] 학부모 로그인: 학부모 슬라이드 + 자녀 그룹 + 학부모 추천 + 자녀 출결
- [ ] 교직원 로그인: **교직원 전용 컴팩트 헤더(신규)** + 교직원 4슬롯 + 교직원 추천(채움모니터·캔버스·한글웹)
- [ ] 관리자 로그인: 관리자 컴팩트 헤더 + 관리자 4슬롯 + 관리자 페이지 진입점

---

## 10. 부록 — 핵심 결정사항 빠른 요약

| # | Before | After | 근거 |
|---|--------|-------|------|
| 1 | 자동 슬라이더 3종, 학부모 슬라이드가 학생에 노출 | 컴팩트 헤더 단일 (역할별 색상만 분기), 5 역할 모두 지원 | P1 + 시각 노이즈 |
| 2 | "오늘 할 일" 섹션 없음 | 1순위 히어로 카드, 역할별 동적 액션 3개 | P2/3/4/5/13 통합 해결 |
| 3 | 4슬롯 라벨 모호 "진행 중 과제" | "미채점 N건" 등 actionable 라벨 | P3 |
| 4 | 4슬롯 클릭 시 토스트만 | 100% 페이지 이동 (5 역할 × 4슬롯 모두 정의) | P2/6 |
| 5 | 학부모/교직원 4슬롯 무의미 | 학부모 "오늘 우리 아이" / 교직원 "오늘 학사" 역할별 재설계 | P4/5 |
| 6 | 인기 콘텐츠 ❓ 5건 노출 | type별 4종 단순 아이콘(영상/문서/이미지/링크) + 클릭 가능 | P6/10 |
| 7 | 본인 클래스가 인기 클래스 1·2위 | excludeOwnerId 파라미터로 자기 제외, 본인 운영 별도 줄 | P11 |
| 8 | 명예의 전당 진입점 0건 | 가로 3카드 미리보기 + 강조 배지 + 알림 권고 | P8 |
| 9 | 폰트 5종 위반 | 9.3 체크리스트 9개 항목 모두 공통 스케일 적용 | P14 |
| 10 | 빈 상태 "없습니다" 한 줄 | 9.6 친절 카피 + CTA 모두 정의 | 5원칙 (d) |

---

## 11. 페르소나 매트릭스 1페이지 요약 (다른 디자이너·개발자 즉시 참고용)

```
                │ 학생          │ 교사            │ 학부모        │ 교직원         │ 관리자
────────────────┼──────────────┼────────────────┼──────────────┼───────────────┼─────────────
컴팩트 헤더 색  │ 그린/시안   │ 블루/퍼플       │ 오렌지/핑크   │ 인디고/퍼플    │ 슬레이트
오늘 할 일 1    │ 마감 과제   │ 미채점 과제     │ 자녀 미제출   │ 미확인 공지   │ 미승인 사용자
오늘 할 일 2    │ 미응시 평가 │ 승인 대기       │ 자녀 결석     │ 학사 일정     │ 시스템 알림
오늘 할 일 3    │ 새 알림     │ 마감 임박 평가  │ 새 교사 메시지│ 결재 대기     │ 오늘 가입
4슬롯 1         │ 참여 클래스 │ 내가 만든 클래스│ 자녀 N명      │ 미확인 공지   │ 전체 사용자
4슬롯 2         │ 이번 주 학습│ 미채점 과제     │ 미제출 과제   │ 결재 대기     │ 전체 클래스
4슬롯 3         │ 새 알림     │ 승인 대기       │ 새 알림       │ 학사 일정     │ 오늘 접속자
4슬롯 4         │ 모은 포인트 │ 마감 임박 평가  │ 자녀 출결률   │ 운영 클래스   │ 시스템 알림
활동 요약 강조  │ 스스로채움  │ 채움클래스      │ 채움클래스    │ 채움콘텐츠   │ LRS
명예의 전당 강조│ 본인 등재   │ 우리반 학생 등재│ 자녀 등재     │ 학교 등재     │ 전체 통계
인기 콘텐츠     │ 학년 필터   │ 본인 교과·학년  │ 자녀 학년     │ 행정 자료    │ 전체
추천 서비스 3   │ 스스로채움/ │ LRS/CBT/        │ 성장기록/     │ 채움모니터/  │ LRS/관리자/
                │ 코딩/수학   │ 성장기록        │ 진로진학/     │ 캔버스/한글웹│ 클래스
                │             │                 │ 나우늘봄      │              │
```

---

## 12. 구현 우선순위 (개발자 가이드)

### Phase 1 (즉시 — 사용자 핵심 요구 ①②③ 충족)
1. 컴팩트 헤더 (배너 슬라이더 폐기) — staff/admin 추가
2. "오늘 할 일" 카드 (히어로) — 역할별 동적 카드
3. 4슬롯 재설계 — 라벨 변경 + 클릭 이동 100% (16개 + 5)
4. 폰트 사이즈 공통 스케일 적용 (9.3 9개 항목)
5. 빈 상태 친절 카피 적용

### Phase 2 (단기 — 사용자 핵심 요구 ④⑤⑥ 충족)
6. 활동 요약 카드 (3분할, 신규 섹션)
7. 명예의 전당 미리보기 (가로 3카드)
8. 인기 콘텐츠 클릭 가능 + 썸네일 4종 절제
9. 인기 클래스 자기 제외 (`excludeOwnerId` 파라미터)
10. 추천 서비스 역할별 분기

### Phase 3 (중기 — API 강화)
11. `/api/portal/my-summary` staff/admin/parent 확장
12. `/api/contents/recommendations` role/grade/subject 필터
13. `/api/notifications` 자녀·우리반 수상 알림 추가

---

> **본 기획서는 `docs/spec_my_class_redesign.md`의 "오늘 할 일"·"4슬롯 역할별 분기" 패턴을 포털 메인에 일관되게 확장한 결과물이다. 두 페이지의 4슬롯 컴포넌트는 동일 컴포넌트로 재사용해야 하며(디자이너 디테일 8책임 7번), 향후 다른 페이지(예: 채움콘텐츠·스스로채움)에서도 동일 패턴을 적용해 다채움 전체의 통합 인지도를 강화한다.**
