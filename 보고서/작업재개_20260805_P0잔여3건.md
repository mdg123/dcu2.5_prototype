# 작업 재개 메모 — 2026-08-05 17:50 중단분

컴퓨터 종료(18:00)로 **P0 잔여 3건 수정 작업을 중간에 세웠다.** 내일 이어서 진행.

## 중단 시점 상태

- `main` HEAD = **`da9482f`** (푸시·GCP 배포 완료, 실서버는 안전)
- **미커밋 변경 12파일 + 신규 테스트 3개** — 두 팀의 작업물이 디스크에 남아 있음
- `harness-stamp check` **exit 0** (정본 DB 무변형)
- 두 팀 모두 **역주입 직전**에 중단 → **코드가 일부러 깨진 상태는 아님**

### 미커밋 파일
```
 M db/class.js                      ← CL팀
 M db/self-learn-extended.js        ← SL팀
 M lib/auth/can-view-user.js        ← CL팀
 M public/admin/daily-learning.html ← SL팀
 M public/class/class-home.html     ← CL팀(코드 마스킹 후 빈 칸 정리 중)
 M public/class/index.html          ← CL팀(동상)
 M public/class/manage.html         ← CL팀
 M public/js/common-nav.js          ← CL팀
 M public/self-learn/learning-map.html ← SL팀
 M routes/class.js                  ← CL팀
 M routes/self-learn.js             ← SL팀
 M test/e2e/smoke.spec.js
?? test/class-invite-code-guard.test.js   ← CL팀 신규
?? test/self-learn-daily-guard.test.js    ← SL팀 신규
?? test/self-learn-grading.test.js        ← SL팀 신규
?? _p07_*.tmp.js, _p07_shots/             ← CL팀 임시(삭제해도 무방)
```

## 재개 순서

1. **먼저 현 상태 확인** — `git status`, `npm test`, `npm run test:e2e:smoke`(단독 실행). 붉으면 어디까지 됐는지부터 파악.
2. **두 팀 재개** — 각 에이전트에 SendMessage 로 이어붙이거나, 아래 남은 일을 새로 지시.
3. 감리 통합 검수 → 커밋 → 배포 → 실서버 재확인.

### SL팀 (스스로채움) — 남은 일
- 마지막 로그: *"Now let me prove the tests fail on the pre-fix code (reverse injection)"* → **수정은 얹혀 있고 역주입 증명이 남음**
- 담당: **P0-5 채점 1칸 밀림** · **P0-6 오늘의 학습 세트 권한** · P1 4건(미배포 노출·수정 500·삭제 404·진단 결과 미반영)
- 파일: `routes/self-learn.js`·`db/self-learn-extended.js`·`learning-map.html`·`admin/daily-learning.html`
- 포트 3241. **격리에 소스 트리 전체 복사 필요**(`routes/self-learn.js` 의 `__dirname` 하드코딩 3곳 때문)

### CL팀 (클래스) — 남은 일
- 마지막 로그: *"코드 마스킹으로 빈 칸이 남는 두 화면(클래스 홈 헤더·나의 클래스 카드) 정리"* → **가드는 얹혔고 UI 마감이 남음**
- 담당: **P0-7 초대코드 유출**(API·화면·GNB 3층) · P1 강퇴 후 재가입 불가
- 파일: `routes/class.js`·`db/class.js`·`manage.html`·`class-home.html`·`class/index.html`·`common-nav.js`·`lib/auth/can-view-user.js`
- 포트 3242
- ⚠ 코드 마스킹 때문에 **다른 화면에 빈 칸이 생기는지** 전수 확인 필요(과잉 차단 방지)

## 오늘 완료분 (참고)

배포 7건 — `2fe11c0` 하네스 복구 · `339c50d` 재검증 장치 · `4d6e478` 성장기록 권한 17곳 · `311ca86` 성취 모집단 정합(재집계) · `1dee924` 성취 화면 임계 일관화 · `da9482f` **기능점검 P0 4건 차단**

기능 전수 점검 **34/34 시나리오 완료** — P0 7 · P1 41 · P2 35
- 결과: `보고서/기능점검_웨이브1_결과_통합_v1.md` · `보고서/기능점검_웨이브2_결과_통합_v1.md`
- **P0 4건 배포 완료 / 3건이 이번 중단분**

## 남은 승인 대기 (사용자 판단)

1. **정본 `homework_submissions#31` 복구** — 5-21 제출분이 강등된 채 소실. `UPDATE ... SET is_draft=0 WHERE id=31` 한 줄이나 정본 write 라 `harness-stamp mark` + 전건 재실행 필요
2. **알림 레거시 링크 100건 DB 정정** — 읽기 시점 정규화로 이미 해소돼 선택 사항. 스크립트는 `scripts/fix-notification-links.js`(격리 검증 완료)
3. **GCP 업로드 파일 유실** — `public/uploads` 5개뿐, `student_gallery` 참조 17건 중 14건이 디스크에 없어 학생 갤러리 이미지 23개가 404. 원인 규명 + 동기화 또는 DB 정리 필요
