# 기능 전수 점검 웨이브 1 — 통합 결과 (v1)

2026-08-05 · 대상 HEAD `1dee924` · 3팀 병렬(격리 DB 사본 + 전용 포트)
증적: `보고서/증적/기능점검_W1_T1_20260805/`(48장) · `_W1_T2_`(39장) · `_W1_T3_`(13장)
정본 DB 무변형 확인 — 3팀 전부 `harness-stamp check` exit 0

---

## P0 — 즉시 수정 (4건)

### P0-1. 자동 임시저장이 채점 완료된 제출을 통째로 은닉 `W1-T1-1`
학생이 과제 페이지를 **열어두고 30초** 지나면 자동 임시저장이 발동해 `is_draft=1`로 뒤집힌다.
- 교사 화면: "아직 제출한 학생이 없어요 · 제출 0 · 채점완료 0"
- 학생 화면: "제출하기" 미제출 폼으로 원복
- DB: 행은 살아 있음(`score=90, status='graded'`)이나 교사 측 전 쿼리가 `COALESCE(is_draft,0)=0`으로 거름
- 원인: `homework-view.html:2964` Quill `text-change`가 **source 미구분** → 저장된 답안 로드가 변경으로 오인 → 30초 뒤 `saveDraft` → `db/homework.js:238`이 `is_draft=1` 무조건 덮어씀
- 복구 수단: 학생 재제출뿐(그러면 `resubmitted`로 바뀌고 채점이 풀림)

### P0-2. 콘텐츠 상세 API 무검사 + 업로드 파일 무인증 서빙 `W1-T3-1`
- `routes/content.js:515` — `is_public`·`status`·`creator_id` 검사 **전무**. 학생이 ID만 알면 남의 draft/pending/rejected 열람
- 검수 전 문항의 **정답·해설까지** 노출(`answer`, `explanation`)
- `server.js:113` — `public/` 전체 static → `/uploads/**` 무인증. 상세로 얻은 `file_path`를 **비로그인 다운로드** 가능
- 삭제된 콘텐츠의 첨부도 디스크에 남아 계속 받아짐(`W1-T3-11`)
- **PM 실서버 실측 확인**: student1 로그인 → id 3797(draft)·3801(approved+비공개)·3129(rejected) **전부 200**
- 파급: 과제·갤러리·알림장 첨부가 모두 같은 `/uploads` 밑 → 각 API가 첨부 URL을 응답에 싣는 순간 사실상 공개

### P0-3. 미승인 갤러리 작품에 댓글·좋아요 주입 가능 `W1-T2-01`
읽기는 전부 차단(정상)인데 **쓰기 경로에 상태 검사 없음**.
- `POST /gallery/{pending}/comments` → 201, `/like` → 200 (`routes/growth.js:547`, `:493`)
- 승인 후 그 댓글·좋아요가 그대로 공개. 좋아요 사전 누적으로 인기순 조작 가능

### P0-4. 알림 기능 전체 불능 `W1-T2-02`
- 알림 링크 404: `/class/class-notice.html`·`class-evaluation.html`·`class-homework.html` — 실제 파일명은 `notice-board.html`·`homework-view.html` 등
- **PM 실서버 실측**: 갤러리 링크만 200, 과제 알림 **404**
- 알림 벨 UI가 `public/plus/gallery.html` **단 한 파일**에만 존재. 공통 GNB에 없음 → 과제·평가·알림장 알림은 갤러리 페이지에 들어가야 확인
- DB에는 191건 누적(student1 미읽음 24건)

---

## PM 특별 지시 답 — 과제 채점 → 성취 반영

**정본 채점 17건 중 귀속 0건 (0%).** 그러나 **파이프는 살아 있다.**

| 구분 | 건수 |
|---|---|
| 채점 완료 제출 | 17 |
| 성취 귀속 | **0** |
| 원인 A — 과제에 성취기준 미부여 | 11 |
| 원인 B — 성취기준은 있으나 `homework_submit` 로그 자체가 없음(시드 제출) | 6 |
| 대조 실험(성취기준 부여 + 정상 제출 + 채점) | **1/1 귀속 성공** |

- 성공 경로 확인: `applyGradeToSubmitLog` → `learning_logs.achievement_code` 갱신 + `lrs_achievement_stats` 시도 생성
- 성취기준 없는 과제를 채점하면 `achievement_code=null` 유지 — **거짓 귀속을 안 만드는 것은 올바른 설계**
- 결론: **코드 결함이 아니라 입력 결함.** 과제 등록 모달의 성취기준이 선택 항목·기본 미선택이라, 교사가 그냥 저장하면 그 과제는 영구히 성취에 기여하지 못한다
- 부수: 정본의 "성취코드 붙은 제출 로그 6건"은 `target_id`가 `homework`에 없는 id를 가리키는 **합성 주입 행**이라 실적이 아님

→ 수정 방향: ① 과제 등록 시 성취기준 **필수화 또는 차시에서 자동 상속** ② 미부여 과제에 대한 사후 매핑 동선 ③ 시드 제출물 backfill 여부 결정

---

## P1 (21건)

### 분모·집계 정합
| ID | 내용 |
|---|---|
| `W1-T1-3` | 수업 "이수 인원"이 라벨과 다른 산식 — 주석은 "모든 콘텐츠 완료"인데 SQL은 **1개만 완료해도 이수**. 같은 교사 화면 두 곳이 정면 모순 |
| `W1-T1-4` | 이수율 분모 3종 — 홈 도넛 10명(**학부모·교직원 포함**) / 수업 탭 34%(부풀림) / 정답 8명 25%. `routes/lesson.js:75`에 `u.role='student'` 누락 |
| `W1-T1-5` | 과제 평균 제출률 분모가 전체 멤버 11명 → 13%(정답 18%). 같은 응답의 `member_count=8`을 안 씀. 카드별 제출률은 순수 학생 기준이라 **상단↔카드 상시 불일치** |
| `W1-T1-9` | 탈퇴 계정(`status='deleted'`)이 분모·명단에 포함, 교사 화면에 실명 노출 |
| `W1-T3-3` | 승인관리 총계가 **응답 배열 길이**(limit 50) — 화면 "총 50건·반려 1" vs 실제 **10,064건·반려 139**. 오래된 대기건이 밀리면 **"대기 0"으로 보여 승인 워크플로우가 조용히 정지** |
| `W1-T2-12` | 알림장 읽음 인원 3중 불일치(read_count 2 / read_members 1 / 화면 1), 미확인 명단에 학부모·교직원 포함 |
| `W1-T2-08` | 콘테스트 "N명 응모"가 실제로는 응모 **건수** (1명이 2회 → "2명") |

### 권한·정책
| ID | 내용 |
|---|---|
| `W1-T2-03` | 무관한 교사(타 학교·비멤버)가 임의 학생 작품 **승인·반려 200**. 승인=전국 공개인데 게이트 무력 — **정책 확정 필요** |
| `W1-T2-06` | 익명 게시글의 `author_id`가 API 응답에 노출. 화면은 "익명"이나 네트워크 탭으로 작성자 특정 |
| `W1-T2-05` | 설문 개설 교사가 자기 설문에 응답 가능 → 분자에 교사 포함, 이론상 분자>분모 |
| `W1-T2-07` | 콘테스트 **시작 전** 응모를 서버가 허용(201). `db/growth.js:1058`이 `end_date`만 검사 |
| `W1-T3-2` | 전역 검색(상단 돋보기)이 `is_public` 미검사 → `approved & 비공개` **13건** 노출 |

### 사용자 동선 단절
| ID | 내용 |
|---|---|
| `W1-T1-6` | 교사 제출 현황에 **미제출자가 없음** — 누가 안 냈는지 알 수 없다. 상세 드로어엔 미제출 분기가 이미 있어 명단 병합이 원 설계로 보임 |
| `W1-T3-4` · `W1-T2-04` | **반려 사유가 작성자에게 안 보임** — 콘텐츠·갤러리 양쪽 동일. API는 주는데 화면이 안 그림 |
| `W1-T3-5` | 반려 후 재승인 요청 동선 없음 → 수정해도 `rejected` 유지 = **영구 반려**. 공개↔비공개 2단 토글이 유일 우회 |
| `W1-T2-09` | 그룹 쪽지 읽음이 **전원 공유** — A만 읽어도 B의 미읽음 0. 메시지당 단일 플래그 |

### 데이터 정합
| ID | 내용 |
|---|---|
| `W1-T1-7` | 성취기준을 골라도 `subject_code`·`grade_group`이 **항상 null** — 존재하지 않는 프로퍼티(`subject_name`)를 읽음. 교과별 성취 집계에서 누락 |
| `W1-T1-8` | 포트폴리오 **이중 등록** — 자동(`source_type='class'`, score `"0.85"`)과 수동(`'homework'`)이 서로를 못 봄. **재채점마다 계속 증가**. 활동명이 영문 `"Activity"`로 뜨는 행 다수 |
| `W1-T3-6` | `content_attempts` 계열 4개 엔드포인트가 `data/dacheum.db` **하드코딩** — `DB_PATH` 무시. 운영에서 경로를 쓰면 풀이 기록이 딴 DB로. 동일 패턴이 `routes/self-learn.js` 6곳 |
| `W1-T3-7` | 담은 자료가 폴더 체계에서 실종 — `folder_id`가 **콘텐츠 전역 컬럼**이라 담으면 원작자 folder_id를 달고 옴 |
| `W1-T2-10` | **전 서비스 시각이 9시간 어긋남** — UTC 저장값을 로컬처럼 표시. 12:56 작성 글이 "9시간 전 · 03:56" |

---

## P2 (15건, 요약)

`W1-T1-10` 교사 제출 목록에 HTML 태그 노출·첨부 미표시 · `W1-T1-11` 첫 제출이 '재제출'로 기록 · `W1-T1-12` `exams.show_answers` 死컬럼 · `W1-T1-13` 시드 로그가 없는 수업/과제 id 참조 → LRS 드릴다운에 남의 학생 혼입 · `W1-T2-13` 게시판 댓글 수정 API 부재(알림장엔 있음) · `W1-T2-14` 설문 `questions` 이중 JSON 인코딩 · `W1-T2-15` 갤러리 플레이스홀더 이미지 5종 404 · `W1-T2-16` 글 작품 상세 모달의 거대 빈 캔버스 · `W1-T2-17` 쪽지·댓글 알림 미생성 · `W1-T3-8` PUT에 boolean 보내면 500(POST는 정상) · `W1-T3-9` 권한 없는 조작이 `success:true` 응답 · `W1-T3-10` 업로드 검증이 확장자 문자열뿐(PE 헤더 통과) · `W1-T3-11` 삭제 후 첨부 파일 잔존 · `W1-T3-12` 공개 문항 상세가 풀기 전 정답 노출 · `W1-T3-13` `contents.status` 기본값이 `approved`

---

## 반복 패턴 (수정 시 함께 볼 것)

1. **"목록에서 가리기"는 되는데 "직접 접근·쓰기 막기"가 없다** — P0-2, P0-3, 콘테스트 시작 전 응모, 설문 자가응답, 전역 검색. **화면만 막고 서버는 열려 있다.**
2. **분모가 화면마다 다르다** — 학부모·교직원·탈퇴 계정 혼입, 응답 배열 길이를 총계로 사용.
3. **반려/실패 사유가 당사자에게 안 간다** — 콘텐츠·갤러리 공통.
4. **SSOT 옆에 판정 사본** — `DB_PATH` 무시, `member_count` 있는데 안 씀, 자동/수동 포트폴리오가 서로를 모름.

---

## 환경 이슈 (점검 중 드러남)

- **`server.js`가 기동 시 `data/sessions.db`를 무조건 삭제** — 팀별 포트 분리로도 못 막는 간섭. 다른 팀·PM 세션이 끊긴다.
- **`DB_PATH` 격리가 완전하지 않다** — `W1-T3-6` 때문. 팀3은 cwd까지 옮겨 우회했다.
- **정본 DB에 테스트 잔여 데이터가 사용자 화면에 노출 중** — 콘테스트 3건("수정된 콘테스트 제목" 등), 갤러리 작품 22건. **점검 전부터 존재**하던 것.

---

## 미확인 (추측 배제)

평가 기간 게이트 · `exam_autosaves` · CBT 이탈 감지 실제 이벤트 · 알림장 첨부 업로드 · 콘테스트 수상작 화면 · 신고 takedown 처리 후 반영 · Socket.IO 실시간 브로드캐스트 · 설문 choice/scale/multi 유형 · `content_progress`/`user_learning_list` 사용처 · `portfolios` 테이블 용도 · `aggregate-stats` 무인증(코드 근거만)
