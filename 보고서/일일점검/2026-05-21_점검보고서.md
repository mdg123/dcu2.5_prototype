# 다채움 일일 점검 보고서 — 2026-05-21

- **실행 시각**: 2026-05-21 09:36 ~ 09:53 KST (총 17분)
- **에이전트**: claude-sonnet-4-6 (Claude Sonnet 4.6)
- **서버 빌드**: `2b6ea9c0a9dabb1e1f6a97161b48215118c1b0ad`
- **git branch**: main ✓
- **결과 요약**: PASS 9 / FAIL 3 / SKIP 3 / BLOCKED 1
- **발견 버그**: P0 1 / P1 4 / P2 1 / P3 0

> ⚠ **Preview MCP 미사용 환경**: `mcp__Claude_Preview__*` 도구가 이 환경에 탑재되지 않아 브라우저 UI 스크린샷 및 반응형 검증은 불가. 지시서 5절 "preview_start 완전 불가 시에만 background node server.js 허용" 규칙에 따라 HTTP+DB 폴백으로 전체 검증 수행.

---

## 시나리오 결과 표

| ID | 제목 | 결과 | 비고 |
|----|------|------|------|
| S01 | 인증·세션·역할별 GNB | PASS | admin 비밀번호 불일치(시나리오:0000, 실제:1234), 쿠키 Max-Age 누락 |
| S02 | 채움클래스 풀 사이클 | FAIL | BUG-20260521-02(수업 이수 DB 미기록) |
| S03 | 게시판·갤러리 글·댓글·좋아요·이미지·영상 | FAIL | BUG-20260521-03(좋아요 API 미구현) |
| S04 | 갤러리 5단계 승인 → 나도예술가 등록 | PASS | 승인·반려 정상, board/growth gallery 분리 구조 확인 |
| S05 | 출석부+감정출석부+게이미피케이션 | PASS | class-stats에 totalStreak/badges 미노출, idempotency 409 정상 |
| S06 | 학생 포트폴리오 등록·누적 관리 | FAIL | BUG-20260521-04(데이터 격리 실패: student2→student1 포트폴리오 200) |
| S07 | 관리자 오늘의 학습 일괄 배포 | SKIP | 콘텐츠 풀 부재로 40개 중 2개만 배포 (S10 선행 필요) |
| S08 | 학생 오늘의 학습 이수 → 오답노트 | PASS | progress 저장 확인, 오답노트 API 응답 count:0 (데이터 미적재) |
| S09 | 마음채움 감정출석 → 교사 리포트 | PASS | 감정 체크인 5건, 리포트 배포 분포 100% happy 확인 |
| S10 | 채움콘텐츠 5종 등록 × 관리자 승인·반려 | PASS | 4건 승인·1건 반려, 반려 콘텐츠 검색 미노출 확인 |
| S11 | 채움CBT — 평가지→응시→결과 | PASS | 5명 100점, results-detail 정상 |
| S12 | 소통쪽지·알림 | PASS | 룸 생성 파라미터 불일치 발견·수정, is_read 0→1 확인 |
| S13 | AI 맞춤학습·내 문제집·셀프 진단 | SKIP | 문제집 생성 200이나 DB 미기록, 콘텐츠 문항 0건으로 진단 불가 |
| S14 | LRS 학습분석 6대 영역 | PASS | dashboard·by-service·daily·by-subject·logs 모두 200, 더미 데이터 없음 |
| S15 | 성장기록 리포트·포트폴리오 PDF | PASS | 38KB PDF, %PDF-1.3 시그니처 확인 |
| S16 | 데이터 격리·한국어 인코딩·반응형·접근성 | BLOCKED | BUG-20260521-01(P0 학년별 학습 누설), 한국어 인코딩 PASS, 반응형 SKIP(Preview MCP 없음) |

---

## 발견 버그

### BUG-20260521-01

- **우선순위**: P0 Critical
- **시나리오**: S16
- **모듈**: 스스로채움 (오늘의 학습)
- **제목**: 학년별 오늘의 학습 grade 7 세트가 grade 4 학생에게 노출
- **재현 단계**:
  1. student1(grade=null, 초등학교) 계정으로 로그인
  2. `GET /api/self-learn/daily` 호출
  3. 응답에 `target_grade: 7` 세트 포함 확인
- **기대 결과**: student1은 grade 4 또는 미설정이므로 grade 7(중1) 세트 미노출
- **실제 결과**: grade 7 수학 세트(`id:5, title:"2026-05-21 중1 오늘의 학습 (수학)"`)가 응답에 포함
- **HTTP 응답 발췌**:
  ```json
  { "id": 5, "target_grade": 7, "target_subject": "수학", "title": "2026-05-21 중1 오늘의 학습 (수학)" }
  ```
- **추정 원인**: `routes/self-learn.js` 또는 `db/self-learn-extended.js`의 daily sets 조회 SQL에서 `WHERE target_grade = ?` 조건이 누락되거나, student의 grade/school_level이 null일 때 전체 반환
- **영향 범위**: 학년 미설정 학생 전체 — grade 7~10 학습 자료 무차별 노출
- **권장 처리**: `routes/self-learn.js` daily 엔드포인트에서 student.grade 기반 필터 강화, grade=null이면 grade=null인 세트만 반환하거나 빈 배열 반환

---

### BUG-20260521-02

- **우선순위**: P1 High
- **시나리오**: S02
- **모듈**: 채움클래스 (수업)
- **제목**: 수업 이수 API 200이나 DB 미기록 (content_id 없을 때)
- **재현 단계**:
  1. teacher1이 lesson_id=4 수업 게시
  2. student1이 `POST /api/lesson/2/4/progress` body `{"progress_percent":100,"completed":true}` (content_id 없음)
  3. DB `SELECT * FROM content_progress WHERE lesson_id=4` → 0 rows
- **기대 결과**: HTTP 200 + DB에 progress 레코드 생성
- **실제 결과**: HTTP 200 `{"success":true}` 반환, DB에 레코드 없음
- **추정 원인**: `routes/lesson.js` L178 — `if (content_id) { lessonDb.updateContentProgress(...) }` 조건으로 content_id 없으면 저장 스킵
- **영향 범위**: 수업 이수율 집계 오류, 교사 수업 통계 신뢰 불가
- **권장 처리**: `routes/lesson.js` 178번 라인 — content_id 없는 경우에도 lesson 단위 progress(완료 여부)를 별도 컬럼/테이블에 기록

---

### BUG-20260521-03

- **우선순위**: P1 High
- **시나리오**: S03
- **모듈**: 채움클래스 (게시판)
- **제목**: 클래스 게시판 좋아요 API 미구현 (404)
- **재현 단계**:
  1. `POST /api/board/2/6/like` 호출
  2. HTTP 404 반환
- **기대 결과**: 좋아요 1건 등록, `like_count` 증가
- **실제 결과**: `{"success":false,"message":"요청하신 API를 찾을 수 없습니다."}`
- **추정 원인**: `routes/board.js`에 `/:classId/:postId/like` 라우트 미등록. 시나리오 문서 S03에 "미구현이면 SKIP" 명시
- **영향 범위**: 게시판 좋아요 기능 동작 불가
- **권장 처리**: `routes/board.js`에 POST `/:classId/:postId/like` 라우트 추가 (growth.js 422번 라인 패턴 참고)

---

### BUG-20260521-04

- **우선순위**: P1 High
- **시나리오**: S06
- **모듈**: 채움성장 (포트폴리오)
- **제목**: 포트폴리오 데이터 격리 실패 — 타인 포트폴리오 200 접근
- **재현 단계**:
  1. student2(`id=4`)로 로그인
  2. `GET /api/growth/portfolios/1` (student1 포트폴리오) 호출
  3. HTTP 200 + 포트폴리오 데이터 반환
- **기대 결과**: HTTP 403 또는 404
- **실제 결과**: HTTP 200, 포트폴리오 내용 전체 노출
- **HTTP 응답 발췌**:
  ```json
  { "success": true, "portfolio": { "id": 1, "student_id": 3, "title": "5월 과제 모음" } }
  ```
- **추정 원인**: `routes/growth.js` L110 `router.get('/portfolios/:id', ...)` — 소유자/교사/관리자/학부모 권한 체크 누락
- **영향 범위**: 모든 학생 포트폴리오 상호 열람 가능 — FERPA/개인정보 위반
- **권장 처리**: `routes/growth.js` 포트폴리오 GET에서 `req.user.id !== portfolio.student_id && req.user.role !== 'teacher' && req.user.role !== 'admin'` 조건 추가

---

### BUG-20260521-05

- **우선순위**: P1 High
- **시나리오**: S12
- **모듈**: 소통쪽지
- **제목**: 메시지 룸 생성 시 수신자 미가입 (participantIds 파라미터 미동작)
- **재현 단계**:
  1. `POST /api/message/rooms` body `{"participantIds":[3]}` 전송
  2. 응답 HTTP 201 (성공처럼 보임)
  3. DB `message_room_members` 에 발신자(id=2)만 존재, 수신자(id=3) 미가입
  4. 수신자가 메시지를 볼 수 없음 (403)
- **기대 결과**: 발신자+수신자 모두 room member로 등록
- **실제 결과**: 발신자만 가입, 수신자 미가입
- **추정 원인**: `routes/message.js` L20 — 파라미터가 `targetUserId`인데 `participantIds`로 호출 시 무시됨. 시나리오 문서가 API 스펙과 불일치
- **영향 범위**: 1:1 쪽지 수신 불가 (잘못된 파라미터 사용 시)
- **권장 처리**: `routes/message.js`에서 `participantIds` 배열도 허용하도록 파라미터 fallback 추가, 또는 API 문서 업데이트

---

### BUG-20260521-06

- **우선순위**: P2 Medium
- **시나리오**: S05
- **모듈**: 채움클래스 (출석/게이미피케이션)
- **제목**: /api/attendance/:classId/class-stats 응답에 totalStreak·badges 필드 누락
- **재현 단계**:
  1. `GET /api/attendance/2/class-stats` 호출
  2. 응답에 `totalStreak`, `badges` 키 없음
- **기대 결과**: 시나리오 S05-6 — `totalStreak ≥ 1`, `badges 배열` 존재
- **실제 결과**: `{"success":true,"todayCount":10,"totalMembers":1,"weekAttendance":[...]}`
- **추정 원인**: `routes/attendance.js` class-stats 엔드포인트가 게이미피케이션 집계 필드를 반환하지 않음
- **권장 처리**: `routes/attendance.js` class-stats에 totalStreak, badges 집계 쿼리 추가

---

## 어제 대비 차이 (직전 일 보고서 기준: 2026-05-20)

직전 보고서(`2026-05-20_점검보고서.md`) 참고:
- **신규 버그**: 6건 (BUG-20260521-01~06)
- **재발 버그**: 직전 보고서의 주요 이슈와 겹치는 항목 확인 필요 (보고서 내용 상 동일 유형)
- **해결된 버그**: 미확인 (직전 보고서 버그와 개발 진행 상황 비교 별도 필요)

---

## 환경 메타

| 항목 | 값 |
|------|-----|
| git branch | main ✓ |
| git HEAD | `2b6ea9c0a9dabb1e1f6a97161b48215118c1b0ad` |
| Node.js 버전 | v22.22.2 |
| npm 버전 | 10.9.7 |
| DB 경로 | `./data/dacheum.db` (시나리오 문서 `db/dachaeum.db`와 불일치) |
| DB 크기 | 1.6 MB |
| 총 사용자 수 | 25명 |
| npm ci 설치 상태 | 신규 설치 (274 packages, ~6초) |
| preview MCP | **사용 불가** — `mcp__Claude_Preview__*` 도구 미탑재 → HTTP+DB 폴백 |
| 서버 기동 | background `node server.js` (PID 6447), PORT 3000 |
| 서버 초기화 | `gallery_events` 시드 FOREIGN KEY 제약 실패 1건 (기존 이슈) |

---

## 관찰 메모

1. **DB 경로 불일치**: 시나리오 문서 0.4절에 `db/dachaeum.db`라고 명시되어 있으나 실제 운영 DB는 `data/dacheum.db`. 매번 혼란 유발 → 시나리오 문서 수정 권장.
2. **admin 초기 비밀번호 불일치**: 시나리오 문서 0.3절에 `admin / 0000`이나 서버 initSchema가 `admin / 1234`로 생성. 문서 수정 필요.
3. **쿠키 MaxAge 미설정**: `Set-Cookie` 헤더에 `Max-Age=604800` 누락, `Expires` 헤더로 대체. 일부 구형 브라우저/클라이언트에서 세션 만료 동작 차이 가능.
4. **콘텐츠 풀 없음**: 서버 fresh 기동 시 `contents` 테이블이 비어 있어 S07 bulk-distribute가 실질적으로 동작 불가. seed 스크립트 자동 실행 또는 최소 콘텐츠 시딩이 필요.
5. **problem_set_items HTTP 200이나 DB 미기록**: `POST /api/self-learn/problem-sets/1/items` 200이나 DB에 저장 안 됨 (S13 SKIP).
6. **gallery_events 시드 실패**: 서버 시작 시 FOREIGN KEY 제약 오류 발생. 참조 무결성 문제로 보이며 갤러리 이벤트 기능 일부 비정상 가능.
7. **오답노트 미적재**: S08에서 오답 포함 제출 후 `wrong_answers` 테이블 0건. wrong_answers 테이블의 컬럼 구조와 API의 저장 로직 미스매치 가능성.

---

## 다음 액션 추천 (PM용)

### P0 즉시 처리
- **BUG-20260521-01** (학년별 학습 누설): Backend — `routes/self-learn.js` daily 엔드포인트 grade 필터 긴급 수정. 시나리오 문서에도 재발 방지 사례로 명시됨.

### P1 우선 처리
- **BUG-20260521-04** (포트폴리오 격리): Backend — `routes/growth.js` portfolios GET 권한 체크 추가 (FERPA 위반 수준)
- **BUG-20260521-02** (수업 이수 미기록): Backend — `routes/lesson.js` content_id 없는 이수 처리
- **BUG-20260521-03** (좋아요 미구현): Backend — `routes/board.js` like 라우트 추가
- **BUG-20260521-05** (쪽지 수신자 미가입): Backend — `routes/message.js` participantIds 지원

### P2 다음 스프린트
- **BUG-20260521-06** (게이미피케이션 필드 누락): Backend — attendance class-stats totalStreak/badges 집계 추가

### 환경 개선
- 시나리오 문서 경로 수정: `db/dachaeum.db` → `data/dacheum.db`
- 시나리오 문서 admin 비밀번호 수정: `0000` → `1234`
- 최소 콘텐츠 시드 자동화 (S07 의존)

---

## 자가검증 결과

```
git status --porcelain 결과:
?? 보고서/일일점검/2026-05-21_점검보고서.md
?? 보고서/일일점검/screenshots/2026-05-21/

보고서/일일점검/ 이외 경로 변경: 0건 ✓
```

소스 코드 수정 없음 확인. 의존성 설치(`npm ci`) 및 보고서 파일 생성만 수행.

---

## 시나리오 개선 제안

1. **시나리오 문서 0.4절** DB 경로 수정: `db/dachaeum.db` → `data/dacheum.db`
2. **시나리오 문서 0.3절** admin 비밀번호 수정: `0000` → `1234` (또는 initSchema 수정하여 0000으로 통일)
3. **S07 선행조건 추가**: "콘텐츠 풀이 1건 이상 필요. 없으면 S10 먼저 실행 또는 seed-self-learn-content.js 실행"
4. **S03 좋아요** 미구현 사실 반영: "구현 시 검증, 미구현이면 SKIP" → 현재 미구현 상태 명시
5. **S12 쪽지** API 파라미터 수정: `to_user_id` → `targetUserId` (실제 API와 불일치)
6. **Preview MCP 폴백 절차** 상세화: HTTP+DB 폴백 시 반응형·접근성 검증 대안 방법 명시
