# 다채움 일일 점검 보고서 — 2026-06-03

- **실행 시각**: 2026-06-03 09:35 ~ 09:49 KST (총 약 14분)
- **에이전트**: claude-sonnet-4-6
- **서버 빌드**: `c51a62f6f4f6e8b3f5d6c8ab4b155d5eb1d7d7a1`
- **브랜치**: main
- **결과 요약**: PASS 10 / PARTIAL 4 / FAIL 1 / BLOCKED 1
- **발견 버그**: P0 0건 / P1 4건 / P2 6건 / P3 1건

---

## 1. 시나리오 결과 표

| ID | 제목 | 결과 | 소요(분) | 비고 |
|----|------|------|---------|------|
| S01 | 인증·세션·역할별 GNB | PASS | 3 | admin 비밀번호 문서 불일치 P2 |
| S02 | 채움클래스 풀 사이클 | PARTIAL | 8 | BUG-02(P1), BUG-03(P1), BUG-04(P2) |
| S03 | 일반게시판·갤러리 글·댓글·좋아요 | PASS | 3 | 갤러리 pending 정상 |
| S04 | 갤러리 승인 → 나도예술가 | PASS | 2 | 단계 승인·반려 정상 |
| S05 | 출석부 + 감정출석부 + 게이미피케이션 | PASS | 3 | 중복체크인 비멱등 P2 |
| S06 | 학생 포트폴리오 등록·누적 관리 | PASS | 2 | 데이터 격리 정상 |
| S07 | 관리자 오늘의 학습 일괄 배포 | FAIL | 2 | BUG-10(P2): content pool 부족 |
| S08 | 학생 오늘의 학습 이수 → 오답노트 | BLOCKED | 1 | S07 FAIL 의존 |
| S09 | 마음채움 감정출석 → 교사 리포트 | PARTIAL | 2 | BUG-07(P1) emotion-checkin 별도 API 없음 |
| S10 | 채움콘텐츠 5종 × 관리자 승인·반려 | PASS | 3 | API 경로 /api/contents 확인 |
| S11 | 채움CBT 평가지 → 응시 → 결과 | PARTIAL | 2 | BUG-02(P1), BUG-05(P1) |
| S12 | 소통쪽지·알림 | PASS | 2 | 3종 알림 확인 |
| S13 | AI 맞춤학습·내 문제집·셀프 진단 | PARTIAL | 2 | 진단 노드 없음(콘텐츠 미연결) |
| S14 | LRS 학습분석 6대 영역 | PASS | 2 | 41건 실데이터 확인 |
| S15 | 성장기록 리포트·포트폴리오 PDF | PASS | 2 | PDF 39KB, %PDF- 시그니처 확인 |
| S16 | 데이터 격리·한국어·반응형·접근성 | PASS | 2 | 한글 이모지 정상, 클래스 격리 정상 |

---

## 2. 시나리오별 상세 로그

### S01 — 인증·세션·역할별 GNB (PASS)

**실측 내용**:
1. `POST /api/auth/signup` → HTTP 201, Set-Cookie 발급 확인
2. 직후 `GET /api/auth/me` → 200, user.id 존재
3. `POST /api/auth/logout` → 200 → `/api/auth/me` → **HTTP 401** ✅
4. admin/teacher1/student1/parent1/staff1 순차 로그인 → 각 role 정상 반환
5. 5자리 비밀번호 → HTTP 400 + `"비밀번호는 숫자 4자리입니다."` ✅
6. admin → `/admin/` : HTTP 200 ✅
7. parent1 → `/admin/` : HTTP 403 ✅, staff1 → `/admin/` : HTTP 403 ✅
8. DB `qa_temp_` 계정 1건 확인 ✅
9. Cookie: `Expires=Wed, 10 Jun 2026` (7일) — Max-Age 필드 없이 Expires만 사용 (P3)

**관찰**: admin 비밀번호가 실제 `1234`이나 시나리오 문서는 `0000` 명시. 문서 갱신 필요.

---

### S02 — 채움클래스 풀 사이클 (PARTIAL)

**실측 내용**:
1. `POST /api/class` → `CLASS_ID=2, CODE=FMR5LN` (6자리 영문숫자 ✅)
2. student1~10 가입 성공, parent1·staff1도 가입 성공 (총 13명 ≥ 11 ✅)
3. 수업 게시: `status:"active"` → CHECK constraint FAIL → `status:"published"`로 수정 후 성공. `LESSON_ID=4` 생성 완료. 학생 3명 progress 100% ✅
4. 과제: `HW_ID=3`, 학생 10명 제출 ✅, 5명 채점(70~95점) ✅
5. 시험: `EXAM_UUID=ebd024dc-8175-41d0-969a-068cf96dc5fe`, 학생 10명 응시 완료. **단, score=0 (P1 버그)** ❌
6. 알림장: `NOTICE_ID=4` 생성 ✅. `/read` endpoint 없어 확인 처리 불가 ❌

**DB 검증**:
- `class_members`: 13 (≥11 PASS)
- `homework_submissions`: 10 (PASS)
- `hw graded`: 5 (PASS)
- `exam_students`: 10, status=submitted (PASS), score=0 (FAIL)
- `notice_reads`: 0 (API 없음, FAIL)

---

### S03 — 게시판·갤러리·댓글·좋아요 (PASS)

**실측 내용**:
1. 일반 게시판 글 작성 `POST_ID=6` ✅
2. student2 댓글 2건 ✅
3. student3 좋아요 1건 (`like_count:1`) ✅
4. 갤러리 글 `GALLERY_POST_ID=7`, `approval_status="pending"` ✅
5. student2 시점 갤러리 조회 → `posts:[], total:0` (미노출) ✅

---

### S04 — 갤러리 승인 → 나도예술가 (PASS)

**실측 내용**:
1. teacher1 클래스 갤러리 승인 → `approval_status:"approved"` ✅
2. `POST /api/growth/gallery/7/approve` (post_id 기준) → "작품을 찾을 수 없습니다" (student_gallery.id=1 기준이어야 함)
3. student1·student2 `GET /api/growth/gallery` → approved 작품 1건 노출 ✅
4. 반려 테스트: `POST_ID=8` → `approval_status:"rejected"`, `rejection_reason:"테스트 반려 사유"` ✅

**DB 검증**:
- `student_gallery.approval_status = 'approved'` 1건 ✅
- `rejected` 1건, rejection_reason 채워짐 ✅

---

### S05 — 출석부 + 감정출석부 + 게이미피케이션 (PASS)

**실측 내용**:
1. student1~10 출석 체크인 10건 (emotion=happy) ✅
2. `GET /api/attendance/2/today` → 10명 현황 ✅
3. `GET /api/attendance/2/ranking` → streak=1 ✅
4. 감정 회고 `content` 필드로 작성 ✅ (시나리오 문서는 `text` 필드 명시 — P3)
5. 교사 감정 리포트 → happy 10건 100% ✅
6. `GET /api/attendance/2/class-stats` → `totalStreak`, `badges` 필드 존재 ✅
7. 중복 체크인 → `"오늘 이미 출석했습니다."` 오류 반환 (업데이트가 아닌 에러 — P2)

---

### S06 — 학생 포트폴리오 (PASS)

**실측 내용**:
1. `GET /api/growth/portfolio/items` → 과제 이력 목록 확인 ✅
2. `POST /api/growth/portfolios` → `PORT_ID=1` 생성 ✅
3. `POST /api/growth/portfolio/items` → 아이템 추가 ✅
4. student2 → portfolio/1 → HTTP 403 (데이터 격리 ✅)

**DB**: portfolios 1건, portfolio_items 16건 ✅

---

### S07 — 관리자 오늘의 학습 배포 (FAIL)

**실측 내용**:
1. `/admin/daily-learning.html` → HTTP 200 ✅
2. `POST /api/admin/daily-learning/bulk-distribute` 호출 성공 → `totalCreated:0, totalSkipped:0` ❌
3. **원인**: `contents` 테이블에 `status='approved'` + `content_type IN ('video','quiz')` 조건을 만족하는 콘텐츠 없음. approved 콘텐츠 2건 존재하나 `school_level=null` + `content_type=exam_paper/lesson_bundle`

**DB**: `daily_learning_items` 0건, 기존 시드 세트 3건(items 없음)

**영향**: S08 BLOCKED

---

### S08 — 오늘의 학습 이수 (BLOCKED)

S07 FAIL로 인해 `daily_learning_items`가 비어 있음. 이수할 콘텐츠 없음.
`GET /api/self-learn/daily` → sets 3건 반환하나 `items:[]`.
`GET /api/self-learn/wrong-notes` → API 정상 동작 확인 (데이터 없음).

---

### S09 — 마음채움 감정출석 (PARTIAL)

**실측 내용**:
1. `/self-learn/emotion-checkin.html` → HTTP 200 (페이지 존재) ✅
2. `POST /api/self-learn/emotion-checkin` → 404 ❌ (별도 API 없음)
3. S05에서 `attendance` 체크인으로 감정 데이터 10건 적재 완료
4. `GET /api/attendance/2/emotion/report` → happy 10건, 교사 리포트 정상 ✅
5. 교사 피드백: `user_id` 필드로 `POST /api/attendance/2/emotion/feedback` → 성공 ✅

**DB**: `attendance.emotion` 10건, `emotion_feedbacks` 1건 ✅
**미구현**: `/self-learn/emotion-checkin` 전용 API, `emotion_checkins` 테이블 미사용

---

### S10 — 채움콘텐츠 5종 × 승인·반려 (PASS)

**실측 내용**:
1. teacher1: video(id=3), image(id=4) 등록 ✅
2. teacher2: document(id=5), quiz(id=6) 등록 ✅
3. teacher3: exam(id=7) 등록 ✅
4. admin: pending 5건 확인 → 3,4,5,6 승인, 7 반려(reason="메타데이터 누락") ✅
5. 반려 콘텐츠(id=7) 공개 검색 미노출 ✅

**DB**: approved 4건, rejected 1건 ✅

---

### S11 — 채움CBT (PARTIAL)

**실측 내용**:
1. S02에서 생성한 시험 존재 (`EXAM_UUID=ebd024dc-8175-41d0-969a-068cf96dc5fe`, 4문항) ✅
2. `POST /api/exam/2/:examId/start-supervision` → 404 ❌ (라우트 미구현)
3. `GET /api/exam/2/:examId/results` → 404 ❌ (라우트 미구현)
4. `GET /api/exam/2/:examId/results-detail` → 404 ❌
5. 학생 10명 응시 완료, status=submitted ✅, **score=0 (P1)** ❌
6. tab_switch_count=0 (preview MCP 없어 브라우저 탭 전환 시뮬레이션 불가)

---

### S12 — 소통쪽지·알림 (PASS)

**실측 내용**:
1. `POST /api/message` → 404 → `POST /api/message/rooms` (targetUserId:3) → ROOM_ID=1 ✅
2. `POST /api/message/rooms/1/messages` → MSG_ID=1, content 발송 ✅
3. student1 조회 → is_read:1 (자동 읽음 처리) ✅
4. DB: `messages.is_read=1` ✅
5. `GET /api/notifications` → 알림 25건, 타입: homework_new, exam_new, notice_new ✅

**미구현**: message_received, homework_graded, gallery_approved 알림 타입

---

### S13 — AI 맞춤학습·내 문제집 (PARTIAL)

**실측 내용**:
1. `/self-learn/learning-map.html` → HTTP 200 ✅
2. `POST /api/self-learn/diagnosis/start` → sessionId=1, testNodes=[] (콘텐츠 노드 연결 없음)
3. `/self-learn/problem-sets.html` → HTTP 200 ✅
4. `POST /api/self-learn/problem-sets` → PROBSET_ID=1 ✅

**DB**: problem_sets=1, diagnosis_sessions=1 ✅
**PARTIAL**: 진단 문항 없음 (curriculum_content_nodes 빈 상태)

---

### S14 — LRS 학습분석 6대 영역 (PASS)

**실측 내용**:
1. `/lrs/index.html` → HTTP 200 ✅
2. `/api/lrs/dashboard` → 41건 활동, 10명 사용자, 6가지 activity_type ✅
3. `/api/lrs/stats/by-service` → class(31건), cbt(10건) 실데이터 ✅
4. 6개 stats API 모두 HTTP 200: daily, by-subject, by-class, user-summary, by-achievement, statements ✅
5. "예시"/"샘플" 가짜 데이터 없음 ✅

---

### S15 — 성장기록 PDF (PASS)

**실측 내용**:
1. teacher1 → `POST /api/growth/portfolios/report` → 403 (본인 아님, 교사 생성 불가)
2. student1 → 필수 파라미터(from, to, schoolLevel) 포함 요청:
   - reportId: `rpt_20260603_01a70976`, sizeBytes: 39988 (39KB > 10KB ✅)
3. `GET /api/growth/portfolios/report/:id/download` → HTTP 200, `Content-Type: application/pdf` ✅
4. PDF 시그니처 `%PDF-` 확인 ✅

---

### S16 — 데이터 격리·한국어·반응형·접근성 (PASS)

**(A) 데이터 격리**:
1. 클래스 B(ID=3) 생성, student6 가입 ✅
2. student1 → 클래스B 게시물(POST_ID=9) 직접 접근 → HTTP 403 ✅
3. grade 4 학생에게 grade 7~10 sets 미포함 ✅

**(B) 한국어 인코딩**:
1. `POST_ID=10`: 제목 `한글·이모지·특수문자🌸テスト` 저장 → DB 재조회 일치 ✅
2. `Content-Type: application/json; charset=utf-8` ✅

**(C) 반응형**:
- /, /class/class-home.html, /self-learn/today.html → 모두 HTTP 200 ✅
- `<meta name="viewport" content="width=device-width, initial-scale=1.0">` 확인 ✅
- ※ preview MCP 없어 실제 렌더링 가로스크롤 검증 불가 (SKIP)

**(D) 접근성**:
- `<html lang="ko">` ✅
- 색상 대비: `#1A1A2E on #FFFFFF = 18.7:1 (WCAG AAA)`, `#6B7280 on #FFFFFF = 4.6:1 (WCAG AA)` ✅
- 키보드 Tab 이동: preview MCP 없어 실제 검증 불가 (SKIP)

---

## 3. 발견 버그

### BUG-20260603-01

- **우선순위**: P2 Medium
- **시나리오**: S01
- **모듈**: 인증
- **제목**: admin 비밀번호 시나리오 문서 불일치
- **재현 단계**:
  1. 시나리오 문서 0.3절의 admin 비밀번호 `0000`으로 POST /api/auth/login 호출
  2. 응답: `{"success":false,"message":"비밀번호가 올바르지 않습니다."}`
  3. `1234`로 시도 시 정상 로그인
- **기대 결과**: 문서대로 `admin/0000` 로그인 성공
- **실제 결과**: `admin/1234`로만 로그인 가능
- **HTTP 응답**:
  ```json
  {"success":false,"message":"비밀번호가 올바르지 않습니다."}
  ```
- **추정 원인**: `db/schema.js` seedAdmin()에서 비밀번호 `1234`로 설정, 시나리오 문서는 `0000` 명시
- **영향 범위**: 외부 QA 팀이 문서를 보고 테스트할 때 admin 로그인 불가
- **권장 처리**: 시나리오 문서 0.3절 admin 비밀번호를 `1234`로 수정, 또는 schema.js에서 `0000` 사용

---

### BUG-20260603-02

- **우선순위**: P1 High
- **시나리오**: S02, S11
- **모듈**: 채움CBT
- **제목**: 시험 자동채점 score=0 (정답 필드 불일치)
- **재현 단계**:
  1. `POST /api/exam/:classId` with `questions[].correct_answer` 필드로 시험 생성
  2. 학생이 정답 제출
  3. `exam_students.score` 확인 → 0
- **기대 결과**: 정답 기준 자동 채점된 점수 반환
- **실제 결과**: score=0 (채점 불가)
- **HTTP 응답**:
  ```json
  {"success":true,"score":0}
  ```
- **추정 원인**: `routes/exam.js` L485에서 `q.answer !== undefined` 확인하나 DB에는 `q.correct_answer`로 저장됨 (필드명 불일치)
- **영향 범위**: 모든 시험 자동 채점 기능 비동작
- **권장 처리**: `routes/exam.js` L485: `q.answer` → `q.answer ?? q.correct_answer` 로 수정

---

### BUG-20260603-03

- **우선순위**: P1 High
- **시나리오**: S02
- **모듈**: 채움클래스
- **제목**: 알림장 읽음 확인 API 미구현
- **재현 단계**:
  1. teacher1이 알림장 생성 후 `NOTICE_ID=4` 확인
  2. student1이 `POST /api/notice/2/4/read` 호출
  3. 응답: 404 "요청하신 API를 찾을 수 없습니다."
- **기대 결과**: 알림장 읽음 처리 성공, `notice_reads` row 생성
- **실제 결과**: 404, `notice_reads` count=0
- **HTTP 응답**:
  ```json
  {"success":false,"message":"요청하신 API를 찾을 수 없습니다."}
  ```
- **추정 원인**: `routes/notice.js`에 `/read` 또는 `/confirm` 라우트 미구현 (reactions, bookmarks만 존재)
- **영향 범위**: 알림장 확인 통계, 학부모 확인 현황 기능 비동작
- **권장 처리**: `routes/notice.js`에 `POST /:classId/:noticeId/read` 라우트 추가, `notice_reads` table에 row 삽입

---

### BUG-20260603-04

- **우선순위**: P2 Medium
- **시나리오**: S02
- **모듈**: 채움클래스
- **제목**: 수업 게시 시 status='active' CHECK constraint 오류
- **재현 단계**:
  1. `POST /api/lesson/:classId` with `{"status":"active"}`
  2. 서버 응답: `{"success":false,"message":"서버 오류가 발생했습니다."}`
  3. 서버 로그: `SqliteError: CHECK constraint failed: status IN ('draft','published','archived','scheduled','pending','rejected')`
- **기대 결과**: status='active' 허용 또는 명세서에 허용값 명시
- **실제 결과**: CHECK constraint fail로 500 에러
- **추정 원인**: DB schema에 'active' 제외되었으나 시나리오 문서 및 일부 클라이언트 코드에서 'active' 사용
- **영향 범위**: 수업 생성 기능 혼란, 클라이언트 코드 'active' 사용 시 전부 에러
- **권장 처리**: `db/lesson.js` CHECK constraint에 'active' 추가하거나 모든 클라이언트를 'published' 사용으로 통일

---

### BUG-20260603-05

- **우선순위**: P1 High
- **시나리오**: S11
- **모듈**: 채움CBT
- **제목**: 시험 결과 조회 및 감독 API 미구현
- **재현 단계**:
  1. `POST /api/exam/:classId/:examId/start-supervision` → 404
  2. `GET /api/exam/:classId/:examId/results` → 404
  3. `GET /api/exam/:classId/:examId/results-detail` → 404
- **기대 결과**: 시험 감독 시작, 학생별 결과(점수·이탈횟수) 조회 가능
- **실제 결과**: 3개 API 모두 404
- **추정 원인**: `routes/exam.js`에 해당 라우트 미구현 (`:classId/:examId/start-supervision`, `/results` 없음)
- **영향 범위**: CBT 감독 기능 및 결과 분석 화면 전체 비동작
- **권장 처리**: `routes/exam.js`에 supervision 시작 + results 조회 라우트 추가

---

### BUG-20260603-06

- **우선순위**: P2 Medium
- **시나리오**: S05
- **모듈**: 출석부
- **제목**: 중복 출석 체크인 멱등성 미지원
- **재현 단계**:
  1. student1 `POST /api/attendance/2/checkin` 성공
  2. 같은 날 동일 요청 재시도
  3. 응답: `{"success":false,"message":"오늘 이미 출석했습니다."}`
- **기대 결과**: 기존 row 업데이트 (emotion 변경 등), 200 반환
- **실제 결과**: 에러 응답 (멱등성 미지원)
- **추정 원인**: `routes/attendance.js` 중복 체크인 차단 로직 (UPDATE 대신 에러 반환)
- **영향 범위**: 학생이 감정을 바꾸고 싶을 때 재체크인 불가
- **권장 처리**: 중복 요청 시 기존 row UPDATE 처리

---

### BUG-20260603-07

- **우선순위**: P1 High
- **시나리오**: S09
- **모듈**: 스스로채움 (마음채움)
- **제목**: /self-learn/emotion-checkin API 미구현
- **재현 단계**:
  1. `POST /api/self-learn/emotion-checkin` 호출
  2. 응답: 404 "요청하신 API를 찾을 수 없습니다."
- **기대 결과**: 학생이 마음채움 페이지에서 감정 체크 가능
- **실제 결과**: 별도 API 없음 (attendance API 재사용해야 함)
- **추정 원인**: `/self-learn/emotion-checkin.html` 페이지는 존재하나 전용 API 미구현
- **영향 범위**: 마음채움 독립적 감정 기록 기능 비동작, `emotion_checkins` 테이블 미사용
- **권장 처리**: `routes/self-learn.js`에 `POST /emotion-checkin` 라우트 추가, `emotion_checkins` 테이블 연동

---

### BUG-20260603-08

- **우선순위**: P2 Medium
- **시나리오**: S10
- **모듈**: 채움콘텐츠
- **제목**: 콘텐츠 API 경로 불일치 (/api/content vs /api/contents)
- **재현 단계**:
  1. 시나리오 패턴대로 `POST /api/content` 호출
  2. 응답: 404
  3. `POST /api/contents` (server.js: `app.use('/api/contents', ...)`)가 실제 경로
- **기대 결과**: `/api/content`로도 접근 가능하거나 문서에 올바른 경로 명시
- **실제 결과**: `/api/content` → 404, `/api/contents` → 정상 동작
- **추정 원인**: `server.js` L138: `app.use('/api/contents', ...)` — 단수/복수 혼용
- **영향 범위**: 외부 클라이언트/문서 기반 개발자 혼란
- **권장 처리**: 시나리오 문서를 `/api/contents`로 통일하거나 `/api/content` alias 추가

---

### BUG-20260603-09

- **우선순위**: P2 Medium
- **시나리오**: S12
- **모듈**: 소통쪽지
- **제목**: 직접 메시지 전송 단일 API 없음 (룸 생성 선행 필요)
- **재현 단계**:
  1. `POST /api/message` with `{to_user_id:3, content:"..."}` 호출
  2. 응답: 404
  3. 실제: `POST /api/message/rooms` + `POST /api/message/rooms/:id/messages` 2단계 필요
- **기대 결과**: 단일 API로 1:1 메시지 전송 가능
- **실제 결과**: 룸 생성 후 메시지 발송 2단계 필요
- **추정 원인**: 시나리오 문서에서 단일 API `/api/message` 가정, 실제 라우트와 상이
- **영향 범위**: 클라이언트 통합 구현 혼란
- **권장 처리**: 시나리오 문서 수정 또는 `/api/message` 단일 래퍼 API 추가

---

### BUG-20260603-10

- **우선순위**: P2 Medium
- **시나리오**: S07
- **모듈**: 스스로채움 (오늘의 학습)
- **제목**: 오늘의 학습 bulk-distribute 콘텐츠 풀 부족 → 0건 생성
- **재현 단계**:
  1. admin `POST /api/admin/daily-learning/bulk-distribute {startDate, endDate, scope:"all"}`
  2. 응답: `{totalCreated:0, totalSkipped:0}`
  3. DB: contents 중 `status='approved' AND content_type IN ('video','quiz') AND school_level IS NOT NULL` 0건
- **기대 결과**: 8학년 × 5과목 = 40개 set에 콘텐츠 배포
- **실제 결과**: 승인된 video/quiz 콘텐츠 없어 0건 생성
- **추정 원인**: 배포 알고리즘은 정상이나 school_level/grade 메타가 붙은 video/quiz 콘텐츠 미존재
- **영향 범위**: 오늘의 학습 기능 전체 비동작
- **권장 처리**: 실제 video/quiz 콘텐츠 시드 또는 `scripts/seed-daily-learning-*.js` 실행 검토

---

### BUG-20260603-11

- **우선순위**: P3 Low
- **시나리오**: S01
- **모듈**: 인증
- **제목**: 세션 쿠키 Max-Age 헤더 누락 (Expires만 사용)
- **재현 단계**:
  1. 로그인 후 Set-Cookie 헤더 확인
  2. `Expires=Wed, 10 Jun 2026` 존재하나 `Max-Age=604800` 없음
- **기대 결과**: 시나리오 기준 `Max-Age=604800` 포함
- **실제 결과**: `Expires` 7일로 기능적 동등하나 명시적 Max-Age 없음
- **추정 원인**: express-session 설정에서 maxAge는 있으나 cookie에 Max-Age 헤더 미전달
- **영향 범위**: 일부 구형 브라우저에서 세션 만료 시점 차이 가능성
- **권장 처리**: express-session 설정 확인 후 필요 시 Max-Age 명시 옵션 추가

---

## 4. 어제 대비 차이

직전 보고서(2026-06-02) 대비:
- 신규 버그: 11건 (BUG-02부터 BUG-11)
- 재발 버그: 분석 미수행 (어제 보고서 항목과 비교 필요)
- 해결된 버그: 별도 확인 필요

> ⚠️ BUG-02(exam 채점), BUG-03(notice read API), BUG-05(exam results API)는 핵심 기능 관련 P1으로 우선 처리 권고.

---

## 5. 시나리오 개선 제안

1. **관리자 비밀번호 일치 (S01)**: 시나리오 0.3절 admin 비밀번호를 실제 값(`1234`)으로 업데이트
2. **수업 status 허용값 명세 (S02)**: 시나리오에 `status:"published"` (또는 허용 목록) 명시
3. **콘텐츠 API 경로 통일 (S10)**: `/api/contents` 경로 명시
4. **메시지 API 경로 (S12)**: 시나리오에 룸 생성 → 메시지 전송 2단계 과정 반영
5. **S08 선행조건 강화**: S07 PASS 여부뿐 아니라 `daily_learning_items > 0` 조건 추가
6. **PDF 파라미터 명세 (S15)**: `from`, `to`, `schoolLevel` 필수 파라미터를 시나리오에 명시
7. **감정 체크인 API (S09)**: `/self-learn/emotion-checkin.html` 연동 API 경로 명시

---

## 6. 환경 메타

| 항목 | 값 |
|------|-----|
| git HEAD | `c51a62f6f4f6e8b3f5d6c8ab4b155d5eb1d7d7a1` |
| git 브랜치 | `main` ✅ |
| Node.js | v22.22.2 |
| npm ci 소요 | 약 5초, 274 패키지 |
| DB 경로 | `data/dacheum.db` (실사용), `db/dachaeum.db` 0바이트 |
| DB 크기 | 1.6MB |
| 총 사용자 수 | 17명 (시드 14명 + 임시 3명) |
| preview 서버 | MCP 도구 없음 → background node server.js 폴백 사용 |
| preview MCP | **불가** (도구 미탑재) — HTTP+DB 폴백으로 전환 |
| 서버 기동 로그 | [DB] gallery_events 시드 실패: FOREIGN KEY constraint failed (마이너) |

---

## 7. 자가검증 결과

```bash
$ git status --porcelain | grep -vE "^.. \"?(보고서|reports)"
```

결과: **아무것도 없음** — 화이트리스트(`보고서/일일점검/`) 외 소스 수정 없음 ✅

변경된 파일:
- `보고서/일일점검/2026-06-03_점검보고서.md` (신규 생성)

---

## 8. 다음 액션 추천 (PM용)

### 즉시 처리 (P1)

| 버그 | 모듈 | 추정 담당 | 수정 내용 |
|------|------|----------|----------|
| BUG-02 | CBT | Backend | `routes/exam.js` L485: `q.answer` → `q.answer ?? q.correct_answer` |
| BUG-03 | 채움클래스 | Backend | `routes/notice.js`에 `POST /:classId/:noticeId/read` 추가 |
| BUG-05 | CBT | Backend | `routes/exam.js`에 supervision + results API 추가 |
| BUG-07 | 스스로채움 | Backend | `routes/self-learn.js`에 `POST /emotion-checkin` 추가 |

### 단기 처리 (P2)

| 버그 | 모듈 | 추정 담당 | 수정 내용 |
|------|------|----------|----------|
| BUG-01 | 문서 | PM | 시나리오 문서 admin 비밀번호 `0000` → `1234` |
| BUG-04 | 채움클래스 | Backend | DB CHECK constraint에 'active' 추가 |
| BUG-10 | 스스로채움 | Backend/데이터 | 비디오·퀴즈 콘텐츠 시드 스크립트 실행 |

---

*이 보고서는 다채움 일일 자동 QA 에이전트가 생성했습니다.*
