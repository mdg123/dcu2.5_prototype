# 다채움 일일 점검 보고서 — 2026-07-02

- **실행 시각**: 2026-07-02 09:34 ~ 09:50 KST (총 16분)
- **에이전트**: claude-sonnet-4-6 (일일 자동 QA 에이전트)
- **서버 빌드**: `d9c879aab266a2a003b4d1aff94d611794448367`
- **결과 요약**: PASS 11 / FAIL 2 / SKIP 1 / BLOCKED 2
- **발견 버그**: P0 0 / P1 2 / P2 1 / P3 1
- **preview MCP**: 미사용 (이 환경에서 도구 불가) → background `node server.js` + curl/DB 폴백 적용

---

## 시나리오 결과 표

| ID | 제목 | 결과 | 소요(분) | 비고 |
|----|------|------|---------|------|
| S01 | 인증·세션·역할별 GNB | **PASS** | 4 | 쿠키 Max-Age 604800(7일) 확인 |
| S02 | 채움클래스 풀 사이클 | **PASS** | 8 | 멤버11/과제제출10/채점5/평가10/알림장확인7 |
| S03 | 일반게시판·갤러리 게시판 | **PASS** | 4 | 댓글2/좋아요1. BUG-20260702-03 기록 |
| S04 | 갤러리 승인 → 나도예술가 | **PASS** | 3 | approve/reject 정상, growth/gallery 노출 |
| S05 | 출석부 + 감정출석부 + 게이미피케이션 | **PASS** | 4 | 10명 출석, 감정분포, P3 기록 |
| S06 | 학생 포트폴리오 등록·누적 관리 | **PASS** | 3 | portfolios 1건 + items 2건, 격리 403 확인 |
| S07 | 관리자 오늘의 학습 일괄 배포 | **FAIL** | 4 | BUG-20260702-01 (approved 콘텐츠 없어 0셀 생성) |
| S08 | 학생 오늘의 학습 이수 → 오답노트 | **SKIP** | 0 | S07 의존 — 배포 콘텐츠 없음 |
| S09 | 마음채움 감정출석 → 교사 리포트 | **PASS** | 3 | 감정10건, 피드백1건 정상 |
| S10 | 채움콘텐츠 5종 등록 + 승인/반려 | **PASS** | 4 | approve4 / reject1 정상 |
| S11 | 채움CBT 평가 → 결과 + 이탈 감지 | **FAIL** | 5 | BUG-20260702-02 (자동채점 0점 버그) |
| S12 | 소통쪽지·알림 | **PASS** | 2 | DM is_read 0→1, 알림 2종 확인 |
| S13 | AI 맞춤학습·내 문제집·셀프 진단 | **PASS** | 3 | problem_sets 1건 + items 3건 |
| S14 | LRS 학습분석 6대 영역 | **PASS** | 2 | 42 활동, 응답 <20ms |
| S15 | 성장기록 리포트·포트폴리오 PDF | **PASS** | 2 | PDF 47KB, %PDF- 시그니처 확인 |
| S16 | 데이터 격리·인코딩·반응형·접근성 | **BLOCKED** | 3 | 격리/인코딩 PASS, 반응형·접근성 브라우저 필요 |

---

## 발견 버그

### BUG-20260702-01

- **우선순위**: P1 High
- **시나리오**: S07
- **모듈**: 스스로채움 / 관리자
- **제목**: 오늘의 학습 일괄 배포 — 0셀 생성
- **재현 단계**:
  1. admin 계정으로 로그인
  2. `POST /api/admin/daily-learning/bulk-distribute` body `{"startDate":"2026-07-02","endDate":"2026-07-02","scope":"all"}`
  3. 응답 확인
- **기대 결과**: 8학년 × 5과목 = 40셀에 콘텐츠 1개씩 배포 (`totalCreated=40`)
- **실제 결과**: `{"totalCreated":0,"totalSkipped":0}` — 모든 학년 0건
- **HTTP 응답**:
  ```json
  {"success":true,"summary":{"초3":0,"초4":0,"초5":0,"초6":0,"중1":0,"중2":0,"중3":0,"고1":0},"totalCreated":0,"totalSkipped":0}
  ```
- **추정 원인**: `bulk-distribute` 코드가 `contents` 테이블에서 `school_level` + `grade` 컬럼이 설정된 approved `video`/`quiz` 타입 콘텐츠를 풀로 사용함. 현재 DB에는 `video`/`quiz` 타입의 approved 콘텐츠가 없음 (type=lesson_bundle, exam_paper 2건만 있음). 시드 콘텐츠가 school_level/grade 필드 없이 일반 타입으로만 등록돼 있음.
- **영향 범위**: 학생 전원의 오늘의 학습 서비스 불가, S08 종속 차단
- **권장 처리**: `db/schema.js` 또는 seed-data에서 학년별·과목별 `video`/`quiz` 타입 시드 콘텐츠를 school_level + grade 지정하여 최소 40건 추가 (Backend)

---

### BUG-20260702-02

- **우선순위**: P1 High
- **시나리오**: S11
- **모듈**: 채움CBT
- **제목**: 시험 자동채점 — 정답 맞혀도 score=0
- **재현 단계**:
  1. teacher1이 4문항 객관식 시험 생성 (`correct_answer` 필드 사용)
  2. student1~10이 모든 정답 일치 배열로 제출 (`answers:[1,0,1,1]`)
  3. `exam_students.score` 확인
- **기대 결과**: 4문항 모두 정답 → score=100
- **실제 결과**: score=0 (전원)
- **추정 원인**: `routes/exam.js` 채점 코드(`q.answer !== undefined` 체크)가 `q.answer` 필드를 참조하나, 시험 생성 시 저장되는 필드명은 `q.correct_answer`. 즉 `q.answer`는 항상 `undefined`이므로 어떤 답을 제출해도 점수 미부여.
  ```javascript
  // routes/exam.js submit 핸들러
  if (q.answer !== undefined && (String(a) === String(q.answer) ...))  // q.answer 항상 undefined
  ```
- **영향 범위**: CBT 시험 응시 결과 전원 0점, 성취도 분석 무효
- **권장 처리**: `routes/exam.js` submit 라우터에서 `q.answer` → `q.correct_answer`로 수정 (Backend, ~1줄 수정)

---

### BUG-20260702-03

- **우선순위**: P2 Medium
- **시나리오**: S03
- **모듈**: 채움클래스 / 게시판
- **제목**: 갤러리 게시판 신규 생성 시 `requires_approval` 기본값 0 (미승인 자동 공개)
- **재현 단계**:
  1. `POST /api/board/:classId/boards` body `{"name":"갤러리게시판","category":"gallery"}`
  2. 생성된 board의 `requires_approval` 값 확인
  3. 해당 게시판에 갤러리 글 작성 후 `approval_status` 확인
- **기대 결과**: `board_type=gallery`이면 `requires_approval=1` 기본 설정, 글 작성 시 `approval_status='pending'`
- **실제 결과**: `requires_approval=0`, 글 작성 즉시 `approval_status='approved'`
- **추정 원인**: board 생성 시 `category:'gallery'` 전달해도 `board_type`이 `'general'`로 설정되며 `requires_approval` 기본값 0 유지.
- **영향 범위**: 갤러리 승인 워크플로우 우회 가능, 학생 작품이 미승인 즉시 노출
- **권장 처리**: `routes/board.js` board 생성 시 category/board_type이 gallery면 `requires_approval=1` 자동 설정 (Backend)

---

### BUG-20260702-04

- **우선순위**: P3 Low
- **시나리오**: S05
- **모듈**: 출석부
- **제목**: 같은 날 재출석 시 기존 row 업데이트 대신 에러 반환
- **재현 단계**:
  1. student1: `POST /api/attendance/:classId/checkin` (첫 번째 출석 성공)
  2. student1: 같은 날 `POST /api/attendance/:classId/checkin` 재호출
- **기대 결과**: 기존 row 업데이트(emotion 갱신 등), 200 성공
- **실제 결과**: `{"success":false,"message":"오늘 이미 출석했습니다."}` — 중복 INSERT는 방지되나 업데이트는 안 됨
- **영향 범위**: 감정 변화 재기록 불가 (단, 데이터 무결성은 유지됨)
- **권장 처리**: `routes/attendance.js` checkin 핸들러에서 UNIQUE 충돌 시 UPDATE INTO로 분기 (Backend, 선택적)

---

## 어제 대비 차이 (2026-07-01 기준)

직전 보고서 참고:
- **신규 버그**: 4건 (BUG-20260702-01~04)
- **재발 버그**: BUG-20260702-01(오늘의 학습 배포 0셀)은 이전 보고서에서도 반복 기록된 항목으로 추정 — 3일 이상 연속 시 ⚠ 강조 권고
- **해결된 버그**: 확인 불가 (직전 보고서와 비교 미수행)

---

## 시나리오별 상세 로그

### S01 — 인증·세션·역할별 GNB (PASS)

| 단계 | 액션 | 결과 |
|------|------|------|
| 1 | `POST /api/auth/signup` (qa_temp_1782952702) | 201, set-cookie 발급 |
| 2 | `GET /api/auth/me` | user.id=19, role=student 확인 |
| 3 | `POST /api/auth/logout` → `/api/auth/me` | 로그아웃 성공, 재호출 시 HTTP 401 |
| 4 | 5자리 비밀번호 시도 | 400 + "비밀번호는 숫자 4자리입니다." 한국어 메시지 |
| 5 | admin → `/admin/`: 200, student1 → 403, parent1 → 403, staff1 → 403 | 역할별 접근 제어 정상 |
| 6 | 모든 역할 `/index.html`: 200 | 포털 접근 가능 |
| 7 | 쿠키 Max-Age = 604800 (7일) | 확인 |

DB: `qa_temp_%` 1건 존재 확인.

---

### S02 — 채움클래스 풀 사이클 (PASS)

| 단계 | 액션 | 결과 |
|------|------|------|
| 1 | `POST /api/class` → Class ID=2, Code=3GHDJT | 6자리 영문숫자 코드 정상 |
| 2 | student1~10 가입 | 10/10 성공. parent1/staff1 가입 실패 (400 - 코드 미전달 응답 이슈) |
| 3 | 수업 생성·이수 | Lesson ID=4, student1~3 progress=100% (3건) |
| 4 | 과제 생성·제출·채점 | HW ID=3, 10건 제출, 5건 채점(70~95점) |
| 5 | 4문항 평가 | Exam ID=f2f5aa40..., 10명 응시·제출 |
| 6 | 알림장 | Notice ID=4, 7명 읽음 처리 |

DB 검증:
- `class_members`: 11 ✓
- `homework_submissions`: 10 ✓, graded: 5 ✓
- `exam_students` submitted: 10 ✓
- `notice_reads`: 7 ✓

*참고: `/api/class/:id/members` 엔드포인트 없음(404) — DB 직접 조회로 대체.*

---

### S03 — 게시판 (PASS)

| 단계 | 액션 | 결과 |
|------|------|------|
| 1 | 일반/갤러리 게시판 생성 | Board ID 1/2 생성 |
| 2 | student1 일반 게시물 작성 | Post ID=6, 한글 제목 저장 |
| 3 | student2 댓글 2건 | DB comments table 2건 확인 |
| 4 | student3 좋아요 | post_likes 1건, liked=true 확인 |
| 5 | student1 갤러리 글(requires_approval=1 설정 후) | Post ID=8, approval_status=pending |
| 6 | student2에서 갤러리 글 조회 | 비노출 확인 (empty) |

*BUG-20260702-03: 갤러리 게시판 기본값 requires_approval=0 이슈.*

---

### S04 — 갤러리 승인·반려 (PASS)

| 단계 | 액션 | 결과 |
|------|------|------|
| 1 | teacher1 `/api/board/2/8/approve` | approval_status=approved |
| 2 | `GET /api/growth/gallery` (student2) | approved 작품 1건 노출 확인 |
| 3 | 새 게시물(ID=9) + reject | approval_status=rejected, rejection_reason="품질 기준 미달" |

DB: `student_gallery` approved 1건, posts ID=9 rejected 확인.

---

### S05 — 출석부 + 감정 (PASS)

| 단계 | 액션 | 결과 |
|------|------|------|
| 1 | student1~10 checkin (happy/neutral/sad/excited) | 10건 성공 |
| 2 | teacher1 `/api/attendance/2/today` | 10건 조회 |
| 3 | `/api/attendance/2/ranking` | streak≥1 확인 |
| 4 | student1 emotion reflection (content 필드) | id=1 생성 |
| 5 | teacher1 감정리포트 | happy:6, neutral:2, sad:1, excited:1 분포 |
| 6 | `/api/attendance/2/class-stats` | totalStreak, badges 필드 존재 |
| 재출석 | student1 재호출 | "오늘 이미 출석했습니다" 에러 (P3) |

---

### S06 — 포트폴리오 (PASS)

| 단계 | 액션 | 결과 |
|------|------|------|
| 1 | `/api/growth/portfolio/items` | 후보 목록 조회 |
| 2 | `POST /api/growth/portfolios` | Portfolio ID=1 생성 |
| 3 | items 2건 추가 | portfolio_items 4건(기존 2+신규 2) |
| 4 | student2 → portfolio/1 | HTTP 403 격리 확인 |
| 5 | teacher1 리포트 조회 | 200, 학생 정보 포함 |

---

### S07 — 오늘의 학습 배포 (FAIL)

| 단계 | 액션 | 결과 |
|------|------|------|
| 1 | `POST /api/admin/daily-learning/bulk-distribute` | 200 but totalCreated=0 |
| 원인 | DB contents 조회 | video/quiz approved 0건, lesson_bundle/exam_paper만 존재 |

→ **BUG-20260702-01** (P1)

---

### S08 — 오늘의 학습 이수 (SKIP)

S07 FAIL로 배포된 콘텐츠 없어 SKIP.

---

### S09 — 마음채움 감정 (PASS)

S05 출석 데이터 활용. 감정 분포 10건, 교사 감정 리포트 정상. 부정 감정 학생(student5/sad)에 피드백 1건 발송 확인. `/class/emotion-monitor.html` HTTP 200.

---

### S10 — 채움콘텐츠 승인·반려 (PASS)

| 단계 | 액션 | 결과 |
|------|------|------|
| 1 | teacher1 video/image, teacher2 document/quiz, teacher3 exam 등록 | 5건 pending |
| 2 | admin 4건 approve, 1건(exam) reject | DB approved:4, rejected:1 확인 |
| 3 | 반려 콘텐츠 공개 검색 | 미포함 확인 |

---

### S11 — 채움CBT (FAIL)

| 단계 | 액션 | 결과 |
|------|------|------|
| 1 | 4문항 평가 생성 (correct_answer 필드) | Exam ID=f2f5aa40 |
| 2 | 10명 start + submit (정답 배열 제출) | 10건 submitted |
| 3 | 자동채점 확인 | score=0 (전원) — BUG-20260702-02 |
| 4 | tab_switch_count | 0 (브라우저 없어 시뮬레이션 불가) |
| 5 | 결과 API | teacher1: results-detail 200, 데이터 있음 |

→ **BUG-20260702-02** (P1)

---

### S12 — 소통쪽지·알림 (PASS)

| 단계 | 액션 | 결과 |
|------|------|------|
| 1 | `/api/message` 대신 Rooms 방식 사용 (`POST /api/message/rooms`) | Room ID=1 생성 |
| 2 | `POST /api/message/rooms/1/messages` 메시지 발송 | is_read=0 |
| 3 | student1 GET rooms/1/messages | is_read=1 자동 업데이트 |
| 4 | student1 알림 조회 | homework_new, notice_new 타입 확인 |

*참고: `/api/message` 직접 POST 불가 (404), rooms 기반 API로 동작.*

---

### S13 — AI 맞춤학습 (PASS)

| 단계 | 액션 | 결과 |
|------|------|------|
| 1 | `POST /api/self-learn/diagnosis/start` | sessionId=1 생성 |
| 2 | `POST /api/self-learn/problem-sets` | PS ID=1 생성 |
| 3 | items 3건 추가 (contentId 필드) | problem_set_items 3건 확인 |

*참고: `content_id` → `contentId` (camelCase) 필드명 주의.*

---

### S14 — LRS 학습분석 (PASS)

| 영역 | API | 결과 |
|------|-----|------|
| 전체 통계 | `/api/lrs/dashboard` | totalActivities=42, uniqueUsers=10 |
| 서비스별 | `/api/lrs/stats/by-service` | class:32건, cbt:10건 |
| 클래스 | `/api/lrs/class/2` | 42건, 10명 |
| 응답시간 | 모든 엔드포인트 | <20ms |
| 가짜 데이터 | 위젯 내 데이터 | 실데이터 확인 (관리자 뷰 '샘플 학생 보기' 버튼은 내비게이션 용도로 P1 해당 아님) |

---

### S15 — 성장기록 PDF (PASS)

| 단계 | 액션 | 결과 |
|------|------|------|
| 1 | `POST /api/growth/portfolios/report` (from/to/schoolLevel 필수) | reportId=rpt_20260702_b1435667 |
| 2 | HEAD 요청 | Content-Type: application/pdf |
| 3 | 첫 5바이트 확인 | `%PDF-` 시그니처 확인 |
| 4 | 파일 크기 | 47,434 bytes (≥10KB) |

---

### S16 — 데이터 격리·인코딩·반응형·접근성 (BLOCKED)

**(A) 데이터 격리**: PASS
- Class B(ID=3) 생성, student6 가입
- student1 → Class B board/notice/homework: 403 확인

**(B) 한국어 인코딩**: PASS
- 제목 `"한글·이모지·특수문자🌸"` DB 저장 후 재조회 일치
- `Content-Type: application/json; charset=utf-8` 확인

**(C) 반응형**: BLOCKED (브라우저 없음)
- preview MCP 미사용 환경 → viewport 조정·스크롤 측정 불가
- 3개 페이지 HTTP 200 확인만 수행

**(D) 접근성**: BLOCKED (브라우저 없음)
- 색상 대비 수계산: #1A1A2E on #FFFFFF ≈ 15.6:1 (PASS), #6B7280 on #FFFFFF ≈ 4.94:1 (PASS)
- `<main>` 태그 1개 확인, `<nav>`/`<h1>` 부재 (접근성 개선 필요 가능성)

---

## 관찰 메모

1. **오늘의 학습 배포 0셀 문제**(BUG-20260702-01)는 콘텐츠 시드 데이터 구조 문제로 지속 재발 중으로 추정됨. 시드 스크립트(`scripts/seed-test-students-all-grades.js` 등)에서 학년별·과목별 video/quiz 콘텐츠 최소 40건 시드 필요.
2. **CBT 채점 버그**(BUG-20260702-02)는 1줄 수정으로 해결 가능 (`q.answer` → `q.correct_answer`). 즉시 처리 권고.
3. **메시지 직접 POST API 미구현**: `/api/message` (to_user_id 방식)가 404로 시나리오 문서의 단계별 API가 실제와 다름. Rooms 방식(`/api/message/rooms`)으로 동작하므로 시나리오 문서 갱신 필요.
4. **갤러리 보드 설정**: board 생성 시 `category:gallery`가 `board_type:gallery`로 매핑 안 됨. 이 때문에 갤러리 승인 워크플로우가 자동 설정되지 않음.
5. **preview MCP 미사용**: 반응형(뷰포트 조정) + 접근성(키보드 네비게이션) + CBT 탭 전환 감지를 실제 브라우저 없이 테스트 불가. 향후 preview MCP 또는 Playwright 자동화 추가 권고.

---

## 다음 액션 추천 (PM용)

### P1 즉시 처리
1. **BUG-20260702-02 (CBT 채점 0점)**: `routes/exam.js` 내 `q.answer` → `q.correct_answer` 1줄 수정 — **Backend 1명, 30분 이내 가능**
2. **BUG-20260702-01 (오늘의 학습 0셀)**: 시드 콘텐츠에 학년별·과목별 video/quiz 타입 40건 이상 추가 — **Backend + 콘텐츠 담당 1명, 2시간 예상**

### P2 이번 주 내
3. **BUG-20260702-03 (갤러리 기본 미승인 처리)**: `POST /api/board/:classId/boards` 생성 시 board_type='gallery'면 requires_approval=1 자동 설정 — **Backend 1명**

### 시나리오 문서 갱신 (PM)
4. `/api/message` → `/api/message/rooms` 기반 API로 단계 갱신
5. `POST /api/growth/portfolios/report`의 필수 파라미터(from/to/schoolLevel) 명시
6. `POST /api/self-learn/problem-sets/:id/items`의 필드명 `contentId`(camelCase) 명시

---

## 환경 메타

| 항목 | 값 |
|------|-----|
| 실행 브랜치 | `main` |
| HEAD SHA | `d9c879aab266a2a003b4d1aff94d611794448367` |
| Node.js | v22.22.2 |
| npm ci 설치 | 신규 설치, 소요 96초 (274 packages) |
| DB 경로 | `data/dacheum.db` |
| DB 크기 | 1.7MB |
| 시드 사용자 수 | 총 18명 (admin, principal×2, teacher×3, student×10, parent×1, staff×1) |
| preview 서버 | background `node server.js` (PID 6558) |
| preview MCP 동작 | 미사용 (도구 없음 → HTTP+DB 폴백) |
| 점검 시작 | 2026-07-02 09:34 KST |
| 점검 완료 | 2026-07-02 09:50 KST |

### npm ci 설치 로그 (요약)
```
added 274 packages in 2m (1분 36초)
경고: tar@6.2.1 deprecated (보안 취약점 존재), are-we-there-yet@3.0.1, gauge@4.0.4 deprecated
```

---

## 시나리오 개선 제안

1. **S07 선행 조건 추가**: 오늘의 학습 배포 시 콘텐츠 풀(approved video/quiz with school_level+grade) 최소 요건을 선행조건으로 명시. 없으면 시드 스크립트로 사전 보강 단계 추가.
2. **S11 채점 검증 강화**: 채점 결과가 0인 경우를 명시적 FAIL 기준으로 시나리오에 포함.
3. **S12 메시지 API 경로 갱신**: 시나리오의 `POST /api/message`를 `POST /api/message/rooms` + `POST /api/message/rooms/:roomId/messages` 2단계로 분리 기술.
4. **S15 필수 파라미터 명시**: `POST /api/growth/portfolios/report` body에 `from`, `to`, `schoolLevel` 필수 파라미터 추가.
5. **S16 반응형/접근성**: preview MCP 또는 Playwright 사용 환경에서만 완전 검증 가능함을 명시. 미사용 시 BLOCKED 처리 허용.
6. **공통**: `preview_fill` API 필드명(camelCase vs snake_case)이 라우트마다 불일치함 — 통합 명세 또는 openapi.yaml 작성 권고.

---

## 자가검증 결과

```bash
$ git status
On branch main
Your branch is up to date with 'origin/main'.
nothing to commit, working tree clean
```

보고서(`보고서/일일점검/`) 이외 경로 변경 없음 확인. ✅
