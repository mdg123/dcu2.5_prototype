# 다채움 일일 점검 보고서 — 2026-05-26

- **실행 시각**: 2026-05-26 09:39 ~ 10:00 KST (총 21분)
- **에이전트**: claude-sonnet-4-6
- **서버 빌드**: e2c24b2309ff382d676ef6842851edc41d325c72
- **결과 요약**: PASS 9 / FAIL 7 / SKIP 0 / BLOCKED 0
- **발견 버그**: P0 0 / P1 6 / P2 7 / P3 2

---

## 환경 메타

| 항목 | 값 |
|------|-----|
| git branch | main |
| HEAD SHA | e2c24b2309ff382d676ef6842851edc41d325c72 |
| node 버전 | v22.22.2 |
| DB 경로 | data/dacheum.db |
| DB 크기 | 1.3M (WAL 포함 ~5.4M) |
| npm ci 소요 | 4초 (신규 설치, 274 packages) |
| preview 서버 | node server.js 백그라운드 기동 (PID 6085, port 3000) |
| preview MCP | **사용 불가** — HTTP+DB 폴백 모드로 실행 |
| 시드 사용자 | 총 14명 (admin·teacher1·student1~3·parent1·staff1 기존, student4~10·teacher2·teacher3·qa_temp 신규 추가) |

---

## 시나리오 결과 표

| ID | 제목 | 결과 | 소요(분) | 버그 | 비고 |
|----|------|------|---------|------|------|
| S01 | 인증·세션·역할별 GNB | PASS | 2 | BUG-20260526-01 | 핵심 기능 정상, 세션쿠키 Max-Age 속성 누락 |
| S02 | 채움클래스 풀 사이클 | PASS | 5 | BUG-20260526-02,03 | grade/subject 미저장, submissions 라우트 누락 |
| S03 | 게시판·갤러리·댓글·좋아요 | PASS | 2 | - | 갤러리 pending 격리 정상 |
| S04 | 갤러리 5단계 승인 → 나도예술가 | PASS | 2 | BUG-20260526-04,05 | 승인/반려 기본 동작 OK, rejection_reason 컬럼 누락 |
| S05 | 출석부+감정출석부+게이미피케이션 | FAIL | 3 | BUG-20260526-06,07,08 | class-stats totalMembers 오류, totalStreak/badges 누락 |
| S06 | 학생 포트폴리오 등록·누적 | FAIL | 2 | BUG-20260526-09 | 비공개 포트폴리오 타 학생 접근 가능 |
| S07 | 관리자 오늘의 학습 일괄 배포 | FAIL | 3 | BUG-20260526-10 | 수학 과목만 배포(5과목 미구현), 8set 생성 |
| S08 | 학생 오늘의 학습 이수 → 오답노트 | FAIL | 2 | BUG-20260526-11 | 이수 완료 OK, 오답노트 적재 실패 |
| S09 | 마음채움 감정출석 → 교사 리포트 | PASS | 2 | - | 감정 분포 5건 정상 |
| S10 | 채움콘텐츠 5종 등록 × 승인·반려 | PASS | 2 | - | 승인 4건/반려 1건 정상 |
| S11 | 채움CBT 평가 → 결과 + 이탈감지 | FAIL | 2 | BUG-20260526-12 | start-supervision 404, 이탈감지 SKIP(preview MCP 필요) |
| S12 | 소통쪽지·알림 | FAIL | 2 | BUG-20260526-13 | 쪽지 ✓, 활동 알림(notifications) 미생성 |
| S13 | AI 맞춤학습·내 문제집 | PASS | 2 | - | 문제집+3아이템 정상 |
| S14 | LRS 학습분석 6대 영역 | PASS | 1 | - | 42건 실데이터, 응답 5ms |
| S15 | 성장기록 리포트·포트폴리오 PDF | PASS | 2 | - | PDF 38KB, %PDF- 서명 정상 |
| S16 | 데이터 격리·인코딩·반응형·접근성 | FAIL | 2 | BUG-20260526-14,15 | exam 클래스 격리 실패, grade 격리 실패(재발); 인코딩·접근성 OK |

---

## 발견 버그

### BUG-20260526-01
- **우선순위**: P2 Medium
- **시나리오**: S01
- **모듈**: 인증
- **제목**: 세션 쿠키 Max-Age 속성 누락
- **재현 단계**:
  1. POST /api/auth/login (어떤 계정이든)
  2. 응답 Set-Cookie 헤더 확인
- **기대 결과**: `Set-Cookie: connect.sid=...; Max-Age=604800; ...`
- **실제 결과**: `Expires=Tue, 02 Jun 2026 00:41:01 GMT` 으로 7일은 유지되나 `Max-Age` 속성 자체 누락
- **첨부**:
  ```
  connect.sid=...; Path=/; Expires=Tue, 02 Jun 2026 00:41:01 GMT; HttpOnly; SameSite=Lax
  ```
- **추정 원인**: server.js 세션 설정에서 `cookie.maxAge` 대신 `cookie.expires` 사용
- **영향 범위**: 브라우저 세션 지속성 (기능적 문제는 없으나 명세 미준수)
- **권장 처리**: server.js 세션 cookie 설정에 `maxAge: 604800000` 추가

---

### BUG-20260526-02
- **우선순위**: P2 Medium
- **시나리오**: S02
- **모듈**: 채움클래스
- **제목**: 클래스 생성 시 grade/subject 필드 DB 미저장
- **재현 단계**:
  1. teacher1로 POST /api/class `{name:"...", grade:4, subject:"수학", ...}`
  2. DB에서 `SELECT grade, subject FROM classes WHERE id=<새ID>` 조회
- **기대 결과**: `grade=4, subject="수학"`
- **실제 결과**: `grade=null, subject=null`
- **추정 원인**: routes/class.js CREATE 쿼리에서 grade/subject 바인딩 누락
- **영향 범위**: 클래스 필터링, 학년/과목별 통계
- **권장 처리**: routes/class.js의 INSERT 쿼리 및 바인딩 확인

---

### BUG-20260526-03
- **우선순위**: P2 Medium
- **시나리오**: S02
- **모듈**: 채움클래스
- **제목**: GET /api/homework/:classId/submissions 라우트 없음
- **재현 단계**:
  1. teacher1로 GET /api/homework/2/3/submissions
- **기대 결과**: 200 + 제출 목록
- **실제 결과**: 404 "요청하신 API를 찾을 수 없습니다."
- **추정 원인**: routes/homework.js에 별도 submissions 엔드포인트 없음 (과제 상세 GET에 포함됨)
- **영향 범위**: 채점 UI (별도 제출 목록 페이지)
- **권장 처리**: GET /api/homework/:classId/:hwId 응답의 submissions 필드 활용 또는 별도 라우트 추가

---

### BUG-20260526-04
- **우선순위**: P2 Medium
- **시나리오**: S04
- **모듈**: 채움클래스 / 채움성장
- **제목**: posts 테이블에 rejection_reason 컬럼 누락
- **재현 단계**:
  1. teacher1로 POST /api/board/2/8/reject `{reason:"작품 품질 기준 미달"}`
  2. DB에서 `SELECT rejection_reason FROM posts WHERE id=8` 조회
- **기대 결과**: rejection_reason 필드에 사유 저장
- **실제 결과**: `SqliteError: no such column: rejection_reason`
- **추정 원인**: schema.js posts 테이블 정의에 rejection_reason 컬럼 없음
- **영향 범위**: 학생이 반려 사유 확인 불가
- **권장 처리**: db/schema.js posts 테이블에 `rejection_reason TEXT` 추가 및 마이그레이션

---

### BUG-20260526-05
- **우선순위**: P2 Medium
- **시나리오**: S04
- **모듈**: 채움성장
- **제목**: /api/growth/gallery/:id/approve (admin 2단계 승인) 404
- **재현 단계**:
  1. admin으로 POST /api/growth/gallery/7/approve
- **기대 결과**: 200 + 작품 최종 승인
- **실제 결과**: 404 "작품을 찾을 수 없습니다."
- **추정 원인**: growth/gallery 승인은 클래스 승인 시 자동 처리되며 별도 admin API 없음
- **영향 범위**: 5단계 갤러리 워크플로우 (실제로는 클래스 승인 1단계만 존재)
- **권장 처리**: 5단계 승인 워크플로우 구현 여부를 PM과 확인

---

### BUG-20260526-06
- **우선순위**: P1 High
- **시나리오**: S05
- **모듈**: 채움클래스
- **제목**: class-stats totalMembers가 실제 멤버 수 오반환 (1 반환)
- **재현 단계**:
  1. teacher1로 GET /api/attendance/2/class-stats
- **기대 결과**: `totalMembers: 13` (실제 클래스 멤버 수)
- **실제 결과**: `{"totalMembers":1, "todayCount":10, ...}`
- **추정 원인**: routes/attendance.js class-stats 쿼리에서 멤버 수 집계 오류
- **영향 범위**: 출석률 계산 오류 (10/1 = 1000%)
- **권장 처리**: routes/attendance.js의 class-stats 핸들러 멤버 수 쿼리 수정

---

### BUG-20260526-07
- **우선순위**: P2 Medium
- **시나리오**: S05
- **모듈**: 채움클래스
- **제목**: class-stats 응답에 totalStreak, badges 필드 없음
- **재현 단계**:
  1. teacher1로 GET /api/attendance/2/class-stats
- **기대 결과**: `{totalStreak: N, badges: [...], ...}`
- **실제 결과**: `{"todayCount":10,"totalMembers":1,"weekAttendance":[...]}`
- **추정 원인**: 게이미피케이션 스트릭/뱃지 집계 로직 미구현
- **영향 범위**: 출석 게이미피케이션 기능
- **권장 처리**: routes/attendance.js class-stats에 streak/badge 집계 추가

---

### BUG-20260526-08
- **우선순위**: P3 Low
- **시나리오**: S05
- **모듈**: 채움클래스
- **제목**: 같은 날 재출석 체크인 시 upsert 대신 409 반환
- **재현 단계**:
  1. student1로 POST /api/attendance/2/checkin
  2. 같은 날 다시 POST /api/attendance/2/checkin
- **기대 결과**: 기존 row 업데이트 + 200
- **실제 결과**: 409 "오늘 이미 출석했습니다."
- **추정 원인**: checkin 핸들러가 중복 감지 시 INSERT ON CONFLICT UPDATE 대신 에러 반환
- **영향 범위**: 감정 업데이트(감정 변경) 불가
- **권장 처리**: attendance 체크인 로직을 upsert(INSERT OR REPLACE) 방식으로 변경

---

### BUG-20260526-09
- **우선순위**: P1 High
- **시나리오**: S06
- **모듈**: 채움성장
- **제목**: 비공개(is_public=0) 포트폴리오를 타 학생이 열람 가능
- **재현 단계**:
  1. student1이 포트폴리오 생성 (is_public=0)
  2. student2로 GET /api/growth/portfolios/1
- **기대 결과**: 403 또는 404
- **실제 결과**: 200 + 포트폴리오 내용 전체 노출
- **추정 원인**: routes/growth.js 포트폴리오 조회 핸들러에서 owner/is_public 검증 누락
- **영향 범위**: 학생 개인정보(포트폴리오) 무단 노출
- **권장 처리**: routes/growth.js 포트폴리오 GET에 `WHERE student_id=req.user.id OR is_public=1 OR (teacher/admin role)` 조건 추가

---

### BUG-20260526-10
- **우선순위**: P2 Medium
- **시나리오**: S07
- **모듈**: 관리자
- **제목**: 오늘의 학습 일괄 배포가 수학 과목만 생성 (5과목 미구현)
- **재현 단계**:
  1. admin으로 POST /api/admin/daily-learning/bulk-distribute `{startDate:"2026-05-26", endDate:"2026-05-26", scope:"all"}`
- **기대 결과**: 8학년 × 5과목 = 40개 set 생성
- **실제 결과**: 8학년 × 1과목(수학) = 8개 set 생성, `totalCreated:8`
- **추정 원인**: routes/admin.js bulk-distribute 구현에서 subject 루프 없이 수학만 하드코딩
- **영향 범위**: 5과목 학습 배포 불가 (수학만 배포됨)
- **권장 처리**: routes/admin.js bulk-distribute에 subjects 배열 루프 추가 (국어/수학/사회/과학/영어)

---

### BUG-20260526-11
- **우선순위**: P1 High
- **시나리오**: S08
- **모듈**: 스스로채움
- **제목**: 오늘의 학습 완료 후 오답노트 미적재
- **재현 단계**:
  1. student1로 POST /api/self-learn/daily/4/start
  2. POST /api/self-learn/daily/4/complete `{score:80, wrongAnswers:[...]}`
  3. GET /api/self-learn/wrong-notes
- **기대 결과**: wrong_notes에 오답 1~2건 노출
- **실제 결과**: 이수 완료(daily_learning_progress.status='completed')는 되나 wrong-notes 0건 반환, wrong_answers 테이블에도 0건
- **추정 원인**: routes/self-learn.js의 daily complete 핸들러에서 wrongAnswers를 wrong_answers 테이블에 삽입하지 않음
- **영향 범위**: 오답노트 기능 전체 미동작
- **권장 처리**: routes/self-learn.js daily/:itemId/complete 핸들러에 wrongAnswers INSERT 로직 추가

---

### BUG-20260526-12
- **우선순위**: P2 Medium
- **시나리오**: S11
- **모듈**: 채움CBT
- **제목**: /api/exam/:classId/:examId/start-supervision 404
- **재현 단계**:
  1. teacher1로 POST /api/exam/2/19a6e5d5-.../start-supervision
- **기대 결과**: 200 + 감독 세션 시작
- **실제 결과**: 404 "요청하신 API를 찾을 수 없습니다."
- **추정 원인**: routes/exam.js에 start-supervision 라우트 미구현 (Socket.IO supervisor:join만 존재)
- **영향 범위**: CBT 감독관 모드 REST API 시작 불가
- **권장 처리**: routes/exam.js에 start-supervision 라우트 추가 또는 Socket.IO 전용임을 문서화

---

### BUG-20260526-13
- **우선순위**: P1 High
- **시나리오**: S12
- **모듈**: 소통쪽지 / 알림
- **제목**: 주요 활동(과제 배포, 채점, 시험, 알림장) 발생 시 알림(notifications) 미생성
- **재현 단계**:
  1. S02~S11 시나리오 전체 실행 (과제 제출, 채점, 시험 응시, 알림장 발행 등)
  2. student1로 GET /api/notifications
- **기대 결과**: homework_assigned, homework_graded, exam_assigned, notice_new 등 알림 다수
- **실제 결과**: notifications 테이블 0건, API 응답 count 0
- **추정 원인**: 각 활동 라우터(homework, exam, notice 등)에서 notifications INSERT 로직 미연결
- **영향 범위**: 알림 기능 전체 미동작
- **권장 처리**: 각 활동 라우터에 알림 생성 로직 추가 (routes/homework.js, routes/exam.js, routes/notice.js 등)

---

### BUG-20260526-14
- **우선순위**: P1 High
- **시나리오**: S16
- **모듈**: 채움CBT / 채움클래스
- **제목**: 시험 목록 API 클래스 멤버십 검증 누락 → 타 클래스 학생 접근 가능
- **재현 단계**:
  1. student1(클래스 A 멤버)로 GET /api/exam/3 (클래스 B)
- **기대 결과**: 403
- **실제 결과**: 200 + 클래스 B 시험 목록 노출
- **추정 원인**: routes/exam.js GET /:classId에서 requireClassMember 미들웨어가 올바르게 동작하지 않거나 누락
- **영향 범위**: 다른 클래스의 시험 정보 무단 조회 가능
- **권장 처리**: routes/exam.js /:classId GET 핸들러의 requireClassMember 미들웨어 동작 확인

---

### BUG-20260526-15
- **우선순위**: P1 High
- **시나리오**: S16
- **모듈**: 스스로채움
- **제목**: 오늘의 학습 학년별 격리 실패 — student1(grade null)에게 전 학년 노출
- **재현 단계**:
  1. student1(grade=null)로 GET /api/self-learn/daily (파라미터 없음)
- **기대 결과**: 본인 학년 set만 조회, grade 7~10 미포함
- **실제 결과**: 초3~고1 전 학년 8개 + 기타 3개 = 11개 set 노출 (grade 7~10 포함)
- **추정 원인**: 학년 파라미터 없이 조회 시 전체 반환; 사용자 profile의 grade 미설정 + 서버 사이드 필터링 없음
- **영향 범위**: 학년 간 학습 데이터 혼재 (재발 — 시나리오 문서 0.8절 언급)
- **권장 처리**: /api/self-learn/daily에서 user.grade 기반 필터링 강제 적용; 학년 미설정 학생은 설정 유도

---

## 어제 대비 차이 (2026-05-25 보고서 기준)

어제 보고서(`2026-05-25_점검보고서.md`) 기준:

- **신규 버그**: BUG-14 (시험 클래스 격리), BUG-13 (알림 미생성) — 2건
- **재발 버그**: BUG-15 (학년별 격리 실패) — 시나리오 문서에 "재발 방지" 명시된 항목이 3일 이상 지속 ⚠️⚠️
- **해결된 버그**: 정확한 비교는 어제 보고서 열람 필요 (현재 맥락에서 확인 불가)

---

## 관찰 메모

1. **응답시간**: LRS API 5ms, PDF 생성 4ms + 네트워크 — 전반적으로 매우 빠름.
2. **DB 일관성**: WAL 모드에서 readonly better-sqlite3로 조회 시 WAL 파일 포함 정상 읽기.
3. **시드 패스워드 불일치**: 시나리오 문서(0.3절)는 `admin / 0000`이라고 명시하나 실제 시드는 `admin / 1234`. seed-data.js도 `admin/0000` 가정으로 실행 실패. → 시나리오 문서 갱신 필요.
4. **preview MCP 미지원**: 이 환경에서는 preview MCP 도구가 전혀 없어 반응형(S16-C), CBT 이탈감지(S11) 등 브라우저 DOM 기반 검증이 불가. UI 검증이 필요한 시나리오는 preview 환경에서 별도 실행 권고.

---

## 다음 액션 추천 (PM용)

### 긴급 (P1 — 금주 내 처리)

| 버그 | 담당 | 파일 |
|------|------|------|
| BUG-09: 포트폴리오 권한 누락 | Backend | routes/growth.js |
| BUG-11: 오답노트 미적재 | Backend | routes/self-learn.js |
| BUG-13: 알림 미생성 | Backend | routes/homework.js, routes/exam.js, routes/notice.js |
| BUG-14: 시험 클래스 격리 누락 | Backend | routes/exam.js |
| BUG-15: 학년별 격리 실패 (재발 ⚠️) | Backend | routes/self-learn.js |
| BUG-06: class-stats totalMembers 오류 | Backend | routes/attendance.js |

### 일반 (P2 — 다음 스프린트)

| 버그 | 담당 | 파일 |
|------|------|------|
| BUG-02: grade/subject 미저장 | Backend | routes/class.js |
| BUG-04: rejection_reason 컬럼 | Backend | db/schema.js |
| BUG-07: class-stats streat/badges | Backend | routes/attendance.js |
| BUG-10: 배포 과목 확장 | Backend | routes/admin.js |
| BUG-12: start-supervision | Backend | routes/exam.js |

---

## 자가검증 결과

```bash
# 실행 명령
git status --porcelain | grep -vE "보고서/일일점검"
# 결과: (출력 없음)
```

- **보고서/일일점검 이외 경로 변경 없음** ✅
- 소스 코드·설정·시나리오 문서 수정 전혀 없음 ✅
- node_modules는 .gitignore 처리로 git status에 미노출 ✅

---

## 시나리오 개선 제안

1. **0.3절 admin 비밀번호 수정**: `admin / 0000` → `admin / 1234` (실제 시드와 불일치)
2. **0.2절 서버 기동**: CCR 환경에서 preview MCP 불가 시 `node server.js` 허용 조건 명시 (현재는 이미 명시됨)
3. **S07 배포 기대값 수정**: 현재 구현은 수학 과목만 배포 → 기대값을 "8개(학년별 1과목)" 또는 BUG-10 해결 후 "40개"로 명확화
4. **S08 wrong-notes 엔드포인트**: `/api/self-learn/wrong-notes`는 목록 응답이 `{items:[]}` 형식, `/api/self-learn/wrong-answers`는 404 → 시나리오의 `GET /api/self-learn/wrong-notes` 사용이 올바른 경로
5. **S11 start-supervision**: Socket.IO 기반이면 REST API 대신 Socket 이벤트 방식으로 시나리오 갱신 권고
6. **S12 쪽지방 API**: `POST /api/message`가 아닌 `POST /api/message/rooms` + `POST /api/message/rooms/:id/messages` 2단계임을 명시
7. **S15 PDF 필수 파라미터**: `from`, `to`, `schoolLevel` 필수임을 시나리오에 명시 (현재는 `period:"month"`만 명시되어 400 발생)
