# 다채움 일일 점검 보고서 — 2026-07-01

- **실행 시각**: 2026-07-01 09:33 ~ 09:47 KST (총 약 14분)
- **에이전트**: claude-sonnet-4-6
- **서버 빌드**: b50f8914094dccb841f8ff6a79a3ad4adfe5d91d (main)
- **결과 요약**: PASS 9 / FAIL 3 / SKIP 1 / BLOCKED 3
- **발견 버그**: P0 0 / P1 3 / P2 3 / P3 0

> **⚠ Preview MCP 미사용**: CCR 원격 환경에서 `mcp__Claude_Preview__*` 도구 불가. HTTP + DB 폴백 방식으로 검증 수행. UI·반응형·접근성 스크린샷 항목은 BLOCKED 처리.

---

## 시나리오 결과 표

| ID | 제목 | 결과 | 소요(분) | 비고 |
|----|------|------|---------|------|
| S01 | 인증·세션·역할별 GNB | PASS | 2 | admin 비밀번호 불일치 P1 |
| S02 | 채움클래스 풀 사이클 | PASS | 6 | notice /read API 없음, GET으로 읽음 처리 |
| S03 | 일반게시판·갤러리 게시판 | PASS | 1 | 갤러리 pending 미노출 정상 |
| S04 | 갤러리 5단계 승인·반려 | PASS | 1 | 승인/반려 정상, rejection_reason 저장 |
| S05 | 출석부 + 감정출석부 + 게이미피케이션 | FAIL | 2 | /today API 0건 버그, 회고 text→content 불일치 |
| S06 | 학생 포트폴리오 | PASS | 1 | 데이터 격리(403) 정상 |
| S07 | 관리자 오늘의 학습 배포 | FAIL | 1 | approved video/quiz 콘텐츠 없어 totalCreated=0 |
| S08 | 학생 오늘의 학습 이수 → 오답노트 | SKIP | 0 | S07 미완료로 의존 SKIP |
| S09 | 마음채움 감정출석 → 교사 리포트 | PASS | 1 | S05 출석 재사용, 리포트·피드백 정상 |
| S10 | 채움콘텐츠 등록×승인·반려 | PASS | 2 | 5종 등록, 4승인+1반려 정상 |
| S11 | 채움CBT 평가→결과산출 | PASS | 1 | /results-detail 엔드포인트 사용 |
| S12 | 소통쪽지·알림 | FAIL | 1 | notifications 0건 (트리거 미연결) |
| S13 | AI 맞춤학습·내 문제집 | BLOCKED | 1 | learning-map/diagnose 라우트 없음 |
| S14 | LRS 학습분석 6대 영역 | PASS | 1 | 대시보드 41건, 실데이터 정상 |
| S15 | 성장기록 리포트·PDF | PASS | 1 | PDF 46KB, %PDF-1.3 시그니처 확인 |
| S16 | 데이터격리·인코딩·반응형·접근성 | BLOCKED | 1 | (A)(B) PASS, (C)(D) Preview MCP 필요 |

---

## 발견 버그

### BUG-20260701-01

- **우선순위**: P1 High
- **시나리오**: S01
- **모듈**: 인증
- **제목**: admin 기본 비밀번호 시나리오 문서(0000)↔실제(1234) 불일치
- **재현 단계**:
  1. admin 계정으로 `/api/auth/login` POST
  2. 비밀번호 `0000` 사용
  3. 응답 확인
- **기대 결과**: `{"success":true}` (시나리오 문서 0.3 기준 `admin/0000`)
- **실제 결과**: `{"success":false,"message":"비밀번호가 올바르지 않습니다."}`; 실제 비밀번호는 `1234`
- **첨부**:
  - HTTP 응답: `{"success":false,"message":"비밀번호가 올바르지 않습니다."}`
- **추정 원인**: `db/schema.js` seedAdmin() 에서 비밀번호를 `1234`로 생성하나 시나리오 문서는 `0000`으로 기술
- **영향 범위**: 시나리오 문서를 따르는 모든 자동 점검, 온보딩 가이드
- **권장 처리**: `db/schema.js` seedAdmin() 비밀번호 확인 후 시나리오 문서 0.3절 수정 (문서 수정 또는 코드 통일)

---

### BUG-20260701-02

- **우선순위**: P2 Medium
- **시나리오**: S05
- **모듈**: 채움클래스 / 출석부
- **제목**: `/api/attendance/:classId/today` 출석 0건 반환 (DB는 10건)
- **재현 단계**:
  1. student1~10이 `POST /api/attendance/2/checkin` 으로 출석 완료
  2. teacher1으로 `GET /api/attendance/2/today` 호출
  3. 응답 내 attendance 배열 확인
- **기대 결과**: `attendance.length = 10`
- **실제 결과**: `attendance.length = 0` (DB에는 10건 정상 저장됨)
- **첨부**:
  - HTTP 응답: `{"success":true,"...","attendance":[]}` (출력 배열 비어있음)
  - DB 확인: `SELECT count(*) FROM attendance WHERE class_id=2 AND attendance_date=DATE('now')` → 10
- **추정 원인**: `routes/attendance.js` 또는 `db/attendance.js` `/today` 핸들러의 쿼리가 UTC 기준 날짜로 조회하여 KST 자정 이후 기록을 누락하거나, `date` 컬럼명 대신 `attendance_date` 컬럼명을 사용하는 불일치
- **영향 범위**: 교사의 실시간 출석 현황 조회 기능 전체
- **권장 처리**: `routes/attendance.js` `/today` 핸들러의 날짜 조회 조건 컬럼명(`date` → `attendance_date`) 및 시간대(KST) 확인

---

### BUG-20260701-03

- **우선순위**: P2 Medium
- **시나리오**: S05
- **모듈**: 채움클래스 / 출석부
- **제목**: 감정 회고 API `text` 필드명 불일치 → 400 오류
- **재현 단계**:
  1. student1으로 `POST /api/attendance/2/emotion/reflections`
  2. body: `{"period":"weekly","text":"이번 주는 즐거웠어요"}`
- **기대 결과**: `{"success":true}`
- **실제 결과**: `{"success":false,"message":"회고 내용을 입력해 주세요."}`
- **첨부**:
  - HTTP 응답: `{"success":false,"message":"회고 내용을 입력해 주세요."}`
- **추정 원인**: 시나리오 문서는 `text` 필드 사용, 실제 API는 다른 필드명 요구 (예: `content`)
- **영향 범위**: 학생 감정 회고 기능
- **권장 처리**: `routes/attendance.js` 감정 회고 핸들러 파라미터 확인 및 시나리오 문서 수정

---

### BUG-20260701-04

- **우선순위**: P1 High
- **시나리오**: S07
- **모듈**: 스스로채움 / 관리자
- **제목**: 오늘의 학습 일괄 배포 시 totalCreated=0 (콘텐츠 풀 없음)
- **재현 단계**:
  1. admin으로 `POST /api/admin/daily-learning/bulk-distribute`
  2. body: `{"startDate":"2026-07-01","endDate":"2026-07-01","scope":"all"}`
- **기대 결과**: 8학년 × 5과목 = 40개 이상 배포 set 생성
- **실제 결과**: `{"totalCreated":0,"totalSkipped":0}` — 승인된 video/quiz 콘텐츠 없어 배포 불가
- **첨부**:
  - HTTP 응답: `{"success":true,"summary":{"초3":0,...,"고1":0},"totalCreated":0,"totalSkipped":0}`
  - DB 확인: `SELECT content_type, status FROM contents WHERE status='approved'` → video/quiz 없음
- **추정 원인**: 콘텐츠 시드 데이터 없음. `seed-self-learn-content.js` 또는 `seed-quiz-items.js` 미실행 상태
- **영향 범위**: 학생 오늘의 학습 기능 전체 (S08 의존 SKIP)
- **권장 처리**: `node scripts/seed-self-learn-content.js` 또는 `node seed-quiz-items.js` 실행하여 콘텐츠 풀 시딩. 배포 API 선행 의존성 명시

---

### BUG-20260701-05

- **우선순위**: P1 High
- **시나리오**: S12
- **모듈**: 소통쪽지 / 알림
- **제목**: 활동 후 notifications 0건 반환 (알림 트리거 미연결)
- **재현 단계**:
  1. S02~S11 모든 활동 수행 (과제 배포, 채점, 시험, 쪽지 등)
  2. student1으로 `GET /api/notifications` 호출
- **기대 결과**: `homework_graded`, `message_received` 등 다수 알림 반환
- **실제 결과**: `{"notifications":[],"total":0}` (알림 0건)
- **첨부**:
  - HTTP 응답: `{"success":true,"notifications":[]}`
  - DB 확인: notifications 테이블 내용 확인 필요
- **추정 원인**: 과제 채점, 쪽지 발송 등 이벤트 발생 시 notifications 테이블 INSERT 로직이 연결되지 않거나 비활성화됨
- **영향 범위**: 모든 사용자의 알림 수신 기능
- **권장 처리**: `routes/homework.js` 채점 핸들러, `routes/message.js` 메시지 핸들러에서 notifications INSERT 코드 확인

---

### BUG-20260701-06

- **우선순위**: P2 Medium
- **시나리오**: S13
- **모듈**: 스스로채움 / AI맞춤학습
- **제목**: `/api/self-learn/learning-map`, `/api/self-learn/diagnose` 라우트 없음 (404)
- **재현 단계**:
  1. student1으로 `GET /api/self-learn/learning-map` 호출
  2. student1으로 `GET /api/self-learn/diagnose` 호출
- **기대 결과**: 학습맵 / 진단 결과 반환
- **실제 결과**: `{"success":false,"message":"요청하신 API를 찾을 수 없습니다."}`
- **추정 원인**: 시나리오 문서에 명시된 라우트가 `routes/learning.js` 또는 `routes/self-learn.js`에 등록되지 않음
- **영향 범위**: AI 맞춤학습 진단 기능 전체
- **권장 처리**: `server.js` 라우트 등록 확인 및 누락 엔드포인트 구현 또는 시나리오 문서 URL 수정

---

## 어제 대비 차이

직전 일 보고서 없음 (첫 실행)

---

## 관찰 메모

- **쪽지 API 설계 차이**: 시나리오 문서는 `POST /api/message`(단일 메시지 직접 발송)를 기술하나 실제 구현은 방(room) 생성 후 메시지 전송하는 2단계 방식. 시나리오 문서 업데이트 필요.
- **알림장(notice) 읽음 API**: 시나리오 문서는 `POST /api/notice/:classId/:noticeId/read`를 기술하나, 실제 구현은 `GET /api/notice/:classId/:noticeId` (상세 조회 시 자동 읽음 처리). 둘 다 정상 동작 결과는 같으나 문서와 다름.
- **exam 결과 엔드포인트**: `/results` 대신 `/results-detail` 사용. `/results` 라우트 없음.
- **LRS 활성**: 이번 점검 활동(41건)이 모두 LRS에 정상 기록됨. 데이터 파이프라인 양호.
- **PDF 리포트**: 46KB, %PDF-1.3 시그니처 정상. Puppeteer 기반 PDF 생성 정상.

---

## 다음 액션 추천 (PM용)

### 즉시 처리 (P1)

| 항목 | 담당 | 설명 |
|------|------|------|
| 콘텐츠 시드 미설치 (BUG-04) | Backend | `node seed-quiz-items.js` 또는 `node seed-self-learn-content.js` 실행. 오늘의 학습 전체 기능 블록 |
| notifications 트리거 미연결 (BUG-05) | Backend | 채점/쪽지/시험 이벤트 → notifications 테이블 INSERT 로직 확인 |
| admin 비밀번호 문서 불일치 (BUG-01) | PM/Backend | 시나리오 문서 0.3절 수정 또는 seedAdmin 비밀번호 통일 |

### 다음 스프린트 (P2)

| 항목 | 담당 | 설명 |
|------|------|------|
| /today 출석 조회 0건 (BUG-02) | Backend | attendance.js today 쿼리 컬럼명·시간대 수정 |
| 감정 회고 필드명 불일치 (BUG-03) | Backend/PM | text→content 통일 또는 문서 수정 |
| AI맞춤학습 라우트 404 (BUG-06) | Backend | learning-map, diagnose 엔드포인트 등록 확인 |

---

## 환경 메타

| 항목 | 값 |
|------|---|
| 점검 일시 (KST) | 2026-07-01 09:33 ~ 09:47 |
| git branch | main |
| git HEAD | b50f8914094dccb841f8ff6a79a3ad4adfe5d91d |
| Node.js | v22.22.2 |
| npm ci 소요 | 약 5초 (274 packages) |
| DB 경로 | data/dacheum.db (1,695,744 bytes) |
| DB 시드 사용자 | 19명 (admin 1, teacher 3, student 10+1qa_temp, parent 1, staff 1, principal 2) |
| Preview 서버 | node server.js 백그라운드 (포트 3000) |
| Preview MCP | **미사용** (CCR 환경 미지원) — HTTP + DB 폴백 |
| 시나리오 문서 라인 수 | 699줄 |

---

## 자가검증 결과

```
git status --porcelain 실행 결과:
(변경 없음 — 보고서/일일점검/ 경로만 추가 예정)
```

- **화이트리스트 외 파일 수정**: 0건 ✓
- **소스코드 변경**: 없음 ✓
- **보고서 경로**: `보고서/일일점검/2026-07-01_점검보고서.md` ✓

---

## 시나리오 개선 제안

1. **0.3 시드 보강 절차**: admin 비밀번호를 `1234`로 통일 (서버 실제값 반영). `0000` → `1234`
2. **S02 알림장 읽음 절차**: `POST /read` 대신 `GET /:noticeId` (상세 조회)로 수정
3. **S12 쪽지 API**: `/api/message` → `/api/message/rooms` (방 생성) + `/rooms/:roomId/messages` (메시지 전송) 2단계로 수정
4. **S07 선행조건 추가**: "콘텐츠 시드(video/quiz 승인된 콘텐츠) 최소 1건 이상 존재"를 선행조건으로 명시
5. **S11 결과 엔드포인트**: `/results` → `/results-detail`로 수정
6. **DB 경로**: 시나리오 문서 0.3 및 0.4절의 DB 경로를 `db/dachaeum.db` → `data/dacheum.db`로 수정 (실제 서버 사용 경로)
7. **Preview MCP 폴백 절차**: CCR 원격 환경에서 Preview MCP 미사용 시 HTTP + DB 검증 방식으로 대체하는 절차를 시나리오 문서 0.4절에 추가
