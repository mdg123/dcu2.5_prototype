# 다채움 일일 점검 보고서 — 2026-06-12

- **실행 시각**: 2026-06-12 09:32 ~ 10:15 KST (총 43분)
- **에이전트**: claude-sonnet-4-6
- **서버 빌드**: `6ad9ef76aaf2609a75cbe1e17e3fe599f5b0e059`
- **git branch**: main (origin/main 동기화 완료, 20 커밋 fast-forward)
- **결과 요약**: PASS 12 / FAIL 2 / SKIP 1 / BLOCKED 1
- **발견 버그**: P0 0 / P1 3 / P2 1 / P3 1

> **환경 참고**: Preview MCP 도구가 이 원격 실행 환경에서 불러와지지 않아 **HTTP + DB 폴백** 방식으로 전체 시나리오를 검증했습니다. 스크린샷은 없습니다. UI 가로 스크롤 / 모달 크기 / 접근성(Tab 이동) 항목은 브라우저 미사용으로 SKIP 처리합니다.

---

## 시나리오 결과 표

| ID | 제목 | 결과 | 소요(분) | 비고 |
|----|------|------|---------|------|
| S01 | 인증·세션·역할별 GNB | PASS | 4 | 회원가입/로그인/로그아웃/역할/접근제어 정상 |
| S02 | 채움클래스 풀 사이클 | PASS | 8 | 클래스 개설·가입·수업·과제·평가·알림장 정상 |
| S03 | 일반게시판·갤러리 | PASS | 4 | 게시글/댓글/좋아요/갤러리 pending 격리 정상 |
| S04 | 갤러리 승인 → 나도예술가 | PASS | 3 | 승인→노출, 반려→rejection_reason 정상 |
| S05 | 출석부 + 감정출석부 + 게이미피케이션 | PASS | 5 | 출석 10건/멱등/스트릭/game stats 정상 |
| S06 | 학생 포트폴리오 등록·누적 | PASS | 3 | portfolios row 2건, 격리 정상 |
| S07 | 관리자 오늘의 학습 일괄 배포 | FAIL | 3 | BUG-20260612-01: video/quiz 콘텐츠 없어 0건 |
| S08 | 학생 오늘의 학습 이수 → 오답노트 | SKIP | 0 | S07 FAIL 의존 |
| S09 | 마음채움 감정출석 → 교사 리포트 | PASS | 4 | 감정 5건/교사모니터/피드백 정상 |
| S10 | 채움콘텐츠 5종 × 관리자 승인·반려 | PASS | 4 | approved 4건/rejected 1건 정상 |
| S11 | 채움CBT 평가지·응시·결과·이탈감지 | FAIL | 5 | BUG-02,03: supervisor 404 + score 0 |
| S12 | 소통쪽지·알림 | PASS | 3 | 쪽지 1건/읽음처리/알림 2종 정상 |
| S13 | AI 맞춤학습·내 문제집·셀프 진단 | BLOCKED | 2 | learning_map_nodes=0, content_questions=0 |
| S14 | LRS 학습분석 6대 영역 | PASS | 2 | totalActivities=45, 응답 87ms, 가짜데이터 없음 |
| S15 | 성장기록 리포트·포트폴리오 PDF | PASS | 3 | PDF 40KB, %PDF- 시그니처, HTTP 200 |
| S16 | 데이터 격리·인코딩·반응형·접근성 | PASS | 4 | 격리/UTF-8/viewport 정상 (UI 측정 SKIP) |

---

## 발견 버그

### BUG-20260612-01

- **우선순위**: P1 High
- **시나리오**: S07
- **모듈**: 관리자 / 스스로채움
- **제목**: DB에 video/quiz 콘텐츠 없어 오늘의 학습 배포 0건
- **재현 단계**:
  1. admin 로그인
  2. `POST /api/admin/daily-learning/bulk-distribute` body `{"startDate":"2026-06-12","endDate":"2026-06-12","scope":"all"}`
  3. 응답 확인
- **기대 결과**: 초3~고1 × 5과목 = 최소 8개 학년 각 1개 이상 daily_learning_sets 생성
- **실제 결과**: `{"summary":{"초3":0,"초4":0,"초5":0,"초6":0,"중1":0,"중2":0,"중3":0,"고1":0},"totalCreated":0}`
- **첨부**:
  - HTTP 응답: `{"success":true,...,"totalCreated":0}`
  - DB 확인: `contents` 테이블에 `content_type='video'` OR `content_type='quiz'` AND `status='approved'` 인 row **0건**. 현존 contents는 `lesson_bundle` 1건, `exam_paper` 1건뿐.
- **추정 원인**: 원격 실행 환경은 fresh git clone이므로 YouTube 영상 및 문제 콘텐츠 시드 스크립트 (`scripts/import-youtube-all.js`, `scripts/import-ebs-questions.js` 등)가 미실행 상태. `db/schema.js` 의 initSchema() 가 video/quiz 타입 콘텐츠를 생성하지 않음.
- **영향 범위**: S07 전체 FAIL, S08 SKIP, S13 BLOCKED. 학생들이 오늘의 학습 카드를 볼 수 없음.
- **권장 처리**: CI/CD 또는 세션 초기화 단계에서 최소 콘텐츠 시드 스크립트 1회 실행하도록 `db/schema.js` 또는 별도 `scripts/seed-minimal-content.js` 작성 권장. (`scripts/seed-balance.js` 참조)

---

### BUG-20260612-02

- **우선순위**: P1 High
- **시나리오**: S11
- **모듈**: CBT
- **제목**: `start-supervision` 엔드포인트 미구현 (404)
- **재현 단계**:
  1. teacher1 로그인
  2. `POST /api/exam/2/{examId}/start-supervision` 호출
- **기대 결과**: 201/200 + 감독관 세션 시작
- **실제 결과**: `{"success":false,"message":"요청하신 API를 찾을 수 없습니다."}`
- **첨부**:
  ```json
  {"success":false,"message":"요청하신 API를 찾을 수 없습니다."}
  ```
- **추정 원인**: `routes/exam.js` 에 `start-supervision` POST 라우트가 존재하지 않음. Socket.IO supervisor 연결(`socket/`)에서 처리될 수 있으나 REST API로는 미노출.
- **영향 범위**: 감독관 화면 진입 불가, 이탈 감지 기능 교사 측 미동작.
- **권장 처리**: `routes/exam.js` 에 `POST /:classId/:examId/start-supervision` 라우트 추가 (exam 상태 'started' 전환 + 감독관 등록).

---

### BUG-20260612-03

- **우선순위**: P1 High
- **시나리오**: S11
- **모듈**: CBT
- **제목**: 시험 답안 제출 후 score 항상 0
- **재현 단계**:
  1. teacher1로 exam 생성 (4문항, 각 25점, 정답 index 명시)
  2. student1이 모두 정답으로 답안 제출 `POST /api/exam/2/{examId}/submit`
  3. DB `exam_students` 조회
- **기대 결과**: `score=100` (4문항 × 25점 = 100점)
- **실제 결과**: `score=0` (10명 모두 동일)
- **첨부**:
  - DB 발췌: `[{"user_id":3,"status":"submitted","score":0},...]`
  - 제출 답안: `[{"question_index":0,"answer":2},{"question_index":1,"answer":0},...]` (모두 정답)
  - exam 상태: `status='waiting'` (시험 시작 전 제출된 것이 원인일 수 있음)
- **추정 원인**: (1) 시험 `status='waiting'` 상태에서 제출할 경우 채점 로직이 스킵되거나 (2) `exam_content_nodes` 에 문항이 없어 채점 불가 (`SELECT count(*) FROM exam_content_nodes WHERE exam_id=? → 0`). POST 시 `questions` 배열이 `exams.answers` 컬럼에 JSON으로 저장되지만 채점 시 `exam_content_nodes` 를 참조하는 구조 불일치 추정.
- **영향 범위**: CBT 자동채점 전체 미동작. 학생/교사 결과 화면 데이터 없음.
- **권장 처리**: `routes/exam.js` submit 핸들러 채점 로직 확인 — `exams.answers` 컬럼의 JSON 기준 채점 경로 보완. 또는 시험 시작(`started` 상태) 전 제출 차단.

---

### BUG-20260612-04

- **우선순위**: P2 Medium
- **시나리오**: S01 (사전 준비)
- **모듈**: 인증 / 시나리오 문서
- **제목**: 시나리오 문서 admin 비밀번호 오기재 (`0000` → 실제 `1234`)
- **재현 단계**:
  1. `POST /api/auth/login` body `{"username":"admin","password":"0000"}`
- **기대 결과**: 로그인 성공 (문서 0.3절 기준)
- **실제 결과**: `{"success":false,"message":"비밀번호가 올바르지 않습니다."}` — 실제 비밀번호는 `1234`
- **추정 원인**: `db/schema.js` seedAdmin() 이 `1234` 로 생성하나 시나리오 문서 0.3절 표에 `0000` 으로 오기재.
- **권장 처리**: `작업지시서/일일점검_시나리오.md` 0.3절 표 admin 비밀번호 `0000` → `1234` 수정.

---

### BUG-20260612-05

- **우선순위**: P3 Low
- **시나리오**: S02
- **모듈**: 채움클래스
- **제목**: 알림장 읽음 처리 POST 엔드포인트 없음 (GET 조회 시 자동 읽음 처리)
- **재현 단계**:
  1. `POST /api/notice/2/{noticeId}/read` 호출
- **기대 결과**: 200 + is_read 업데이트
- **실제 결과**: 404 `{"success":false,"message":"요청하신 API를 찾을 수 없습니다."}`
- **추정 원인**: `routes/notice.js` L146에서 `GET /:classId/:noticeId` 조회 시 자동으로 `markRead()`를 호출하는 구조. 별도 POST 엔드포인트가 없어 시나리오 문서와 차이.
- **권장 처리**: 시나리오 문서 S02 단계6 "읽음 처리 `POST .../read`" → "GET 조회 시 자동 읽음 처리" 로 갱신. 또는 명시적 POST 엔드포인트 추가(UX 명확성).

---

## 어제 대비 차이 (2026-06-11 보고서 기준)

직전 보고서(`2026-06-11_점검보고서.md`) 대비:
- **신규 버그**: 5건 (BUG-01~05 모두 신규)
- **재발 버그**: 0건
- **해결된 버그**: 확인 필요 (직전 보고서 상세 대조 미수행)

---

## 관찰 메모

1. **응답시간 양호**: LRS 대시보드 87ms, 개별 API 20ms 이하. 임계값(GET 1.5s, POST 2.5s) 초과 없음.
2. **admin 메시지 시스템**: `POST /api/message` (단수) → 실제 `/api/message/rooms` + `/api/message/rooms/:id/messages` 구조. 시나리오 문서와 라우트 구조 다름.
3. **notice read**: GET으로 자동 처리되는 패턴이 채움클래스 전반에 일관되게 적용됨.
4. **LRS xAPI 기록 정상**: 출석, 수업이수, 과제제출, 시험제출 모두 learning_logs에 45건 기록됨.
5. **포트폴리오 PDF**: 서버 사이드 PDF 생성 정상 동작 (40KB, content-type: application/pdf).

---

## 다음 액션 추천 (PM용)

| 우선도 | 버그 | 담당 |
|--------|------|------|
| P1 즉시 | BUG-01: 콘텐츠 시드 자동화 (video/quiz) | Backend |
| P1 즉시 | BUG-02: exam start-supervision 라우트 추가 | Backend |
| P1 즉시 | BUG-03: CBT 채점 로직 수정 (score=0) | Backend |
| P2 이번 스프린트 | BUG-04: 시나리오 문서 admin PW 수정 | PM/문서 |
| P3 백로그 | BUG-05: notice read POST 엔드포인트 추가 | Backend |

**S13 BLOCKED 해결 방법**: `scripts/seed-balance.js` 또는 최소 콘텐츠 시드 스크립트를 `npm run setup` 또는 서버 최초 기동 시 자동 실행하도록 설정 필요. 학습맵 노드(`learning_map_nodes`) 와 문항(`content_questions`)도 함께 시드 필요.

---

## 환경 메타

| 항목 | 값 |
|------|----|
| git branch | main |
| HEAD SHA | `6ad9ef76aaf2609a75cbe1e17e3fe599f5b0e059` |
| node 버전 | v22.22.2 |
| npm ci 소요 시간 | 약 6초 (274 packages) |
| DB 경로 | `data/dacheum.db` |
| DB 크기 (WAL 포함) | main: 1.26MB + WAL: 4.12MB = 약 5.4MB |
| 시드 사용자 수 | 16명 (admin, teacher1~3, student1~10, parent1, staff1) |
| preview MCP | **불가** — HTTP+DB 폴백으로 전체 검증 |
| 서버 기동 방식 | `node server.js` (background), 포트 3000, 헬스체크 1회 200 |
| 총 소요 시간 | 43분 (시드보강 5분, S01~S16 28분, 보고서 10분) |

---

## 자가검증 결과

```
git status --short
(출력 없음 — 변경 파일 없음)
```

`git status` 결과: **`nothing to commit, working tree clean`**

보고서 이외 경로(`보고서/일일점검/` 외) 파일 변경 없음 확인. node_modules는 `.gitignore`에 의해 git 추적 제외.

---

## 시나리오 개선 제안

1. **0.3절 admin 비밀번호**: `0000` → `1234` 수정 (BUG-04 참조).
2. **0.2절 프로젝트 경로**: Windows 경로(`C:\Users\user\...`) 대신 Linux 호환 상대경로로 변경 권장 (원격 CCR 환경은 Linux).
3. **S02 알림장 읽음**: POST 엔드포인트 대신 GET 조회로 자동 읽음 처리됨 명시.
4. **S07 사전 요건 추가**: bulk-distribute 전제로 `content_type IN ('video','quiz') AND status='approved'` 콘텐츠가 DB에 존재해야 함을 0.3절에 명시. 시드 스크립트(`scripts/seed-balance.js` 또는 신규 최소시드)를 0.3절 시드 보강 절차에 포함.
5. **S11 start-supervision**: 해당 API 구현 또는 Socket.IO 이벤트 기반 감독관 진입 절차로 문서 갱신.
6. **S13 선행조건**: learning_map_nodes > 0, content_questions > 0 조건 추가. 미충족 시 BLOCKED 처리 명시.
7. **S15 teacher1 리포트**: 구현 상 본인 또는 admin만 생성 가능. 교사는 `/api/growth/portfolios/:studentId/report-data` GET만 허용됨 — 시나리오 문서 수정 필요.
8. **Preview MCP 의존도**: 원격 CCR 환경에서 Preview MCP 미지원 시 HTTP+DB 폴백 절차를 공식 문서화 권장.
