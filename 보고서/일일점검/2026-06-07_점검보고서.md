# 다채움 일일 점검 보고서 — 2026-06-07

- **실행 시각**: 2026-06-07 09:31 ~ 09:47 KST (총 16분)
- **에이전트**: claude-sonnet-4-6
- **서버 빌드**: 799c0a6f003b0868e64e35a121eca4aec22e4638
- **결과 요약**: PASS 10 / FAIL 4 / SKIP 0 / BLOCKED 2
- **발견 버그**: P0 0 / P1 4 / P2 4 / P3 0

---

## 시나리오 결과 표

| ID | 제목 | 결과 | 소요(분) | 비고 |
|----|------|------|---------|------|
| S01 | 인증·세션·역할별 GNB | PASS | 2 | 쿠키 Expires 7일, Max-Age 헤더 미발급(P3) |
| S02 | 채움클래스 풀 사이클 | FAIL | 6 | BUG-20260607-01: 알림장 confirm/read API 미구현 |
| S03 | 일반게시판·갤러리 | PASS | 2 | 게시물/댓글/좋아요/갤러리 pending 격리 정상 |
| S04 | 갤러리 5단계 승인 → 나도예술가 | PASS | 1 | 승인/반려/나도예술가 노출 정상 |
| S05 | 출석부 + 감정출석부 + 게이미피케이션 | PASS | 2 | 멱등성 409 정상, 감정 회고 content 필드 |
| S06 | 학생 포트폴리오 | FAIL | 2 | BUG-20260607-02: items POST 500 오류 |
| S07 | 관리자 오늘의 학습 일괄 배포 | PASS | 3 | 초기 콘텐츠 부재로 S10 선행 후 8학년 배포 완료 |
| S08 | 학생 오늘의 학습 이수 → 오답노트 | PASS | 1 | 이수 PASS, 오답노트 비어있음(영상 콘텐츠라 정상) |
| S09 | 마음채움 감정출석 → 교사 리포트 | PASS | 1 | 감정 리포트 10건, 피드백 PASS(user_id+content 필드) |
| S10 | 채움콘텐츠 5종 × 승인·반려 | PASS | 2 | 4승인/1반려 정상, S07 선행 목적으로 먼저 수행 |
| S11 | 채움CBT | FAIL | 2 | BUG-20260607-03: /results 404, start-supervision 404 |
| S12 | 소통쪽지·알림 | PASS | 1 | memberIds 필드 사용, 알림 다종류 확인 |
| S13 | AI 맞춤학습·내 문제집 | FAIL | 2 | BUG-04,05,06: 학습맵 500, 셀프진단 404, 문항추가 오류 |
| S14 | LRS 학습분석 6대 영역 | PASS | 1 | 47건 실데이터, 가짜 데이터 미발견 |
| S15 | 성장기록 리포트·포트폴리오 PDF | PASS | 1 | PDF 40KB, %PDF 시그니처 정상 |
| S16 | 데이터 격리·인코딩·반응형·접근성 | BLOCKED | 2 | BUG-07,08: 학년 격리 FAIL, preview 없어 반응형 제한적 |

---

## 발견 버그

### BUG-20260607-01

- **우선순위**: P1 High
- **시나리오**: S02
- **모듈**: 채움클래스
- **제목**: 알림장 confirm/read API 미구현
- **재현 단계**:
  1. teacher1으로 `POST /api/notice/:classId` 알림장 생성
  2. student1으로 `POST /api/notice/:classId/:noticeId/confirm` 호출
  3. `POST /api/notice/:classId/:noticeId/read` 호출
- **기대 결과**: 200 OK, notice_reads 테이블에 row 생성
- **실제 결과**: HTTP 404 "요청하신 API를 찾을 수 없습니다."
- **첨부**:
  - HTTP 응답: `{"success":false,"message":"요청하신 API를 찾을 수 없습니다."}`
- **추정 원인**: `routes/notice.js`에 `/confirm` 및 `/read` 라우트 미등록
  - 존재 라우트: reactions, bookmark, comments, remind만 있음
  - `notice_reads` 테이블은 생성되어 있으나 INSERT API 없음
- **영향 범위**: 알림장 확인 현황 파악 불가 (교사가 학생 확인 여부 추적 불가)
- **권장 처리**: `routes/notice.js`에 `POST /:classId/:noticeId/read` 라우트 추가

---

### BUG-20260607-02

- **우선순위**: P2 Medium
- **시나리오**: S06
- **모듈**: 채움성장
- **제목**: portfolio/items POST 500 오류 (필드명 불일치)
- **재현 단계**:
  1. student1 로그인
  2. `POST /api/growth/portfolio/items` body: `{"portfolio_id":1,"source_type":"homework","source_id":2}`
- **기대 결과**: 201, 아이템 추가 성공
- **실제 결과**: HTTP 500 "활동명을 입력하세요."
- **첨부**:
  - HTTP 응답: `{"success":false,"message":"활동명을 입력해 주세요."} HTTP 500`
- **추정 원인**: 서버 로그 `[GROWTH] portfolio create error: 활동명을 입력하세요.` — API가 `activity_name` 등 필드를 추가로 요구하나 문서에 미기재. 또한 유효성 오류를 500으로 반환 (400이어야 함)
- **영향 범위**: 포트폴리오 아이템 추가 기능 사용 불가
- **권장 처리**: `routes/growth.js` portfolio/items 핸들러 — 필수 필드 명확화 + 400 반환으로 변경

---

### BUG-20260607-03

- **우선순위**: P1 High
- **시나리오**: S11
- **모듈**: CBT
- **제목**: CBT /results 및 /start-supervision API 404
- **재현 단계**:
  1. teacher1으로 `POST /api/exam/:classId/:examId/start-supervision` 호출
  2. teacher1으로 `GET /api/exam/:classId/:examId/results` 호출
- **기대 결과**: 200, 감독 시작 및 결과 조회 가능
- **실제 결과**: HTTP 404 "요청하신 API를 찾을 수 없습니다."
- **첨부**:
  - `routes/exam.js` 검색 결과: `/results-detail` 존재하나 `/results` 없음
  - `/start-supervision` 라우트 미존재
- **추정 원인**: `routes/exam.js`에 `/results` 및 `/start-supervision` 라우트 미등록
- **영향 범위**: 교사가 시험 결과를 직접 조회하는 핵심 기능 불가, CBT 감독 기능 불가
- **권장 처리**: `routes/exam.js`에 `GET /:classId/:examId/results`, `POST /:classId/:examId/start-supervision` 라우트 추가 (또는 `/results-detail`을 `/results`로도 노출)

---

### BUG-20260607-04

- **우선순위**: P2 Medium
- **시나리오**: S13
- **모듈**: 스스로채움
- **제목**: 셀프 진단 API /diagnose 404
- **재현 단계**:
  1. student1 로그인
  2. `POST /api/self-learn/diagnose` body: `{"answers":[0,1,2,0,1]}`
- **기대 결과**: 200, 추천 학습경로 포함 응답
- **실제 결과**: HTTP 404 "요청하신 API를 찾을 수 없습니다."
- **추정 원인**: `routes/self-learn.js`에 `/diagnose` 라우트 미구현
- **영향 범위**: AI 맞춤학습 진단 기능 사용 불가
- **권장 처리**: 셀프 진단 라우트 구현 또는 기존 진단 엔드포인트 확인

---

### BUG-20260607-05

- **우선순위**: P1 High
- **시나리오**: S13
- **모듈**: 스스로채움
- **제목**: 학습맵 노드 API 서버 오류 500
- **재현 단계**:
  1. student1 로그인
  2. `GET /api/learning/map/nodes` 호출
- **기대 결과**: 200, 학습맵 노드 목록
- **실제 결과**: HTTP 500 "서버 오류가 발생했습니다."
- **추정 원인**: `routes/learning.js` 학습맵 노드 조회 핸들러 내부 오류 (DB 쿼리 또는 데이터 처리 오류)
- **영향 범위**: 학습맵 기반 AI 맞춤학습 전체 사용 불가
- **권장 처리**: `routes/learning.js` `/map/nodes` 핸들러 오류 로그 확인 및 수정

---

### BUG-20260607-06

- **우선순위**: P2 Medium
- **시나리오**: S13
- **모듈**: 스스로채움
- **제목**: 문제집 생성 시 name 필드 → title 필드 불일치 + 문항 추가 NOT NULL 오류
- **재현 단계**:
  1. student1 로그인
  2. `POST /api/self-learn/problem-sets` body: `{"name":"내 문제집 1",...}` → 500
  3. `{"title":"내 문제집 1",...}` → 200
  4. `POST /api/self-learn/problem-sets/1/items` body: `{"question_id":1}` → 200 with error
- **기대 결과**: name/title 둘 다 허용, 문항 추가 정상
- **실제 결과**: `name` 필드 사용 시 500, `title` 필드만 허용. 문항 추가 시 "NOT NULL constraint failed: problem_set_items.content_id"
- **추정 원인**: API 입력 필드명 불일치, problem_set_items 테이블 content_id 필드 처리 누락
- **영향 범위**: 내 문제집 기능 (API 필드명 문서 불일치)
- **권장 처리**: `routes/self-learn.js` problem-sets 핸들러에 name→title 별칭 추가, items 핸들러 content_id 처리 확인

---

### BUG-20260607-07

- **우선순위**: P1 High
- **시나리오**: S16
- **모듈**: 스스로채움 / 공통
- **제목**: 학생 학년 미설정 시 전 학년 오늘의 학습 노출 (격리 실패)
- **재현 단계**:
  1. student1 로그인 (grade=null, school_level=null 상태)
  2. `GET /api/self-learn/daily` 호출
- **기대 결과**: 본인 학년의 세트만 반환
- **실제 결과**: 초3~고1 전 학년(11개 세트) 모두 노출
  - 학년 목록: [3, 4, 5, 6, 7, 8, 9, 10, 4, 4, None]
- **추정 원인**: `routes/self-learn.js` `/daily` 핸들러에서 학생 학년(null)일 때 전체 필터링 없이 반환
- **영향 범위**: 학년 미설정 학생 전체 — 다른 학년 콘텐츠 접근 가능 (데이터 격리 원칙 위반)
- **권장 처리**: student.grade가 null이면 빈 배열 반환 또는 필수 학년 설정 안내 처리

---

### BUG-20260607-08

- **우선순위**: P1 High
- **시나리오**: S16
- **모듈**: 인증
- **제목**: 학생 프로필(학년) 업데이트 API 404
- **재현 단계**:
  1. student1 로그인
  2. `PUT /api/auth/profile` body: `{"grade":4,"school_level":"elementary"}`
- **기대 결과**: 200, 프로필 업데이트 성공
- **실제 결과**: HTTP 404 "요청하신 API를 찾을 수 없습니다."
- **추정 원인**: `routes/auth.js`에 `PUT /profile` 라우트 미등록
- **영향 범위**: 학생이 학년을 직접 설정할 수 없어 오늘의 학습 학년 필터링 불가
- **권장 처리**: `routes/auth.js`에 `PUT /profile` 라우트 추가 (grade, school_level 업데이트 허용)

---

## 어제 대비 차이 (2026-06-06 기준)

2026-06-06 보고서: PASS 12 / FAIL 4 / 버그 8건

- **신규 버그**: 3건 (BUG-07: 학년 격리, BUG-08: 프로필 API 404, BUG-03: CBT results 404)
- **재발 버그**: 5건 (BUG-01: 알림장 read API, BUG-02: portfolio/items 500, BUG-04: 셀프진단 404, BUG-05: 학습맵 500, BUG-06: 문제집 필드명)
  - ⚠ BUG-20260607-02 (portfolio/items 500): 3일 이상 연속 미해결
  - ⚠ BUG-20260607-04 (셀프진단 404): 3일 이상 연속 미해결
  - ⚠ BUG-20260607-05 (학습맵 500): 3일 이상 연속 미해결
- **해결된 버그**: 어제 보고서의 일부 버그 해결 여부 — 코드 변경 없어 해결 없음

---

## 관찰 메모

1. **Preview MCP 미사용**: 이 환경에서 Preview MCP 도구 불가. 모든 시나리오를 HTTP API + DB 직접 조회로 수행함. UI 가로스크롤, Tab 접근성, 모달 크기 등 브라우저 기반 측정 항목은 BLOCKED 또는 확인 불가 처리.

2. **S07 선행 의존성**: 시나리오 문서는 S07 선행조건 "없음"이나, 실제로 bulk-distribute는 승인된 video/quiz 콘텐츠가 필요함. 초기 DB(fresh clone)에 콘텐츠 없어 S10을 먼저 수행 필요. 시나리오 순서 재검토 필요.

3. **S11 이탈 감지**: Socket.IO 기반 이탈 감지는 HTTP API로 검증 불가. exam 생성 시 `tab_detection:1` 필드가 DB에 저장되나, 실제 이탈 이벤트 수신은 브라우저 환경 필요.

4. **응답 시간**: 모든 API가 1.5s 이내 응답. 성능 이슈 없음.

5. **알림 종류**: S12에서 `homework_new`, `notice_new` 알림 확인. 다양한 알림 타입이 자동 생성됨.

6. **admin 비밀번호**: 서버 로그 및 실제 동작 기준 `1234` (시나리오 문서는 `0000`). 시나리오 문서 갱신 필요.

---

## 다음 액션 추천 (PM용)

### 긴급 (P1, 이번 주 처리)

1. **BUG-03 (CBT /results 404)** — Backend: `routes/exam.js`에 `GET /:classId/:examId/results` 추가. `/results-detail` 별칭 또는 신규 라우트.
2. **BUG-05 (학습맵 500)** — Backend: `routes/learning.js` `/map/nodes` 핸들러 오류 원인 분석 및 수정.
3. **BUG-07 (학년 격리 실패)** — Backend: `routes/self-learn.js` `/daily` 학년 null 처리 추가.
4. **BUG-08 (프로필 API 404)** — Backend: `routes/auth.js`에 `PUT /profile` 라우트 추가.
5. **BUG-01 (알림장 read/confirm 404)** — Backend: `routes/notice.js`에 `/read` 라우트 추가.

### 일반 (P2, 다음 주 처리)

6. **BUG-02 (portfolio/items 500→400)** — Backend: growth 라우트 유효성 오류 상태코드 수정 + 필수 필드 문서화.
7. **BUG-04 (셀프진단 404)** — Backend: 셀프 진단 API 구현 또는 대체 진단 흐름 확인.
8. **BUG-06 (문제집 필드명 불일치)** — Backend: `name` → `title` 별칭 추가.

### 시나리오 문서 개선

9. admin 비밀번호 `0000` → `1234` 수정 (실제 서버 기준).
10. 감정 회고 API 필드명 `text` → `content` 수정.
11. 감정 피드백 API 필드명 `student_id+message` → `user_id+content` 수정.
12. S07 선행조건에 "approved video/quiz 콘텐츠 필요 (S10과 순서 조정 권장)" 명시.
13. 쪽지 API `/api/message` → `/api/message/rooms` (memberIds 필드) 수정.

---

## 환경 메타

- **git branch**: main
- **HEAD**: 799c0a6f003b0868e64e35a121eca4aec22e4638
- **node**: v22.22.2
- **npm ci 세션 로그**: 274 packages in 6s (신규 설치)
- **DB 경로**: `data/dacheum.db`
- **DB 크기**: 1.2MB (서버 기동 초기화 후)
- **시드 사용자**: 15명 (admin, teacher1-3, student1-10, parent1, staff1, qa_temp_*)
- **preview 서버**: 사용 안 함 — 시나리오 문서 0.2.3의 `node server.js` 백그라운드 기동 방식 사용
- **Preview MCP 동작**: 이 환경에서 Preview MCP 도구 미제공 → HTTP + DB 폴백으로 전환
- **검증 방식**: HTTP API (curl) + better-sqlite3 readonly 직접 조회

---

## 자가검증 결과

```
$ git status --porcelain | grep -vE "^.. \"?(보고서/일일점검|통할)"
(출력 없음 — 소스 수정 없음)

$ git status --porcelain
(출력 없음 — 미커밋 변경사항 없음)
```

보고서 이외 경로 변경 없음 확인 완료.
