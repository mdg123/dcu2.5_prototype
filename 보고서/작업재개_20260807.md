# 작업 재개 메모 — 2026-08-06 종료분

컴퓨터 자동 종료로 작업을 세웠다. 이 문서 하나만 읽으면 이어서 진행할 수 있다.

---

## 1. 종료 시점 상태

- `main` HEAD = **`5036f39`** (커밋 완료)
- `npm test` **778/778 · fail 0** · 스모크 **84/84 · 실패 0** · `harness-stamp check` **exit 0**
- ⚠ **커밋 메시지 첫 줄에 `@` 가 섞여 있다** — PowerShell here-string 문법(`@'…'@`)을 Bash 도구에서 써서 생긴 것. 내용은 온전하다. 고치려면 `git commit --amend` + force push 가 필요한데 **미용상 문제일 뿐이라 그대로 두었다.** 다음에 Bash 로 커밋할 땐 heredoc(`<<'EOF'`)을 쓸 것.

### 재개 시 가장 먼저 확인할 것
```bash
git log --oneline -3
git status --short
node scripts/harness-stamp.js check
```
푸시·배포가 어디까지 갔는지는 아래 §2 를 보고 판단.

---

## 2. 오늘 커밋에 들어간 것 (`5036f39`)

| 영역 | 내용 |
|---|---|
| **모집단 정합 42곳** | 화면마다 손 SQL 이라 같은 반 학생 수가 달랐다. `db/class.js` 정본으로 교체 + 소스 락 별칭 무관 일반화(`test/_source-lock.js`) |
| | 삭제 계정 **한서윤**이 학생 랭킹·위험학생 명단에 실명 노출되던 것 차단 |
| | 클래스 홈 헤더 7명 ↔ 수업 게시판 5명 모순 해소(7개 반 전수) |
| | 포털 "미제출 N명" **97→66** (분모·분자가 반대 방향으로 틀려 오차 크기조차 몰랐다) |
| | `/warnings` 한 응답 안에서 명단이 갈리던 것 봉합 |
| **오늘의 학습 관리자 전용** | 교사 권한은 진입 UI 없는 API 뒷문이었다. 읽기(`SET_INSPECT_ROLES`)는 분리해 교사 점검 시야 유지. 교사 소유 세트 8건이 학생에게 그대로 보이는 것 실측 확인 |
| **학습맵 진행률 문항 기준** | 단원 노드 콘텐츠 0건 → FE 가 `in_progress → 50%` 로 숫자를 지어내고 있었다. "9까지의 수" 0/41 → **50%→0%**. 145개 중 3개만 이동. 상단 KPI("3개 진행 중"→"1개")도 일치 |
| **수업꾸러미 문항** | 평가지 플레이어(`content-player`) 재사용으로 통일. **구 화면은 정답을 초록으로 칠하고 해설까지 편 사실상 정답지였고, 그 정답이 틀렸다**(1-based 판정 vs 0-based DB). 교사 미리보기도 동일 수정 |
| **`/completion` 권한** | `requireAuth` 뿐이라 로그인한 아무나 남의 반 학생 실명·이수율 열람 가능. 모집단 복구로 이 API 가 되살아나며 위험 실체화 → `canManageClass` 게이트 |
| **히트맵 빈 열** | `weeks` off-by-one. 셀 없이 머리글만 남던 열 제거 |
| **스모크 하네스 3겹** | teardown 이 남의 DB 를 글롭 삭제 / 뿌리는 `makeSmokeDb` 가 config 로드마다 새 사본 생성(잔재 11개·**511MB** 실측) / 경로 env 고정 + 멱등화 + 청소 기준 "2시간 이상" |

### 신규 회귀 테스트
`_source-lock.js` · `class-lesson-player-quiz` · `class-population-copies` · `growth-heatmap-weeks` · `learning-completion-guard` · `lrs-warnings-population` · `self-learn-map-progress` · `smoke-temp-isolation`

---

## 2-1. 배포 — **완료됨** (2026-08-06 종료 직전)

- `git push` **`7028533 → 5036f39`** (pre-push 하네스 778/778 통과)
- GCP 배포 완료 — VM HEAD **`5036f39`** · PM2 `dacheum` **online** · 앱 에러 0
- 페이지 200 확인: `/` · `/class/class-home.html` · `/self-learn/learning-map.html` · `/growth/student-report.html`
  ⚠ 확인 시 **`/plus/` 접두어를 붙이면 404** 다. 실제 경로는 접두어 없음(과거 메모의 `/plus/…` 는 낡았다).
- 공개 URL: `https://34-22-69-18.sslip.io`

### ⚠ 실서버 기동 배너에 뜨는 것 (별건)
```
마지막 변형 : 2026-08-05T00:31:31.929Z  (rebuild-lrs-aggregates-w2a.js @ dacheum-vm)
마지막 검증 : (없음)
```
어제 **실서버에서 재집계 스크립트를 돌린 뒤 검증 스탬프가 비어 있다.** 실서버에서는 아무도 `npm test` 를 돌리지 않으므로 **차단이 아니라 가시화 수준**이고 로컬 검증과는 무관하다. 다만 배너가 계속 뜨므로 정리 대상.

---

## 3. 배포 확인이 남았다면 (참고 — 이미 완료)

배포는 아래 1줄. **`data/`·`uploads/` 는 gitignored 라 무손상.**
```bash
gcloud compute ssh dacheum-vm --zone=asia-northeast3-a --quiet --command="sudo -u ubuntu git -C /home/ubuntu/dacheum pull origin main && sudo -iu ubuntu pm2 restart all --update-env"
```
검증: VM 내부 `git log --oneline -1` 이 `5036f39` 인지 + `pm2 logs dacheum --nostream` 에러 0.

**실서버에서 눈으로 확인할 것** (오늘 바뀐 것):
1. 클래스 홈 헤더 학생 수 == 수업 게시판 "N명 중"
2. 학습맵 "9까지의 수" 가 **0%** (진행중 50% 아님)
3. 수업꾸러미에 평가지 넣고 열면 **1문항 1페이지** + 정답이 미리 칠해져 있지 않음
4. 성장리포트 30일 히트맵에 **빈 열 없음**
5. 교사 계정으로 오늘의 학습 세트 만들기 시도 → **403**

---

## 4. 감리 3차 — 중간 판정 받음 (배포 진행 가능)

시간 제약으로 **최고위험 2건 우선 판정**으로 전환했고, 아래를 받았다. 전체 리포트(`보고서/감리_배포전_최종_v3.md`)는 미작성일 수 있다.

### ① 정답 인덱스 — **방향 맞음 (조건부 OK)**
전수 5,201건 판정: **0-based 96.1% (4,998건) / 1-based 3.9% (203건)**.
보조 근거 — 4지선다 6,957건에서 1-based 해석 시 **범위 밖 1,714건**, 0-based 해석 시 **7건**. 0-based 가 정본.

> **핵심**: `content-player.html` 은 **원래부터 0-based** 였다(`q.options[q.answer]`). 즉 학생 채점은 처음부터 맞게 돌고 있었고, 이번 변경은 **교사 미리보기를 실제 채점기와 일치시킨 것**이라 학생이 배우는 답을 바꾸지 않는다.

🔴 **남은 위험 (별건 최우선)** — **1-based 로 저장된 콘텐츠 144개**는 지금도 채점이 틀린다.
`lesson_contents` 사용 0건(수업꾸러미 무영향)이나 **`node_contents` 117건 · `daily_learning_items` 126건** 에 붙어 있어 AI 학습맵·오늘의 학습에서 오답 처리된다. 실제 풀이 시도는 아직 3건(2명).
→ **"0-based 로 통일" 데이터 정합 작업**이 필요하다.

### ② 과잉 차단 — **OK, 사라진 사람 없음**
| class | SSOT | 실측 | 전체 멤버 |
|---|---|---|---|
| 1 | 7 | **7** | 10 |
| 2 | 5 | **5** | 7 |
| 3 | 4 | **4** | 6 |
| 4 | 6 | **6** | 8 |
| 1004 | 8 | 8 | 9 |
| 1005 | 24 | 24 | 25 |

**학부모(`이학부모`)·교직원(`정교직원`)은 전체 멤버 목록에 그대로 남아 있다** — 학생 분모에서만 빠졌고 명단에서 지워지지 않았다. 0명이 된 화면 없음.

### 곁가지 — 이미 OK 판정
- **`/completion` 권한** 실측: class 1 → teacher1(개설자) 200 · admin 200 · teacher2(비멤버) **403** · student1/2 **403**. class 3 → teacher2 200 · teacher1 **403**. 과잉 차단 없음
- **`/warnings` 모집단**: class 2 의 `inactive`·`weakAchievements` 모두 탈퇴 멤버 강다은 부재. 소스 락 일반화도 역주입 실증(`u2`·`stu`·`cm.role='member'` 전부 포착)
- **학습맵 수치 재현**: `9까지의 수` 0/41→**0%**, `50까지의 수` 1/40→**3%**, `100까지의 수` 0/40→0%. 주장과 정확히 일치

### 🔴 감리가 새로 찾은 것 — 내일 처리
**학습맵 게이트가 절반만 적용됐다.** `db/self-learn-extended.js:1180` 의 게이트는 **합성(synth)만 차단**하고 `user_node_status` 에 **이미 저장된 단원 행**은 손대지 않는다(`continue` 가 `rowByNode.get(u.unit_id)` 보정 전에 걸림).
실측: **uid 4(박학생/student2)** — `getLearningDashboard(4).inProgressNodes = 0` 인데 `getUserNodeStatuses(4)` 는 `9까지의 수` 를 `in_progress` 로 반환 → **KPI 0 ↔ 카드 "진행중"** 이 어긋난다.
이번 수정의 명시적 목적("게이트가 한쪽에만 있으면 어긋난다")이 저장된 행 경로에서 미달성.

소소 지적: 같은 게이트가 `getUserNodeStatuses` 에서는 `completed` 합성까지 막는데 `getLearningDashboard` 에서는 `else if (engaged>0)` 안에만 있어 **`completed` 는 게이트 밖**. 주석과 코드 불일치(현재 데이터로는 해당 0건, 잠재 결함).

### 미확인으로 남은 것
- 수업꾸러미 **동기화(lockstep)·판서·`content_attempts` 1행·이수 처리** 브라우저 실측(코드만 확인)
- 히트맵 15/30/60/90일 × 여러 기준일 프리뷰 (산식 검토는 완료)
- `.card h3` 확대 스크린샷 · `today.html` 문구 시각 확인
- 모바일 375px 시각 확인

---

## 4-1. (구) 감리 상태 메모

배포 직전 3차 감리를 돌리다 시간 제약으로 **최고위험 2건 우선 판정**으로 전환했다.
- ① 수업꾸러미 **정답 인덱스가 정말 0-based 가 맞는지** DB 표본 대조
- ② **과잉 차단** — 원래 보여야 할 학생이 사라진 화면이 있는지

**판정을 못 받고 종료했을 가능성이 크다.** 재개하면 이 두 가지를 먼저 확인할 것.
문제가 나오면 이미 커밋돼 있으므로 `git revert 5036f39` 로 되돌리기 쉽다.

리포트 예정 경로: `보고서/감리_배포전_최종_v3.md` (없으면 미작성)

---

## 5. 내일 할 일 (우선순위 순)

### 최우선
0. **1-based 로 저장된 콘텐츠 144개 정합** (감리 3차 발견) — `node_contents` 117건 · `daily_learning_items` 126건 에 붙어 있어 **AI 학습맵·오늘의 학습에서 지금도 오답 처리**된다. 0-based 로 통일하는 데이터 마이그레이션 필요.
   ⚠ 정본 DB write 라 `harness-stamp mark` + 전건 재실행 필수(CLAUDE.md 하네스 규칙 5). 판정 근거는 "해설 안에 보기 텍스트가 정확히 하나만 등장하는 문항" 방식이 유효했다.

0-1. **학습맵 게이트 절반 적용** (감리 3차 발견) — §4 참조. `db/self-learn-extended.js:1180` 이 저장된 단원 행을 안 건드려 uid 4 에서 KPI 0 ↔ 카드 "진행중" 모순. 작은 수정이나 회귀 확인 필요.

1. **정답이 API 응답에 그대로 실려 나간다** — `db/lesson.js:227` `getLessonContents` 가 `content_questions` 전 컬럼(`answer`·`explanation`)을 학생에게 내려보낸다. `/api/contents/:id` 도 동일.
   화면에선 가렸지만 **네트워크 탭이나 `contents[0].questions` 콘솔 한 줄이면 답이 보인다.**
   ⚠ 기존 호출부 3곳이 같은 페이로드에 의존하므로 응답 축소 시 회귀 확인 필수.

2. **진단 v3 종료·중단 재설계** — 기획서 완성본: `보고서/기획_진단v3_종료·중단·학습경로_v1.md`
   조사 근거: 부모 세션 대화 + (파일 미작성) — 핵심만 옮기면
   - v3 에 **"출발선 찾음 → 종료" 조건이 아예 없다.** v2/CAT 의 `position_found` 에 대응하는 코드가 한 줄도 없음
   - 원 단원 복귀는 `[P1 본류복귀 2026-06-11]` 로 **의도된 설계**이나 그 설계가 결함
   - 죽은 브레이크 2개: `DIAG_V3_CONCEPT_CAP=30` 이 **빈 if 문**, 12분 소프트스톱 미집행 → 중단 확인 모달 트리거로 연결
   - 큐 정렬이 **오름차순(낮은 학년 먼저)** — 사용자 확정은 **내림차순**(목표에 가까운 선수부터). `_gradeAbs` 가 학년·학기를 한 축으로 합쳐 놓아 비교 방향만 뒤집으면 됨. 절단(`slice(0,N)`)은 따라서 저절로 옳아짐
   - **완료된 v3 세션 18건 중 `learning_paths` 가 있는 건 2건** — `finishDiagV3(true)` 가 `/finish` 를 건너뛰어 `_applyDiagnosisV3NodeResults`·`buildRecommendedPath` 누락
   - **"이어서 진단 →" 버튼이 재개가 아니라 새 세션 생성** — "나중에 이어서" 를 제공하려면 선행 수정 필수
   - 회귀 9종(INV-DV3-1~9) 설계는 기획서에 있음

### 그 다음
3. **학생 목표 설정 기능 제거** (task #16) — 사용자 확정. `daily_learning`(8행, goals 전부 `[]`)은 죽은 시스템이고 실사용은 `daily_learning_sets` 뿐. 제거하면 "교사 배포가 학생 목표를 덮어쓴다"는 위험도 소멸
4. **완료율 100% vs 2%** (task #17) — 같은 카드에서 `rate` 가 두 뜻. 이름 분리 + 기간별로 그림이 3형태로 바뀌는 것 안내
5. **삭제 계정 잔여** (task #18) — 마일리지·알림장 등은 **멤버 전원이 분모인 게 맞다**(사용자 확인). `liveUserSql` 만 추가. `routes/admin.js:160` 이 removed·invited 까지 세는 건 별개 진성 결함
6. **JS 필터 모집단 구멍** (task #11) — `routes/lrs.js:2310`·`7245` 가 `getClassMembers().filter(m => m.user_role==='student')`. 소스 락이 SQL 만 훑어 영영 못 잡는 자리
7. **정본 DB 테스트 잔재** (task #9) — 갤러리·콘테스트에 노출 중 + 학생 소유 세트 id 14
8. **스모크 세그폴트** (task #14) — teardown 이 exit 139 로 죽으며 포트 3100 에 고아 서버를 남긴다. 매번 손으로 죽여야 함. **`reuseExistingServer:true` 로 우회하지 말 것**(삭제된 DB 를 가리키는 낡은 서버 재사용 → 더 나쁜 실패)

### 배경으로 남아 있는 것
- 기능 전수 점검 **P1 약 29건 · P2 약 35건** — `보고서/기능점검_웨이브1_결과_통합_v1.md`·`_웨이브2_…`
- task #4 W4~W5 잔여

---

## 6. 오늘 확정된 정책 (재확인 불필요)

| 항목 | 결정 |
|---|---|
| 오늘의 학습 | **관리자만** 구성·배포. 교사·학생 불가 |
| 학생 목표 설정 | **없앤다** |
| 클래스 분모 | **학생만 유지**(오늘 작업 그대로). "멤버 전원" 전환은 사용자가 보류 — 근거 수치는 task #19 에 기록 |
| 학습맵 진행률 | **문항 기준**. 영상 제외. 단원은 자식 차시 문항 **총합**(평균 아님) |
| 진단 종료 | 남은 갈래는 **다 보고**, 다 본 뒤 **원 단원 복귀 없이 종료**. 중단 시 2지선다(결과 보기 / 나중에 이어서), 중단해도 결과 + 실패 단원 학습경로 제공 |
| 출발선 판정 | **실패한 것 중 근본도가 가장 낮은 단원** (통과한 것 중 최고가 아님) |

---

## 7. 작업 시 주의 (오늘 실제로 겪은 것)

- **`git checkout/restore/stash/reset` 금지** — 과거 감리가 미커밋 작업물을 파괴한 사고. 되돌릴 땐 정확한 역-Edit
- **`routes/lrs.js` 는 NUL 바이트 2개** — `grep -a` 안 쓰면 매치가 조용히 누락(과거 37건 중 17건 누락)
- **스모크는 반드시 `--config=test/e2e/smoke.config.js`** — 파일 경로로 주면 "테스트 없음"인데 **exit 0**
- **스모크 전 포트 3100 고아 확인** — 남아 있으면 "already used" 로 통째 실패
- **Bash 로 커밋 메시지 쓸 때 heredoc(`<<'EOF'`)** — PowerShell `@'…'@` 쓰면 `@` 가 메시지에 섞인다(이번에 겪음)
- **에이전트 완료 보고를 그대로 믿지 말 것** — 오늘도 감리가 "수치 재현" 으로 REWORK 4건을 잡아냈다
- **하네스 통과 ≠ 검증 완료** — 오늘 잡힌 것 중 히트맵 빈 열·`멤버 10 명` 띄어쓰기·정답 노출은 **전부 하네스가 초록인 상태에서 사용자·감리가 눈으로 찾은 것**
