# 작업 재개 메모 — 2026-08-07 종료분

이 문서 하나만 읽으면 이어서 진행할 수 있다.

---

## 1. 종료 시점 상태

- `main` HEAD = **`a8434ef`** — 원격·GCP 모두 동기. **실서버 반영 확인 완료**
- **미커밋 1건**: `public/self-learn/learning-map.html` (+329 / −53) — 학습경로 화면 UX 재구성 **작업 중 중단**
  - 스크립트 블록 2개 **문법 파싱 OK** (반쯤 깨진 상태 아님)
  - 다만 **검증·프리뷰 미완**. 아래 §5 참조
- 하네스: 중단 직전 기준 `npm test` **861/861** · 스모크 **84/84** · `harness-stamp check` exit 0
  ⚠ 미커밋 변경이 있으므로 **재개 시 먼저 `npm test` 를 돌려 현재 상태를 확인**할 것

```bash
git log --oneline -3
git status --short
npm test
```

---

## 2. 오늘 배포 3회 — 전부 실서버 확인 완료

`https://34-22-69-18.sslip.io`

| 커밋 | 내용 |
|---|---|
| `5036f39` | 학생 모집단 정합 42곳 · 오늘의 학습 관리자 전용 · 학습맵 진행률 문항 기준 · 스모크 하네스 결함 3겹 |
| `f10bcfa` | 정답 노출 8곳 차단 · 채점 서버 이관 · 정답 인덱스 162문항 정합 · 삭제 계정 분모 5곳 · 완료율 이름 충돌 |
| **`a8434ef`** | **진단 v3 재설계** · 문항 우회 5경로 차단 · 폴백 전 과목 랜덤 제거 |

**하네스 743 → 861건** (오늘 회귀 118건 박제)

### 진단 v3 (오늘 최대 작업) — 사용자 원 지적 해소
> "일부러 틀려 이전 학년으로 이동해 출발선을 확인했는데 갑자기 원래 학년-학기 문제가 나온다. 진단이 언제 끝나는거야"

원인: **v3 엔진에 "출발선을 찾았으면 종료" 조건이 아예 없었다.** 원 단원 복귀는 의도된 설계였고 그 설계가 결함.
같은 하네스 전/후 비교에서 **구 엔진은 이 시나리오에서 한 건도 종료되지 않았다.**

확정 사양(전부 사용자 확정, 정본 = `보고서/기획_진단v3_종료·중단·학습경로_v1.md`):
- 통과 시 `position_found` 종료 · 원 단원 재출제 없음
- 큐 정렬을 목표에 가까운 학년·학기 순으로(3곳). **출발선 선택은 오름차순 유지** — 뒤집으면 가장 어려운 개념이 출발선이 된다
- 프리셋 3종(빠르게 4/3 · **표준 5/4 기본** · 꼼꼼히 차시 전부)
- **차시당 1문제** + **조기 확정 제거** → 고른 수와 실제 출제 수가 일치
- **목표 단원에도 프리셋 동일 적용** — 통과 불가능해져도 끝까지 채운 뒤 판정
- 차시를 균등 간격으로 뽑고 **앞뒤 교차**로 질문
- 실패할 때마다 **[추가 진단에 넣기] / [넣지 않기]**. **"넣지 않기"는 종료가 아니라** 그 단원 아래로만 안 파는 것
- 문항 하드 상한(200) **제거** — 유한성은 구조가 보장(visited 단조 증가 + branchVerdicts 재진입 차단 + downCount 20)

실측 문항 수: 전형 **빠르게 20 · 표준 25 · 꼼꼼히 51** / 최악 **82 · 99 · 194**, 미종료 0건

---

## 3. 🔴 재개 시 가장 먼저 — 미완료 작업 1건

### 학습경로 탭 UX 재구성 (미커밋, 검증 미완)

**사용자 지적**: "내 학습 경로에서 목록이 많다보니까 **실제 학습해야 하는 목록이 매우 아래에 있음.** 사용자 중심 UI 로."

현재 화면 문제 — 이력 9개(전부 진행률 0%)를 지나야 STEP 1 이 나온다.

**PM 이 제안하고 사용자가 승인한 구조**:
```
🎯 지금은 여기부터
   소수의 곱셈          초5-2 · 수와 연산
   진단 1/4 (25%) · 15차시 · 약 75분
                        [ 학습 시작 → ]

전체 경로   ●──○──○   0/3 완료

STEP 2  소수의 나눗셈(1)              [시작]
STEP 3  소수의 나눗셈(2)  🔒 STEP 2를 마치면 열려요

▸ 지난 학습 경로 9개 보기          ← 접힘(기본)
```

원칙: ①"지금 할 것"이 스크롤 없이 ②이력은 접기 ③진행 미니맵 ④**잠금에 이유 표기** ⑤**강조 CTA 1개**(현재 하단에 강한 버튼 2개 — `CLAUDE.md` 위반)

**이력 자체도 정리 대상**:
- `8월 6일 23시간 전` 3건이 **같은 시각** — 한 번의 진단이 3행으로 보인다. 진단 단위로 묶을 것
- `평면도형의 이동` 이 6월 9일에 **두 번**
- 진행 중인 것 우선, 0% 방치분은 접기
- 빈 상태(진단 이력 없는 학생) 설계도 필요

### 🔴 함께 확인할 숫자 모순 (아직 원인 미규명)
화면: **`진단 단원 3개 · 통과 1 · 복습 필요 3`** — **1 + 3 = 4인데 3개**.
계산 위치: `public/self-learn/learning-map.html:10736~10740` 부근의 `totalNodes` · `passedNodes` · `failedNodes`.

셋 중 하나:
- **통과 단원이 경로에 잘못 들어감** ← 오늘 확정한 규칙 위반(통과 단원은 경로 제외)
- 요약 숫자가 중복을 셈
- "진단 단원" 이 다른 집합을 셈

⚠ **로컬 DB 에 오늘 세션이 없다**(사용자가 실서버에서 진단). 격리 사본에서 진단을 직접 수행해 재현할 것.
⚠ **진단 엔진(BE)을 고쳐야 하는 문제면 손대지 말 것** — 오늘 막 배포하고 감리를 거쳤다.

### 회귀로 박을 것
`test/self-learn-path-ui.test.js`(신규) — INV-PU1 "지금 할 것"이 이력보다 먼저 렌더 / PU2 이력 기본 접힘 / PU3 잠금 이유 문구 / PU4 강조 CTA 1개 / PU5 요약 수치 무모순

---

## 4. 오늘 사용자가 확정한 정책 (재확인 불필요)

| 항목 | 결정 |
|---|---|
| 오늘의 학습 | **관리자만** 구성·배포 |
| 학생 목표 설정 | 없앤다 (미구현, task #16) |
| 클래스 분모 | **학생만 유지**. "멤버 전원" 전환은 보류(근거 수치는 task #19) |
| 학습맵 진행률 | 문항 기준 · 영상 제외 · 단원은 자식 총합 |
| 진단 종료 | §2 참조 |
| 재응시 평가 정답 공개 | **그대로 둔다**(다시 제안 말 것) |
| 지수표기 식별자 우회 | **대응 안 함** — UI 도달 불가, F12 콘솔이 필요해 현실적 위협 아님 |
| 문항-차시 매핑 | **프로토타입이라 그대로** — 문항 내용으로 차시 성격을 추론하지 말 것 |
| 차시명 7건 | **나중에 DB 에서** 직접 UPDATE (task #25 에 SQL 준비됨) |

---

## 5. 내일 우선순위

### 최우선
1. **학습경로 UX 재구성 마무리** (§3) — 미커밋 상태라 가장 먼저 정리
2. **숫자 모순 원인 규명** (§3)

### 작은 것 (묶어서 한 번에 배포 권장 — 배포 고정비가 ~1시간)
3. 정답키 손상 **35건** (30건이 동일 템플릿 → 규칙 하나 + 개별 5건, 그중 1건은 사람 판단 필요)
4. `answer="0.0"` **11건** (문항 2종, `"0"` 으로 포맷 정규화만)
5. 차시명 7건 DB UPDATE (task #25)
6. 학생 목표 기능 제거 (task #16)
7. 완료율 이름 충돌 — **교사용 반 대시보드**에도 같은 병(task #17 참조)
8. 삭제 계정이 아직 세지는 곳 + `admin` 멤버 수(task #18 은 완료, 잔여는 없음)
9. LRS 위험학생 "N명" 이 행 수(task #22) · 알림장 역할 라벨(task #23)

### 확인 필요
10. **학습맵 게이트 절반 적용** — `db/self-learn-extended.js:1180` 이 저장된 단원 행을 안 건드려 uid 4 에서 KPI 0 ↔ 카드 "진행중" 모순
11. 비학생 진단 결과가 학생 통계에 섞이는지(task #24)
12. 정본 DB 테스트 잔재(task #9) — 오늘 `content_view` 1건 추가 오염(4개 테이블 파생)
13. 스모크 세그폴트 → 포트 3100 고아(task #14)

### 배경
기능 전수 점검 **P1 약 29건 · P2 약 35건** — `보고서/기능점검_웨이브1_결과_통합_v1.md`·`_웨이브2_…`

---

## 6. 작업 시 주의 (오늘 실제로 겪은 것)

- **`git checkout/restore/stash/reset` 금지** — 되돌릴 땐 정확한 역-Edit
- **`routes/lrs.js` 는 NUL 바이트** — `grep -a` 안 쓰면 매치가 조용히 누락
- **스모크는 반드시 `--config=test/e2e/smoke.config.js`** — 파일 경로로 주면 "테스트 없음"인데 **exit 0**
- **스모크 전 포트 3100 고아 확인** — 남아 있으면 "already used" 로 통째 실패
- **Bash 커밋 메시지는 heredoc(`<<'EOF'`)** — PowerShell `@'…'@` 쓰면 `@` 가 메시지에 섞인다
- **`git add 보고서/증적` 금지** — 오늘 262개·62MB 가 딸려 들어가 되돌렸다. `.gitignore` 에 넣어 뒀다
- **정본 DB 는 `--apply` 두 번 돌리지 말 것** — 오늘 3행 이중 차감 + `rollback.sql` 소실 사고. 멱등성은 **사본에서** 확인
- 🔴 **단언을 조건문 안에 가두지 말 것** — 오늘 이 함정이 **다섯 번** 나왔다(낡은 단언이 몇 달간 잠듦 / 빈 답안 누출을 테스트가 못 봄 / INV-DV3-8 이 게이트를 안 때림 / 재출제 제거로 중단 확인이 죽음 / 역주입 2건이 안 붉어짐)
- **에이전트 완료 보고를 그대로 믿지 말 것** — 오늘 "했다"고 보고했으나 실제로는 안 된 사례가 있었고(증적 보존 가드), 감리가 코드를 열어 잡았다
- **하네스 통과 ≠ 검증 완료** — 오늘 잡힌 것 중 히트맵 빈 열·`멤버 10 명` 띄어쓰기·빈 답안 정답 누출은 **전부 하네스가 초록인 상태**에서 사용자·감리가 눈으로 찾았다
- **기획서에 모순을 쌓지 말 것** — 결정이 바뀌면 **그 자리에서 앞 서술을 고칠 것**. 오늘 §1-B 가 네 번 뒤집혀 낡은 절을 근거로 잘못 지시한 적이 있다
