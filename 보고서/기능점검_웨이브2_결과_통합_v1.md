# 기능 전수 점검 웨이브 2 — 통합 결과 (v1)

2026-08-05 · 대상 HEAD `da9482f` · 3팀 병렬(격리 DB 사본 + 전용 포트)
증적: `보고서/증적/기능점검_W2_T4_20260805/`(13장) · `_W2_T5_`(27건) · `_W2_T6_`(21장)
정본 DB 무변형 — 3팀 전부 `harness-stamp check` exit 0

**집계: P0 3건 · P1 20건 · P2 20건**

---

## P0 (3건)

### P0-5. 학습맵 문제 채점이 1칸 밀려 **오답을 정답으로 저장** `W2-T4-1`
- 재현: student1 → 학습맵 차시 → `openSolve` 경로 → 6개짜리 문제에 **④ 2** 선택
- 화면: **"✅ 정답입니다! 그림에는 🍎가 6개 있습니다. 따라서 정답은 6입니다."** (자기모순)
- API: `POST /api/self-learn/problem-attempt` → `{correct:true, correctAnswer:"6"}`
- DB: `problem_attempts.is_correct = 1`, `selected_answer='2'`
- 원인: `learning-map.html:7208` `submitSolve` 가 `(idx+1).toString() === answer` 로 **1-based** 판정. 정본 규약은 **0-based**(`judgeQuestionAnswer`, `content-player.html:662`). 게다가 FE 가 `question_id` 를 안 보내 **서버 재채점이 발동하지 않고 클라 판정을 그대로 신뢰**한다.
- **PM 실측**: `content_questions` 11,886건 중 **`answer='0'` 이 2,275건(19%)** — 1-based 로는 정답이 아예 존재할 수 없다.
- 현재 피해 규모는 작다(`problem_attempts` 48행) — `openSolve` 경로 사용이 적었던 것으로 보인다. **방치하면 성취 데이터가 조용히 오염된다.**
- 오답노트 재풀이·문제집 제출은 **서버 재채점이 정상 작동**(클라 `isCorrect:true` 무시)하므로 이 경로 한정.

### P0-6. 오늘의 학습 세트 API 권한 전면 개방 `W2-T4-2`
student1 세션으로 전부 **200 + DB 반영**:
| 요청 | 결과 |
|---|---|
| `POST /daily/sets` | 세트 생성(`teacher_id=3`=학생 본인, 임의 학년) |
| `PUT /daily/sets/{교사세트}` | 제목이 "학생이 제목 바꿈"으로 변경 |
| `DELETE /daily/sets/{id}/items/{itemId}` | 교사 세트 항목 삭제 |
| `POST /daily/{타학년 itemId}/complete` | 3학년 항목을 4학년 학생이 이수 + 포인트 적립 |

**PM 실측 확인** — `routes/self-learn.js` 4개 라우트 전부 `requireAuth` 만:
```
router.post('/daily/sets', requireAuth, …)
router.put('/daily/sets/:setId', requireAuth, …)
router.post('/daily/sets/:setId/items', requireAuth, …)
router.delete('/daily/sets/:setId/items/:itemId', requireAuth, …)
```
같은 저장소의 `routes/class-mileage.js` 는 `requireClassMember, requireOwner` 를 정확히 쓴다 — **올바른 패턴이 이미 있는데 여기만 빠졌다.**

### P0-7. 학생이 클래스 설정 화면 전면 진입 → **비공개 클래스 초대코드 유출** `W2-T6-1`
- **PM 실서버 실측**: student1(일반 멤버) → `GET /api/class/2` → `200 | myRole=member | code=UE9LT9` (3개 클래스 전부 코드 노출)
- 화면(`manage.html`)이 그 200 만 보고 렌더 — `myRole==='owner'` 게이트 **부재**. 초대코드 + 복사·QR, 클래스명·공개여부 편집, 멤버 관리, **"클래스 삭제" 버튼**까지 표시.
- GNB `채움클래스 > 클래스 관리` 항목에 `roles` 제한이 없어 URL 조작 없이 도달.
- **`POST /api/class/join` 이 `is_public` 을 보지 않는다** — 코드만 맞으면 비공개 클래스도 가입. 초대코드가 유일한 접근 통제인데 그것이 멤버에게 노출된다.
- 파괴적 API(수정·삭제·강퇴·역할변경)는 전부 403 정상 차단. **유출되는 것은 정보와 UI.**

---

## P1 (20건)

### 권한·개인정보
| ID | 내용 |
|---|---|
| `W2-T5-1` | **같은 반 학생이 반 전원 감정·이유·실명 열람.** `routes/attendance.js:167 GET /:classId/today` 가 주석은 "(교사용)"인데 `requireMember` 만. 옆 라우트(`/class-stats`·`/table`)엔 있는 owner 검사가 여기만 빠졌다. 실측 응답에 `emotion_reason:"수학 시험이 걱정돼서 마음이 무거워요"` 포함. **교사가 `is_public=0` 으로 꺼도 API 는 200 + 전원 데이터** — 게이트가 FE 에만 있다. **W3(`4d6e478`)은 `routes/growth.js` 만 고쳤고 `attendance.js` 는 손대지 않았다.** (PM 실서버 확인: 학생이 200 수신 — 게이트 부재 확정. 당일 체크인 0건이라 페이로드는 비어 있었음) |
| `W2-T6-6` | 명예의 전당 API 가 학생에게 **전국 학생 20명 실명·학교·학년** 반환. **화면은 이 배열을 쓰지도 않는다.** (PM 실서버 확인: `topLearners 20명`) |

### 기능 불능
| ID | 내용 |
|---|---|
| `W2-T4-3` | **미배포(`is_active=0`) 세트가 학생 화면에 노출** — `getDailySets` 에 필터 없음 |
| `W2-T4-4` | 관리자 배포 세트 **"수정"이 항상 500** — UI 가 `is_active` 를 boolean 으로 전송, better-sqlite3 가 거부 |
| `W2-T4-5` | 관리자 **"삭제" 버튼이 죽어 있음** — `DELETE /daily/sets/:setId` 라우트 미존재(404) |
| `W2-T4-8` | **문항 0개 콘텐츠가 오늘의 학습 21항목·문제집 24항목에 편성** — 클릭해도 풀 수 없고 100% 완료가 구조적으로 불가 |
| `W2-T4-9` | 진단 결과가 노드 상태에 반영 안 됨 — `user_node_status.diagnosis_result` 13행 중 10행 NULL, `completed` 0행 |
| `W2-T5-3` | **주/월 출석률이 항상 0%** — FE 가 참조하는 `this_week_rate`·`this_month_rate`·`best_streak` 를 서버가 안 보냄 |
| `W2-T6-7` | **강퇴·탈퇴 후 재가입 영구 불가** — `removeMember` 가 `status='removed'` 행을 남기는데 `addMember` 가 UNIQUE 충돌을 "이미 멤버"로 삼킴. 학생 재가입 409, **교사 재초대도 409**. 복구 경로 없음 |
| `W2-T6-8` | **"세션 7일 유지"가 무효** — `server.js:17` 이 기동마다 `sessions.db` 를 무조건 삭제 → 배포할 때마다 전 사용자 강제 로그아웃. `SESSION_SECRET` 폴백이 저장소에 커밋돼 있음(GCP env 설정 여부 미확인) |

### 숫자·라벨
| ID | 내용 |
|---|---|
| `W2-T5-2` | **클래스 공동 목표 300%** — 분모가 `class_members.role==='student'` 인데 그 컬럼 값은 `owner`/`member` 뿐 → 항상 0 → `\|\| 1`. **한 명만 출석해도 목표 달성** |
| `W2-T5-4` | 출석부 모집단에 **학부모·교직원 혼입** — CSV `4,이학부모,-,-,-,0,0,-`. 총원·출석률·랭킹 분모 오염 |
| `W2-T5-5` | 같은 화면에 **"평균 출석률 4%"** 와 **"개근자 10명"**(전원) 동시 — 미기록(-)을 개근으로 셈 |
| `W2-T5-6` | 랭킹 모집단 필터 전무 — class1 랭킹에 **김선생(교사) 9위**. 탈퇴자 출석 29건도 집계 |
| `W2-T5-7` | **출석 날짜가 UTC** — `new Date().toISOString().slice(0,10)`. **KST 00:00~08:59 체크인이 전날로 기록**(등교 시간대가 정확히 이 구간). 같은 저장소 `db/lrs-school.js:146` 은 localtime 사용 — 기준 불일치 |
| `W2-T4-6` | 화면 **"+77P 획득"** 인데 실제 적립 **10P** — `daily_learning_items.point_value` 가 적립에 안 쓰이고 `system_settings` 고정값 사용 |
| `W2-T4-7` | 오답노트 "정답"에 **원시 인덱스 노출** — "정답 = 4"인데 실제는 "6". 361건 중 52건(14%) |
| `W2-T4-10` | 단원 카드 진행률 **하드코딩 50%** — 실제 차시 진행률은 6%/0%/0%/0%/0% |
| `W2-T4-11` | **"오늘" 라벨에 전체 분모** — `today_total=2657`(전 학년·전 날짜), `ai_total=1307`(전 노드). 이미 지적된 "유령 분모" 재발 |
| `W2-T6-2` | **인기 클래스 랭킹이 구조적 영구 공백** — 산식이 `learning_logs.class_id` 에 의존하는데 156,430건 중 **783건(0.5%)** 만 값이 있고 최근 30일은 0건. `homework_submit` 11,332건 중 11,305건 NULL |
| `W2-T6-3` | 크리에이터 랭킹 = 기간 내 업로드 **건수** → 공백이거나 **`관리자`(시드 계정) 독식**. `deleted_at`·`status` 필터 없음 |
| `W2-T6-4` | 뱃지 등급 근거 불명 — 범례는 "다이아 10회"인데 산식은 `min(활동수/5, 12)`. **활동 216건과 17건이 똑같이 "다이아 12회"** |
| `W2-T6-5` | 명예의 전당 "출석률"·"피드백" 막대가 **항상 0** — API 가 두 축을 반환하지 않음 |

---

## P2 (20건, 요약)

**CBT**: `tab_events` 감사로그 짝 불일치(leave 1 : return 6, `tab:return` 에 throttle 없음) · 감독 화면 제출시각이 새로고침 전후 9시간 점프 · **socket.io 로드 실패 시 가짜 학생 7명 렌더**(`loadMockStudents`)
**출결·감정**: 한마디 30자 서버 검증 없음(120자 저장됨) · 자동출석 선행 시 한마디 영구 불가 · "멤버별 라벨" 미구현 · 시간대별 감정 차트 X축 UTC · DB 감정 값이 5종 스케일 밖 8종 혼재
**게이미피케이션**: `best_streak` 미구현 → 뱃지와 모순 · 뱃지 사실상 미작동(`attendance_badges` 전체 2행, 교사 `PATCH /cell` 은 미지급, UNIQUE 제약 없음) · `late` 1회로 스트릭 0인데 교사 KPI 는 late 를 출석으로 계산 · `isWeekendGap` 이 금→일(2일)도 통과 · 공휴일·방학 미고려
**스스로채움**: 0문항 진단도 `completed` 마감 · 보정 경로가 자기 단원 1개 · 진단 범위 이탈(초1 단원 진단에 5학년 문항) · 진단 모달 `max-width:1280px` 로 90vw 규격 미달 · 학습맵 교과가 수학 단독, 라벨 `수학`/`수학과` 이중 · 노드 상세가 미승인 영상 노출 · 오답노트 "평균 재도전 0.0" ↔ 개별 "1~2회" · 재이수 시 `progress.score` 는 갱신되나 `learning_logs` 는 최초값 유지
**진입 동선**: 포털에서 "공지사항"이 **"외부" 배지**로 표시 · `/plus/external.html` 이 5종을 전부 "채움코딩"으로 표시 · "인기 콘텐츠"가 실제로는 학년 추천(조회수 1~4회짜리가 "인기") · "가입신청" 버튼이 승인 없이 즉시 가입 · 회원가입 역할 4종 자기신고(학부모·교직원 포함, `staff` 는 교장 대시보드 집계에 반영)
**공동담임**: `class_members` `CHECK(role IN ('owner','member'))` 로 INSERT 불가 + API 400 + UI 옵션 없음. 대체 경로("개설자 권한 부여")는 **클래스 삭제 권한까지 주는 과잉 권한**이고, `growth.js`·`lrs.js`·`class-mileage.js`·`lesson-player.html` 의 `co_teacher` 분기는 전부 죽은 코드

---

## 🔴 T4-5 문항 매핑 전수 — 사용자 지적 사항의 실체

**차시(level 3) 1,162개는 100% 매핑되어 있다.** 죽은 클릭의 실제 원인은 둘이다.

| 구간 | 전체 | 연결 0건 | 비율 |
|---|---|---|---|
| level 2 (단원) | 145 | **145** | **100%** |
| level 3 (차시) | 1,162 | 0 | 0% |

① **단원 노드 145개 전부가 콘텐츠 0건** → 단원 상세는 "등록된 문제가 없어요"만 뜬다(안내는 있음).
② **문항 0개인 승인 quiz 37건이 학생 화면에 편성돼 있다** — 오늘의 학습 21항목, 문제집 24항목. 여기서 "클릭해도 안 되는" 경험이 나온다.

**연결은 있으나 못 여는 것**: `node_contents` 10,634 링크 중 **213건(2.0%)이 `status='rejected'` 영상**. 그 결과 승인 영상이 0인 차시 27개(그중 18개는 연결 영상이 전부 미승인). 카드는 "영상 0개"인데 **상세 API 는 그 영상을 그대로 반환**한다.

**교과 분포**: 수학/수학과 **1,307개(100%)**, 국어·사회·과학·영어 **0개**. 수학 내부는 결손 없음(초1~고1 전 학년, 차시별 승인 문항 보유율 100%).

**화면에서 사용자가 보는 것**: 무반응·콘솔 에러는 **없다**(콘솔 에러 0건). 문제는 ① 풀 수 없는 항목을 애초에 편성해 둔 것 ② `openSolve` 폴백이 문항이 없으면 **가짜 보기 `보기1~보기4` 를 만들어 띄우고 제출까지 받는 것**(`learning-map.html:7172·7177`)이다.

---

## 전 항목 PASS — 회귀 시 지켜야 할 기준선

**CBT 이탈 감지**(RFP 필수) — 실시간 반영·누적시간 정확(3→9→11초)·Throttle(80ms 연타 6회에 +1)·제출 후 유지·권한 분리
**수업꾸러미 동기화** — 강제이동·잠금·추종·늦은 입장·새로고침 복원·**학생 소켓 제어권 탈취 차단**·OFF 해제·교사 이탈 시 fail-open·**서버 재기동 시 자동 해제**. **교사 자신은 잠기지 않음**(과거 H-1 결함 재발 없음)
**감정출석부 기능 자체** — 이모티콘 5종 + 이유 저장·캘린더 표시 · 주간/월간 회고(KST 보정 정확) · 교사 리포트(분포·요일·시간대·관심학생) · **교사 피드백이 학생 화면에 실제 도달**
**인증** — 비밀번호 **752행 전수 bcrypt**(평문 0), 가입 시 `role=admin`·`principal` 차단, 중복 아이디 409, 비밀번호 형식 검증, 쿠키 `httpOnly`+`sameSite=Lax`
**클래스 권한** — 강퇴 후 접근 403 + 친절 안내, 학생의 파괴적 조작 전부 403, owner 자기 강퇴 방지
**비로그인 포털** — 개인 데이터 0, 인기 콘텐츠에 미승인·비공개 혼입 0, 썸네일 404 0
**외부 서비스 9종** — 전부 "외부" 배지 + "준비 중" 토스트, 죽은 링크 0
**마일리지** — 잔액 == SUM(log) 11/11 정합, 지급·차감·회수·규칙·CSV 권한 분리 정확
**오답노트** — 자동 수집, **서버 재채점이 클라 `isCorrect:true` 를 무시**
**날짜 귀속 정책** — 진도=배정일 / 스트릭=활동일 분리 정상
**가로 스크롤 0** — 전 팀 전 화면 375·1200·1440

---

## 환경 — 웨이브 2에서 추가로 드러난 것

1. **`routes/self-learn.js` 의 DB 경로 하드코딩이 9곳이고, 그중 347·491·624행은 `__dirname` 기반이라 `DB_PATH` 도 cwd 변경도 우회 불가.** 491행은 **쓰기**(`/map/nodes/:id/start`). 팀4는 **소스 트리 전체를 복사**해야만 격리할 수 있었다.
2. **Windows 에서는 `server.js` 의 `sessions.db` 삭제가 EBUSY 로 실패**한다(unlink 예외를 조용히 삼킴). 웨이브 2 §0-A 함정 1의 전제와 달라, 팀 간 세션 단절이 실제로는 안 일어날 수 있다. **반대로 Linux(GCP)에서는 정상 삭제되므로 배포마다 전원 로그아웃**이 맞다.
3. 팀4 방식(소스 트리 전체 복사 + junction)이 가장 안전한 격리였다 — `server.js` 의 세션 삭제도 `__dirname` 기반이라 다른 팀에 영향이 없었다.

---

## 미확인 (추측 배제)

세션 소실 상태의 서버 재기동 시 학생 잠금 잔존(Windows EBUSY 로 재현 실패, 코드 추론만) · KST 00:00~09:00 출석의 전날 귀속(점검 시각이 창 밖, 코드로만 확정) · 랭킹 6위 이하 노출 시 교사 표시 · 탈퇴자 랭킹 혼입 화면 실측 · `loadMockStudents` 가짜 학생 렌더 · 오늘의 학습 기간 일괄 배포 · 진단 v3 이외 구 경로 · 영상 시청 완료 → 노드 승격 · `collectFallbackProblems` 폴백 2·3 도달 · GCP `SESSION_SECRET` env 설정 · 세션 7일 실경과 만료 · 클래스 정원 제한 의도 · 명예의 전당 레이더 모달
