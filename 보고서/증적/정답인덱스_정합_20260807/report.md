# content_questions.answer 인덱스 기준(0-based) 정합 — 분석 리포트

> ## 🔴 정정 공지 (2026-08-07, 감리 지적 반영)
>
> **이 파일 본문은 "적용이 끝난 뒤 다시 돌린" 분석 결과다. 적용 실적이 아니다.**
> 스크립트의 `OUT_DIR` 기본값이 `--db` 와 무관하게 늘 이 폴더였던 탓에, 적용 이후의
> 재실행·사본 검증 산출물이 원래 증적을 덮어썼다. 그래서 아래 §4·§5 가
> "대상 0건 / 과거 제출 영향 0건" 으로 적혀 있으나 **사실과 다르다**.
> (근본 원인은 `scripts/fix-answer-index-base.js` 의 OUT_DIR 기본값 — 같은 날 수정했다.
>  이제 사본 대상 실행은 사본 옆 폴더에 쓰고 이 폴더를 덮지 않는다.)
>
> ### 실제로 적용된 것
>
> | 항목 | 실제 값 | 근거 |
> |---|---|---|
> | UPDATE 된 문항 | **162건** | `changes.csv` 162 레코드 · `rollback.sql` 162 UPDATE |
> | 대상 콘텐츠 | **132개** | `changes.csv` 의 서로 다른 content_id |
> | 판정 내역 | ONE_EXPLAIN 159건 + MANUAL_INCLUDE 3건 | `changes.csv` verdict 열 |
> | 되돌리기 | `rollback.sql` (162행, 유효) | 보존 규칙으로 살아남음 |
>
> ### 실제 과거 제출 영향 — **0건이 아니다**
>
> `content_attempts` 19건을 현재 정답키로 전수 재채점한 결과 **1건**이 옛 정답키로
> 기록돼 있었다(2026-08-07 재검증, `scripts/regrade-content-attempts.js`):
>
> | attempt | content | user | 선택 | 기록된 값 | 올바른 값 |
> |---|---|---|---|---|---|
> | 3 | 196 | 4 (student2) | `[1, 3]` | 0/2 · 0% | **2/2 · 100%** |
>
> 나머지 18건은 저장값과 재계산값이 일치했다(단 attempt 32 는 `answers` 가 `null` 이라
> 재채점 불가 — 문항별 선택 기록이 없어 검증 대상에서 제외). 이 1건은 재채점해 반영했고,
> 되돌리기는 `보고서/증적/채점정합_content_attempts_20260807/rollback.sql` 에 있다.
>
> ### 함께 남은 사고 기록
>
> `--apply` 를 두 번 돌려 MANUAL_INCLUDE 3행(q69·q227·q228)이 2회 차감됐고
> `repair-double-decrement.sql` 로 수리했다. 현재 DB 값은 q69=3 · q227=1 · q228=3 으로
> 의도한 최종값과 일치한다(2026-08-07 실측). 재발 방지 가드는 스크립트의
> `MANUAL_INCLUDE.expect` 멱등 검사다.

- 생성: 2026-08-07T02:06:58.853Z
- 대상 DB: `C:\Users\user\OneDrive - 금성초등학교\바탕 화면\다채움 품질 제고사업 프로토타입 - 실동작\data\dacheum.db`
- tier: `explain` / 모드: ANALYZE(읽기전용)

## 1. 문항 모집단

| 항목 | 값 |
|---|---|
| question_type in (choice, multiple_choice) | 11878 |
| 분석 가능(options 배열 ≥2 & answer 정수) | 11878 |
| 제외 | 0 |
| answer === 0 (1-based 불가 → 0-based 확정) | 2337 |
| answer === options.length (0-based 불가 → 1-based 후보) | 35 |
| 어느 해석으로도 범위 밖 | 0 |
| ⚠ answer 가 실수 문자열("0.0" 등) | 11 |

## 2. 해설 신호 (보조)

| 신호 | 문항 |
|---|---|
| 0-based 일치 | 5992 |
| 1-based 일치 | 20 |
| 모호(보기 2개 이상 등장) | 3238 |
| 불일치(어느 해석과도 안 맞음 = 정답키 손상 의심) | 388 |
| 근거 없음 | 2240 |

## 3. 콘텐츠 단위 판정

| 판정 | 콘텐츠 | 문항 | 처리 |
|---|---|---|---|
| ZERO_VETO | 2212 | 4198 | 0-based 확정 — 변환 금지 |
| ONE_EXPLAIN | 2 | 2 | 해설로 1-based 확인 — **변환 대상** |
| ONE_STRUCT_ONLY | 1 | 2 | 구조 증거만 — 정답키 손상 의심, 변환 제외 |
| CONFLICT | 42 | 91 | 신호 충돌 — 손대지 않음(사람 판단) |
| NO_EVIDENCE | 7294 | 7585 | 증거 없음 — 손대지 않음 |

## 4. 변환 계획

> ⚠ 아래 "0개 / 0건" 은 **적용이 끝난 뒤 재실행한 시점** 기준이다(= 남은 대상이 없다는 뜻).
>   실제 적용 실적은 **162건 / 132콘텐츠** — 상단 정정 공지와 `changes.csv`·`rollback.sql` 참조.

- 대상 콘텐츠: **0개** (자동 판정 2 + 수동 편입 3건이 속한 콘텐츠)
- 대상 문항(실제 UPDATE): **0건** (`answer -= 1`)
- 안전 가드로 건너뛴 행: 2건

### 수동 편입 멱등 가드 — 건너뛴 행

- q69(content 85) 기대 answer=4 인데 현재 3 → 편입 건너뜀 (이미 변환 완료된 것으로 보임 — 정상)
- q227(content 196) 기대 answer=2 인데 현재 1 → 편입 건너뜀 (이미 변환 완료된 것으로 보임 — 정상)
- q228(content 196) 기대 answer=4 인데 현재 3 → 편입 건너뜀 (이미 변환 완료된 것으로 보임 — 정상)
- 롤백: `보고서\증적\정답인덱스_정합_20260807\rollback.sql`
- 변경 목록: `보고서\증적\정답인덱스_정합_20260807\changes.csv`

### 건너뛴 행
| qid | content | 사유 |
|---|---|---|
| 10103 | 8279 | 수동 제외 — 보기 ["같거나 작은","같거나 큰","같은","작은"] / 정답은 "같거나 큰"(idx1). 해설의 "크거나 같은" 중 "같은" 조각만 매칭돼 1-based 로 오판. 변환해도 오답 → 정답키 재작성 필요 |
| 10339 | 8515 | 수동 제외 — 보기 ["4, 5, 3, 4","5, 4, 4, 4","4, 4, 4, 5","5, 4, 4, 3"] / 현재 answer=2 → "4, 4, 4, 5" 로 **이미 정답**. 해설 말미 "큰 순서: 5, 4, 4, 4"(재정렬 서술)에 매칭돼 오판. 변환하면 멀쩡한 문항이 오답 처리됨 |

## 5. 영향 범위

| 참조 테이블 | 대상 콘텐츠 참조 건수 |
|---|---|

### 과거 제출 재채점 대상 (자동 재채점 안 함 — 목록만)

> ⚠ 아래 "0건" 은 재실행 시점에 **대상 콘텐츠 집합이 비어 있어서** 나온 값이지
>   "영향이 없었다" 는 뜻이 아니다. 실제로는 `content_attempts` **1건**(attempt 3,
>   student2, content 196, `[1,3]` → 0/2·0% 로 잘못 기록)이 영향을 받았다.
>   상단 정정 공지 참조. 재채점은 `scripts/regrade-content-attempts.js` 로 반영했다.

- content_attempts 중 판정이 바뀌는 건: **0건**

- problem_attempts 중 대상 콘텐츠 관련 행: **0건** (문항 id 컬럼이 없어 문항 단위 판정 불가 → 전량 재검토 후보)

## 6. 별도 판단 필요

- 신호 충돌 콘텐츠: 42개 → `conflicts.md`
- 구조 증거만(정답키 손상 의심): 1개 → `struct-only.md`
- answer 실수 문자열("0.0"): 11건 — 채점기는 문자열 비교(`String(pick)===String(answer)`)라 **영구 오답**. 별도 정규화 필요.

| qid | content | answer | 보기수 |
|---|---|---|---|
| 42 | 58 | "0.0" | 4 |
| 43 | 59 | "0.0" | 4 |
| 161 | 176 | "0.0" | 4 |
| 176 | 179 | "0.0" | 4 |
| 177 | 179 | "0.0" | 4 |
| 182 | 180 | "0.0" | 4 |
| 183 | 180 | "0.0" | 4 |
| 190 | 181 | "0.0" | 4 |
| 191 | 181 | "0.0" | 4 |
| 206 | 183 | "0.0" | 4 |
| 209 | 184 | "0.0" | 4 |
