# A4b — 판정 임계 일관성 (2026-08-05)

A-4 배포(`311ca86`) 후 GCP 실서버 student1(전 셀 `attempts<3`) 화면에서 드러난 3건 + 감리 REWORK 5건의 증적.

## 재현 방법

정본 DB·GCP 무접촉. `VACUUM INTO` 격리 사본으로 서버를 별도 포트에 띄우고, **GCP 실측 페이로드를
Playwright `page.route` 스텁으로 주입**해 촬영했다(로컬 실데이터는 판정 완료 셀이 있어 이 상태가
재현되지 않는다).

| 픽스처 | 내용 |
|---|---|
| GCP 재현 | `counts{total:14, reached/partial/notReached:0, insufficient:14}` · `attempts>0` 7건 · `attempts>=3` 0건 |
| 4상태 혼합 | 도달 1 · 부분(70%) 1 · 부분(60%) 1 · 미도달(20%) 1 · 평가부족 1 |
| 추세 | `status:ok · r²=0.02 · confidence:low · 관측 4주 · 이번 주 100%(2문제) · 최근 2주 35→100` |

## 파일

### 1차 (G1~G3)

| 파일 | 내용 |
|---|---|
| `before_01_G1_도넛중앙_14대14_1440.png` | 도넛 중앙 `14 / 14` — "14개 중 14개 달성"으로 오독. 실제 도달 0개 |
| `before_02_G2_평가부족이_미도달빨강_1440.png` | 부제는 "판정이 끝난 성취기준만"인데 `insufficient` 셀 2개가 빨간 미도달 막대로 |
| `before_03_G2_추천카운터_보완오분류_1440.png` | `보완 1개 · 심화 0개 · 미판정 14개` — 평가부족 추천을 "보완"으로 계수 |
| `before_04_G3_저신뢰_단정문구_1440.png` | r²=0.02 인데 `이미 정답률 80% 수준이에요 · 이대로 꾸준히! · 지난주보다 65점 올랐어요!` |
| `after_01_G1_도넛중앙_0개평가된기준_1440.png` | `0개 / 평가된 기준` — 분모 0 자체를 표기 |
| `after_02_G2_판정전_빈상태_부속UI숨김_1440.png` | 판정 완료 0건 → 빈 상태 + 부속 UI(표시 개수·범례) 숨김 |
| `after_03_G2_추천카운터_SSOT어휘_1440.png` | `추천 1장 — 보완 0개 · 아직 판정 못 함 1개 · 심화 0개` |
| `after_04_G3_저신뢰_참고용배지_1440.png` | `변동이 커서 참고용` + 수준 단정 제거 + 얇은 표본 고지 |

### 2차 (감리 REWORK R-1·R-4)

| 파일 | 내용 |
|---|---|
| `before_06_R1_도넛부라벨_회색링겹침_375.png` | 375px — 부라벨 `아직 평가된 기준 없음` **111px** vs 도넛 구멍 **92px** → 좌우 각 9~10px 가 회색 링 침범(회색 글자 × 회색 링). **A4b 1차본이 만든 회귀** |
| `after_06_R1_도넛중앙_구멍내부_375.png` | 375px — `평가된 기준` **59px** vs 구멍 92px, 여유 **33px**. 플러그인이 `innerRadius` 실측으로 `maxWidth` 를 걸어 구조적으로 재발 차단 |
| `before_07_R4_역주입_부분도달이빨강_1440.png` | 감리 R5 역주입(막대 색 = 버킷 기반) — 정답률 60% "부분 도달"이 미도달 빨강(`#ef4444`)으로 강등 |
| `after_07_R4_색계약_4상태혼합_1440.png` | 막대 색 = 상태 색(`#16a34a`/`#f59e0b`/`#f59e0b`/`#ef4444`), 좌·우 버킷이 색을 바꾸지 않음 |

### 실데이터 (격리 사본, 스텁 없음)

| 파일 | 내용 |
|---|---|
| `after_08_실데이터_강약_1440.png` | 판정 완료 5건 전부 도달(초록) · "아직 약점으로 판정된 성취기준이 없어요" |
| `after_09_실데이터_도넛_375.png` | `5 / 5` + `도달 / 평가된 기준` — 2줄로 접혀 구멍 안(여유 11px) |

## 하네스

- `test/e2e/smoke.spec.js` — `INV-A4-9`(임계 일관성 + 색 계약 + 부속 UI + 어휘 SSOT) · `INV-A4-10`(도넛 중앙 비율 계약) · `INV-A4-11`(저신뢰 단정 금지). `INV-A4-5` 의 세 번째 렌더러는 추천 카운터 → 강·약 부제로 이전.
- `test/regression.test.js` — `INV-K9` 12 → **16조합**(얇은 표본 축 추가). 과거엔 `valuedPts` 에 `attempts` 가 없어 `thinRecent` 분기가 한 번도 검사되지 않았다.
